"""Tagless unit test — backfill fixture (DATA, not collected by the real suite).

Its title matches the REMOVED FR-05.09 wording and no active FR, so the engine
must classify it as a *possible* orphan — a review candidate, never a confirmed
stale-feature accusation (Spec §11-R4).
"""


def test_copies_launch_command_to_clipboard():
    assert True
