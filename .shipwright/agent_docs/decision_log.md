<!-- Adoption merge marker — top of file is /shipwright-adopt scaffold,
     followed by the existing decision log preserved verbatim.
     Pre-existing entries: up to ADR-008 (8 ADR section(s) detected).
     Adoption ADR: ADR-009.
     Original file backed up to .shipwright/adopt/backups/. -->

# Decision Log — shipwright

_This log was bootstrapped from existing prior art at `README.md#architecture` — see the **Imported decisions** section below for the verbatim contents._

### ADR-009: Adopt this repository into the Shipwright SDLC

- **Status**: accepted
- **Date**: 2026-05-02
- **Commit**: `87dbf72a8614bcf85ab28c2793517800fa907450`

#### Context

This repository existed with 0 detected feature(s) and substantive git history before /shipwright-adopt ran. The goal is to bring it under the Shipwright SDLC (CLAUDE.md + .shipwright/agent_docs + .shipwright/planning/ + .shipwright/compliance/ + configs) without disrupting the existing codebase.

#### Decision

Adopted into Shipwright using profile `python-plugin-monorepo` and scope `library`. Retroactively marked `completed_steps = ["project", "plan", "build", "test"]` so that `/shipwright-iterate` and downstream skills (`/shipwright-compliance`, `/shipwright-test`) work as on a natively-built project.

#### Consequences

- Future changes MUST go through `/shipwright-iterate` (not `/shipwright-project`/`/shipwright-plan`/`/shipwright-build`).
- Compliance reports (RTM, SBOM, change-history) are seeded; test-evidence starts collecting from the first `/shipwright-test` run.
- Any existing E2E baseline auto-generated by Adopt (under `e2e/flows/adopted-baseline.spec.ts`) is a regression guard, not a substitute for real acceptance tests.

#### Rejected alternatives

- Manual `/shipwright-project` init: would lose git history and force re-description of existing code.
- No adoption (ad-hoc `/shipwright-iterate`): would mean missing configs, no compliance reports, and the audit pipeline would silently no-op.

---

### ADR-010: Plugin-per-phase architecture

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
Shipwright spans many distinct SDLC phases (project, plan, build, test, security, deploy, changelog, compliance, iterate, design, preview, adopt). A monolithic skill would have grown to thousands of lines and entangled phase-specific dependencies, hooks, and review chains.

#### Decision
Each SDLC phase is implemented as its own Claude Code plugin under plugins/<phase>/ with isolated pyproject.toml, hooks/hooks.json, skills/<phase>/SKILL.md, and tests. Cross-plugin code lives under shared/.

#### Consequences
Plugins evolve independently and have small surface area. The trade-off is a runtime plugin cache (~/.claude/plugins/cache/shipwright/) that must be re-synced after plugin-side changes via scripts/update-marketplace.sh. Hooks and the event log carry inter-plugin state.

---

### ADR-011: Hooks as single source of truth for between-phase actions

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
Between-phase quality gates (canon checks, drift checks, phase-quality stop hook, compliance updates) need to execute deterministically regardless of which plugin invoked the next phase.

#### Decision
All between-phase actions and quality gates are wired in plugins/<phase>/hooks/hooks.json and centrally documented in docs/hooks-and-pipeline.md. SKILL.md files do not duplicate hook behavior in prose; they reference the doc.

#### Consequences
Adding or changing a phase requires updating both hooks.json and docs/hooks-and-pipeline.md; the doc is the authoritative reference for who reads/writes which artifact and when each gate fires.

---

### ADR-012: External LLM review at SDLC gates (plan/iterate/code)

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
Self-review by the same LLM that wrote the plan or code has a strong confirmation bias. Single-reviewer dual-LLM checks (Gemini + GPT in parallel) caught real blockers (e.g. monorepo-auto-descent-guard semantics) that would otherwise have shipped invisible.

#### Decision
All external LLM review flows through one shared CLI: shared/scripts/tools/external_review.py, with three modes: plan (full plan vs project spec), iterate (mini-plan vs iterate spec), code (diff vs section/iterate spec). Each mode runs Gemini + GPT in parallel via OpenRouter and writes a marker file used by compliance.

#### Consequences
Non-trivial plans gain ~60s + cents-per-review of overhead in exchange for catching blocker-class bugs early. Marker files (external_review_state.json, external_code_review_state.json) are committed as compliance evidence. The underlying lib (llm_review.py) is implementation detail; skills must call the CLI.

---

### ADR-013: Per-scanner exclude lists for security tools

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
Trivy and Gitleaks do not honor .gitignore by default, so secrets stored in fixture or test directories were being flagged as production findings. Semgrep does honor gitignore.

#### Decision
Each scanner gets its own exclude list rather than collapsing on a single gitignore-as-SSoT model. .worktrees/ was added after a benchmark showed scan times improved.

#### Consequences
Slightly more configuration per scanner, but findings now match developer intent. Replaces the original "gitignore is SSoT for all scanners" thesis from earlier iterates.

---

### ADR-014: Artifact relocation under .shipwright/

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
Top-level directories named planning/, designs/, agent_docs/, and compliance/ collided with target-project directories of the same names and made it hard to tell which artifacts belonged to Shipwright vs the user's app.

#### Decision
Relocated all four artifact roots under .shipwright/<artifact>/ (planning, designs, agent_docs, compliance). Hard-cutover migration with sub-iterates A through G per artifact, plus a per-scanner gitignore-exclude pass (sub-iterate H). Documented as the standard pattern in docs/migrations/artifact-migration-reference.md.

#### Consequences
All Shipwright-owned artifacts live under .shipwright/. e2e/ stayed top-level (Playwright convention) and CHANGELOG-unreleased.d/ stayed top-level (Keep-a-Changelog sibling-to-CHANGELOG convention). Configuration JSON files (shipwright_*_config.json) and events.jsonl stayed top-level; their relocation was evaluated and deferred.

---

### ADR-015: /shipwright-iterate as primary post-adoption interface

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
Once a project is adopted into Shipwright, users still need a low-friction way to make code changes — fixes, small features, refactors — without invoking the full project/plan/build/test/deploy chain.

#### Decision
/shipwright-iterate is the canonical entry point for ongoing changes in any completed Shipwright project. It is complexity-adaptive: trivial changes proceed directly while medium-or-larger iterates run mini-planning, external review, TDD, and a structured review cascade. /shipwright-adopt installs the suggest_iterate hook to route ad-hoc change prompts to it.

#### Consequences
Users do not need to remember which SDLC phase to invoke for a given change. The trade-off is that /shipwright-iterate has substantial internal logic (intent + complexity classifier, sub-iterate spawning) that the other phases delegate to.

---

### ADR-016: Public-ready git history rewrite

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
The repo started with the maintainer's company email in commit metadata. Going public required cleaning that without losing history.

#### Decision
On 2026-04-30 the entire history (524 commits) was retroactively rewritten to use the GitHub-noreply email, then force-pushed. Documented as a one-time migration; future commits use the noreply email by default.

#### Consequences
Commit SHAs changed for every commit. External clones and forks made before that date no longer share history. Worth it for public-readiness.

---

## Imported decisions (verbatim from `README.md#architecture`)

_Copied during /shipwright-adopt onboarding. Original ADR numbering and ordering preserved. Future decisions land above this section, not within it._

## Architecture

```
shipwright/
├── plugins/                          # Claude Code plugins (one per SDLC phase)
│   ├── shipwright-run/               # Pipeline Initializer
│   ├── shipwright-project/           # Requirements decomposition (IREB)
│   ├── shipwright-design/            # UI mockups (HTML)
│   ├── shipwright-plan/              # Deep planning + external LLM review
│   ├── shipwright-build/             # TDD implementation
│   ├── shipwright-test/              # Test runner (unit/smoke/E2E)
│   ├── shipwright-security/          # Scanners + remediation
│   ├── shipwright-deploy/            # Deployment (extensible flavors)
│   ├── shipwright-changelog/         # Changelog + PR
│   ├── shipwright-compliance/        # Traceability, RTM, SBOM, dashboard
│   ├── shipwright-iterate/           # Daily iteration (complexity-adaptive)
│   ├── shipwright-preview/           # Local browser preview
│   └── shipwright-adopt/             # Brownfield onboarding (analyze existing repos)

---

## Existing decision log (preserved during adoption)

# Decision Log — shipwright

_This log was bootstrapped from existing prior art at `README.md#architecture` — see the **Imported decisions** section below for the verbatim contents._

### ADR-001: Adopt this repository into the Shipwright SDLC

- **Status**: accepted
- **Date**: 2026-05-02
- **Commit**: `8da26f338b0510167f12e16d990c0f57b07c27fa`

#### Context

This repository existed with 0 detected feature(s) and substantive git history before /shipwright-adopt ran. The goal is to bring it under the Shipwright SDLC (CLAUDE.md + .shipwright/agent_docs + .shipwright/planning/ + .shipwright/compliance/ + configs) without disrupting the existing codebase.

#### Decision

Adopted into Shipwright using profile `python-plugin-monorepo` and scope `library`. Retroactively marked `completed_steps = ["project", "plan", "build", "test"]` so that `/shipwright-iterate` and downstream skills (`/shipwright-compliance`, `/shipwright-test`) work as on a natively-built project.

#### Consequences

- Future changes MUST go through `/shipwright-iterate` (not `/shipwright-project`/`/shipwright-plan`/`/shipwright-build`).
- Compliance reports (RTM, SBOM, change-history) are seeded; test-evidence starts collecting from the first `/shipwright-test` run.
- Any existing E2E baseline auto-generated by Adopt (under `e2e/flows/adopted-baseline.spec.ts`) is a regression guard, not a substitute for real acceptance tests.

#### Rejected alternatives

- Manual `/shipwright-project` init: would lose git history and force re-description of existing code.
- No adoption (ad-hoc `/shipwright-iterate`): would mean missing configs, no compliance reports, and the audit pipeline would silently no-op.

---

### ADR-002: Plugin-per-phase architecture

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
Shipwright spans many distinct SDLC phases (project, plan, build, test, security, deploy, changelog, compliance, iterate, design, preview, adopt). A monolithic skill would have grown to thousands of lines and entangled phase-specific dependencies, hooks, and review chains.

#### Decision
Each SDLC phase is implemented as its own Claude Code plugin under plugins/<phase>/ with isolated pyproject.toml, hooks/hooks.json, skills/<phase>/SKILL.md, and tests. Cross-plugin code lives under shared/.

#### Consequences
Plugins evolve independently and have small surface area. The trade-off is a runtime plugin cache (~/.claude/plugins/cache/shipwright/) that must be re-synced after plugin-side changes via scripts/update-marketplace.sh. Hooks and the event log carry inter-plugin state.

---

### ADR-003: Hooks as single source of truth for between-phase actions

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
Between-phase quality gates (canon checks, drift checks, phase-quality stop hook, compliance updates) need to execute deterministically regardless of which plugin invoked the next phase.

#### Decision
All between-phase actions and quality gates are wired in plugins/<phase>/hooks/hooks.json and centrally documented in docs/hooks-and-pipeline.md. SKILL.md files do not duplicate hook behavior in prose; they reference the doc.

#### Consequences
Adding or changing a phase requires updating both hooks.json and docs/hooks-and-pipeline.md; the doc is the authoritative reference for who reads/writes which artifact and when each gate fires.

---

### ADR-004: External LLM review at SDLC gates (plan/iterate/code)

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
Self-review by the same LLM that wrote the plan or code has a strong confirmation bias. Single-reviewer dual-LLM checks (Gemini + GPT in parallel) caught real blockers (e.g. monorepo-auto-descent-guard semantics) that would otherwise have shipped invisible.

#### Decision
All external LLM review flows through one shared CLI: shared/scripts/tools/external_review.py, with three modes: plan (full plan vs project spec), iterate (mini-plan vs iterate spec), code (diff vs section/iterate spec). Each mode runs Gemini + GPT in parallel via OpenRouter and writes a marker file used by compliance.

#### Consequences
Non-trivial plans gain ~60s + cents-per-review of overhead in exchange for catching blocker-class bugs early. Marker files (external_review_state.json, external_code_review_state.json) are committed as compliance evidence. The underlying lib (llm_review.py) is implementation detail; skills must call the CLI.

---

### ADR-005: Per-scanner exclude lists for security tools

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
Trivy and Gitleaks do not honor .gitignore by default, so secrets stored in fixture or test directories were being flagged as production findings. Semgrep does honor gitignore.

#### Decision
Each scanner gets its own exclude list rather than collapsing on a single gitignore-as-SSoT model. .worktrees/ was added after a benchmark showed scan times improved.

#### Consequences
Slightly more configuration per scanner, but findings now match developer intent. Replaces the original "gitignore is SSoT for all scanners" thesis from earlier iterates.

---

### ADR-006: Artifact relocation under .shipwright/

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
Top-level directories named planning/, designs/, agent_docs/, and compliance/ collided with target-project directories of the same names and made it hard to tell which artifacts belonged to Shipwright vs the user's app.

#### Decision
Relocated all four artifact roots under .shipwright/<artifact>/ (planning, designs, agent_docs, compliance). Hard-cutover migration with sub-iterates A through G per artifact, plus a per-scanner gitignore-exclude pass (sub-iterate H). Documented as the standard pattern in docs/migrations/artifact-migration-reference.md.

#### Consequences
All Shipwright-owned artifacts live under .shipwright/. e2e/ stayed top-level (Playwright convention) and CHANGELOG-unreleased.d/ stayed top-level (Keep-a-Changelog sibling-to-CHANGELOG convention). Configuration JSON files (shipwright_*_config.json) and events.jsonl stayed top-level; their relocation was evaluated and deferred.

---

### ADR-007: /shipwright-iterate as primary post-adoption interface

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
Once a project is adopted into Shipwright, users still need a low-friction way to make code changes — fixes, small features, refactors — without invoking the full project/plan/build/test/deploy chain.

#### Decision
/shipwright-iterate is the canonical entry point for ongoing changes in any completed Shipwright project. It is complexity-adaptive: trivial changes proceed directly while medium-or-larger iterates run mini-planning, external review, TDD, and a structured review cascade. /shipwright-adopt installs the suggest_iterate hook to route ad-hoc change prompts to it.

#### Consequences
Users do not need to remember which SDLC phase to invoke for a given change. The trade-off is that /shipwright-iterate has substantial internal logic (intent + complexity classifier, sub-iterate spawning) that the other phases delegate to.

---

### ADR-008: Public-ready git history rewrite

- **Status**: accepted (retroactive, llm-inferred)
- **Commit**: ``

#### Context
The repo started with the maintainer's company email in commit metadata. Going public required cleaning that without losing history.

#### Decision
On 2026-04-30 the entire history (524 commits) was retroactively rewritten to use the GitHub-noreply email, then force-pushed. Documented as a one-time migration; future commits use the noreply email by default.

#### Consequences
Commit SHAs changed for every commit. External clones and forks made before that date no longer share history. Worth it for public-readiness.

---

## Imported decisions (verbatim from `README.md#architecture`)

_Copied during /shipwright-adopt onboarding. Original ADR numbering and ordering preserved. Future decisions land above this section, not within it._

## Architecture

```
shipwright/
├── plugins/                          # Claude Code plugins (one per SDLC phase)
│   ├── shipwright-run/               # Pipeline Initializer
│   ├── shipwright-project/           # Requirements decomposition (IREB)
│   ├── shipwright-design/            # UI mockups (HTML)
│   ├── shipwright-plan/              # Deep planning + external LLM review
│   ├── shipwright-build/             # TDD implementation
│   ├── shipwright-test/              # Test runner (unit/smoke/E2E)
│   ├── shipwright-security/          # Scanners + remediation
│   ├── shipwright-deploy/            # Deployment (extensible flavors)
│   ├── shipwright-changelog/         # Changelog + PR
│   ├── shipwright-compliance/        # Traceability, RTM, SBOM, dashboard
│   ├── shipwright-iterate/           # Daily iteration (complexity-adaptive)
│   ├── shipwright-preview/           # Local browser preview
│   └── shipwright-adopt/             # Brownfield onboarding (analyze existing repos)

---

### ADR-017: Repo cleanup post self-adoption: webui drift, legacy plans, FR populate
- **Date:** 2026-05-02
- **Section:** Iterate — change: post-adoption framework cleanup
- **Context:** Audit of the 2026-05-02 self-adoption surfaced 6 follow-up findings: stale webui sibling-directory references in CONTRIBUTING.md harvested into conventions.md, untracked legacy iterate plans, placeholder FR in spec.md, test-fixture noise in known_issues.md.
- **Decision:** Manual cleanup only (no plugin code changes): rewrite CONTRIBUTING.md to point at separate shipwright-webui repo, re-generate conventions.md from clean source, commit legacy iterate plans in-place under .shipwright/planning/iterate/ with README, populate spec.md FR-01.01..FR-01.13 from enrichment.json, add disclaimer header to known_issues.md, update test_profile_matcher to expect python-plugin-monorepo match.
- **Commit:** pending
- **Rationale:** Re-generating adopt artifacts is safer than manual conventions.md edit because preserve_existing.py:22-23 documents that agent_docs are overwritten on re-run; manual edits would be silently overwritten next time anyone re-runs adopt.
- **Consequences:** Repo state is now drift-free; 13 FRs available for RTM/compliance; 225 adopt tests green. Iterate 2 (adopt plugin durable fixes) is the second half of the post-adoption cleanup epic and addresses the framework-side root causes.
- **Rejected:** Touch adopt plugin in this iterate (deferred to Iterate 2 with external review); invent a _archive/ directory pattern (no framework backing, kept iterate plans in-place).

---

### ADR-018: Adopt plugin: drift detection, test-fixture filter, compliance fallback fix
- **Date:** 2026-05-02
- **Section:** Iterate — change: shipwright-adopt durable fixes (Iterate 2 of 2)
- **Context:** Self-adoption audit (2026-05-02) surfaced 3 bug classes in the shipwright-adopt plugin that produced silent-drift artifacts: prior_art_harvester verbatim-copied stale CONTRIBUTING.md path refs, known_issues inventory included test-fixture TODOs as real findings, and compliance_bridge fallback path failed for 3 of 5 generators with ModuleNotFoundError.
- **Decision:** Sub-2A: harvest_conventions(project_root, excludes=None) signature, default-None preserves backwards-compat. Post-harvest validation scans cd <path> tokens in fenced code blocks; excluded or absent paths get a <!-- adopt-drift: ... --> marker prepended to the block. Sub-2B: known_issues TODO scanner default-skips test-shaped paths (tests/ anywhere, test_*.py, *_test.py, *.test.ts, *.spec.ts) via _is_test_fixture_path heuristic; opt-in via scan_tests=True. Sub-2C: compliance_bridge.run_lib_fallback now also inserts lib_dir.parent.parent (= compliance plugin root) on sys.path so generators that do from scripts.lib.X import Y can resolve the scripts namespace package.
- **Commit:** pending
- **Rationale:** Sub-2A annotate-not-strip preserves harvested content while making drift visible to operators. Sub-2B default-skip is safe because broad grep over plugins/*/tests/ at audit time found 0 real workflow TODOs in plugin test directories; --scan-tests opt-in handles repos that legitimately track TODOs in tests. Sub-2C the sys.path fix is more chirurgical than refactoring relative imports across the compliance plugin; lives only in the adopt plugin and avoids cross-plugin coupling.
- **Consequences:** Future adoptions of brownfield repos with stale sibling-directory references in CONTRIBUTING.md will see drift markers in the generated conventions.md instead of inheriting the drift silently. known_issues.md no longer drowns real TODOs in test fixtures by default. compliance_bridge fallback is now consistent across all 5 generators. 233 adopt tests green (225 prior + 8 new TDD). External LLM iterate review skipped because the plan-level review (Plan v2-v3) already covered all sub-iterates.
- **Rejected:** Strip drift sections (too aggressive — verbatim-with-marker is non-destructive). Hard-skip tests/ everywhere (no opt-in escape would penalize repos with legitimate test TODOs). Refactor compliance generators to relative imports (cross-plugin churn for a 1-line fix in adopt).

---

### ADR-019: Hook installer writes canonical matcher-group shape
- **Date:** 2026-05-02
- **Section:** Iterate — bug: hook-installer-shape
- **Context:** /shipwright-adopt installed the UserPromptSubmit hook in the legacy bare-command shape (top-level type/command keys); Claude Code's parser requires the canonical matcher-group shape with an inner hooks array. The shipwright monorepo's self-adopt produced a settings.json that Claude Code rejected with 'Expected array, but received undefined', killing all permissions and hooks for the project.
- **Decision:** hook_installer.py now appends a matcher-group entry ({hooks:[{type,command}]}) instead of a bare command. The reader keeps both-shape detection for idempotency against settings.json files written by buggy pre-fix releases.
- **Commit:** PENDING
- **Rationale:** All plugin hooks.json files already use the canonical shape; the project-level installer was the lone outlier.
- **Consequences:** Future /shipwright-adopt runs produce parseable settings.json. Existing buggy installs are detected as already_present so re-running install does not double-add. New regression test (test_detects_legacy_shape_a_install) locks in the backward-compat path.
- **Rejected:** (a) Removing the hook entirely — no plugin hooks.json registers suggest_iterate, so this would silently delete the feature on every adopted project. (b) Switching the writer's command from CLAUDE_PLUGIN_ROOT to CLAUDE_PROJECT_DIR — that is a separate path-resolution bug deferred to its own iterate.

---

### ADR-020: Quote uv-run path placeholders + upgrade legacy hook entries (Shape + command) in place
- **Date:** 2026-05-03
- **Section:** Iterate — bug: suggest_iterate quoted-path + Shape A→B upgrade-in-place
- **Context:** On Windows projects whose path contains spaces (OneDrive-synced "AI Backup - Documents", Windows usernames with spaces, paths under "Program Files"), the suggest_iterate UserPromptSubmit hook installed by /shipwright-adopt and /shipwright-project emitted an unquoted shell command. Builds on ADR-019 which had just fixed the carrier shape (Shape A → Shape B) but left the command literal unquoted.
- **Decision:** Wrap the path in double quotes and prefix uv run with --no-project in (a) _HOOK_COMMAND, (b) the documented Shape B JSON snippets in shipwright-project + shipwright-run SKILL.md, (c) every uv-run-with-placeholder snippet across plugins/*/skills/, plugins/*/agents/, and shared/scripts/ docstrings. The hook installer also upgrades any of six legacy command literals in place across BOTH Shape A (carrier replaced wholesale with canonical Shape B) and Shape B (sub.command rewritten) carriers, reporting via a new upgraded:bool field.
- **Commit:** pending
- **Rationale:** External code review (Gemini + GPT via OpenRouter) flagged that recognition-without-rewrite leaves bugs latent on re-run; empirical 12-combination round-trip (Shape A/B × 6 legacy commands) confirms full upgrade-in-place coverage. Layers cleanly on ADR-019 — the carrier shape and command literal are independent failure modes, both must be fixed.
- **Consequences:** Already-adopted projects are fixed by re-running /shipwright-adopt regardless of whether they had the pre-ADR-019 Shape A entry, the post-ADR-019 Shape B entry with unquoted command, or any partial fix. Documentation snippets the LLM agent renders into shell now teach the right pattern. New project convention: any uv-run shell command with a placeholder path argument must be double-quoted; hook commands installed into target projects must additionally pass --no-project.
- **Rejected:** Generating the hook command via shlex.quote with the resolved plugin path embedded literally — would hard-code the version-numbered cache path and break on plugin upgrade. The ${CLAUDE_PLUGIN_ROOT} indirection is what lets plugin upgrades work without re-running adopt. Also rejected: sweeping plugins/*/hooks/hooks.json between-phase commands — different blast radius (only fails when the plugin install path contains spaces, today only on Windows usernames with spaces); follow-up.

---

### ADR-021: Adopt scaffolds .env.local with profile + framework keys (Layer-3 SSoT)
- **Date:** 2026-05-03
- **Section:** Iterate — feature: adopt scaffolds .env.local from validate_env framework set
- **Context:** Brownfield onboarding via /shipwright-adopt never wrote a .env.local even though the framework reads from it (load_shipwright_env in shared/scripts/lib/env.py, which external_review.py and check-external-review-keys.py both depend on). Operators had to invent the file by hand and the Step H handoff didn't tell them what keys mattered.
- **Decision:** Adopt's generate_adoption_artifacts.py now invokes shared/scripts/validate_env.init_env_file(project_root, 'all', profile_dir, include_framework=True) after run_config write and before compliance seeding. validate_env grew a _SHIPWRIGHT_FRAMEWORK_VARS constant (OPENROUTER_API_KEY, GEMINI_API_KEY, OPENAI_API_KEY in the same fallback order as external_review_config.is_external_review_enabled). init_env_file gained an include_framework kwarg (default False — preserves --init CLI semantics), a hard-stop on .gitignore failure (action=skipped), an export KEY= parser branch, and a rich return contract (action, path, vars, framework_keys, missing_keys). SKILL.md gains Step E.5 + Step H Next-steps banner block.
- **Commit:** PENDING
- **Rationale:** Framework keys live in validate_env.py (single hardcoded list mirrored by an AST-based drift-protection test against external_review_config.py) rather than scattered across N profile JSONs, so the LLM-review key set has one home. .gitignore hard-stop matches the existing security invariant: never stage a secrets file in a repo where the ignore rule cannot be enforced. Both external reviewers (Gemini + OpenAI via OpenRouter) reviewed the plan AND the diff; HIGH and MEDIUM findings on AC2/AC6 contract, drift-protection rigor, payload shape, and gitignore append cases were folded back into the spec and tests before commit.
- **Consequences:** Every brownfield-adopted repo now has a single canonical .env.local SSoT for both profile-specific build/deploy keys and the framework-level review keys. Re-running adopt is byte-equal idempotent. Foundation for the v0.4 shipwright-security phase_parameters work — the new secrets surface can read from the same load_shipwright_env() rather than inventing a second source. One incidental fix: corrected pre-existing broken assertion in test_skips_deploy_phase (deploy phase has worked since 2026-03-30 commit 2ae53b4 but the test was never updated).
- **Rejected:** Add LLM keys to every profile JSON (scatters the same three entries across 3+ files, drift hotspot). Run validate_env via subprocess from the SKILL.md (LLM has to remember to invoke; integration tests can't directly assert behavior). Scaffold .env.example as a checked-in convention (no concrete consumer today; deferred). Generic phase=all framework merge for direct CLI users (changes shared semantics; gated behind include_framework instead).

---

### ADR-022: Quote ${CLAUDE_PLUGIN_ROOT} in plugins/*/hooks/hooks.json

> Parallel-iterate note: numbering 022 (not 021) because ADR-021 was
> reserved for Sven's parallel `iterate/adopt-env-local-scaffold`
> (env.local scaffolding) which landed first locally.

- **Date:** 2026-05-03
- **Section:** Iterate — bug: hooks.json quoting (deferred from ADR-020)
- **Context:** Every plugins/*/hooks/hooks.json embeds uv-run/bash with unquoted ${CLAUDE_PLUGIN_ROOT}. Same shell word-splitting bug as ADR-020 (suggest_iterate UserPromptSubmit hook), but trigger is the plugin-install-path containing spaces — today only fires for users whose Windows username has a space (e.g. 'John Doe' → ~/.claude/plugins/cache/shipwright/... = C:\Users\John Doe\...). Empirically reproduced via end-to-end shell test: unquoted path → uv exits 2 with 'Failed to spawn: c:\Users\you\My'. Deferred from ADR-020 with explicit out-of-scope marker; closed here.
- **Decision:** Repo-wide sweep of plugins/*/hooks/hooks.json (12 files, ~78 command strings): wrap ${CLAUDE_PLUGIN_ROOT}/path/to/script in escaped double quotes inside the JSON value. Add shared/tests/test_hooks_json_quoting.py as parametrized regression that walks every plugin's hooks.json and asserts the AC-1 invariant — future hooks added without quoting fail the test, not the user's machine. NOT adding --no-project: between-phase hooks need uv to resolve plugin-local pyproject.toml deps.
- **Commit:** pending
- **Rationale:** Empirical: end-to-end test reproduces the exact failure (exit 2 + 'Failed to spawn: c:\Users\you\My') and verifies the canonical fix (exit 0). Same as ADR-020 TEST 1, applied to the between-phase hooks.
- **Consequences:** Users with Windows usernames containing spaces (or anyone with a non-default plugin install path containing spaces) get functional Stop/SessionStart/PreToolUse/UserPromptSubmit hooks. Test prevents regression. Diff is purely text — no behavior change for users on space-free install paths.
- **Rejected:** Adding --no-project to between-phase hooks: rejected because they need pyproject.toml resolution for plugin-local deps. Rejected: 'wait until a user reports it' — 5-min mechanical fix with regression test, low risk.

---

### ADR-023: Detect Git-Bash MSYS path-mangling in changelog drop bullets
- **Date:** 2026-05-03
- **Section:** Iterate — bug: changelog MSYS path-mangling linter
- **Context:** During v0.15.0 release prep, an Added bullet appeared as 'C:/Program Files/Git/shipwright-adopt now scaffolds ...' instead of '/shipwright-adopt now scaffolds ...'. Caught at dry-run by hand. Root cause: Git-Bash on Windows auto-converts a leading-slash argv arg into the Bash install root before the receiving Python script sees it (verified locally: 'python -c "print(sys.argv[1])" /foo' → 'C:/Program Files/Git/foo'). aggregate_changelog.py was blind to this and would have published the mangled bullet verbatim.
- **Decision:** Add a release-time linter to aggregate_changelog.py that flags drop-file bullets whose first line starts with the regex '^C:/Program Files/Git/[A-Za-z0-9._-]+(?=[\s/]|$)'. Default behavior: stderr WARN per finding, aggregation continues. New --strict flag (also exposed on the CLI) elevates to AggregatorError + non-zero exit, recommended for release CI. write_changelog_drop.py also emits a defensive WARN at write-time so the operator can catch it earlier. NEITHER auto-rewrites the bullet — legitimate references to paths under Git's install dir would be false positives.
- **Commit:** pending
- **Rationale:** Empirical: bug class confirmed by rerunning 'python -c "..." /foo' locally during this iterate. Detection-without-rewrite is the safer default per the 'verify reviewer findings' memory rule (auto-rewrite risks operator-intended path references). The two-layer defense (write + aggregate) ensures the operator gets at least one chance to catch it even if WARN scrolls off.
- **Consequences:** Future releases catch the bug at aggregator time (or earlier at write time) instead of needing manual dry-run inspection. CI can opt into hard-fail via --strict. New JSON output field msys_mangled_findings exposes the data programmatically. Backward compatible: existing callers without --strict see only added stderr lines on hit.
- **Rejected:** Auto-rewrite at write time: false positives on legitimate paths like 'C:/Program Files/Git/cmd/git.exe ...'. Detect at the shell level via shell wrapper: out of Python's reach. Block the bug class entirely by quoting argv differently inside the Iterate skill: the skill is a textual prompt, not a shell wrapper, can't fix the operator's local Bash. Hard-fail by default: would break existing release automation; opt-in via --strict matches Sven's autonomy preference.

---

### ADR-024: Boundary Tests Foundation
- **Date:** 2026-05-03
- **Section:** Iterate — feature: boundary tests foundation (campaign iterate-skill-hardening Sub-Iterate A)
- **Context:** The 2026-05-03 env-iterate shipped two latent producer/consumer bugs (UTF-8 BOM + inline-comment stripping) that survived 47 unit tests AND two external LLM reviews. Each side's tests passed against a stub representation; drift between sides was invisible until a real round-trip probe surfaced it.
- **Decision:** Encode the lesson as a first-class iterate concept: new touches_io_boundary risk flag in classify_complexity.py (with diff-driven helper is_io_boundary_change), new references/boundary-probes.md (8 edge-case categories), new references/round-trip-tests.md (producer→file→consumer pattern + duplicated-consumer drift protection), 7th Self-Review item Affected Boundaries, Boundary Probe sub-step in Build TDD (Path A 6a, Path B 6a, Path C 5), Phase Matrix row, Override Classes Safety-enforced.
- **Commit:** PENDING
- **Rationale:** Sven caught the env-iterate bugs by demanding an empirical producer→file→consumer round-trip — the cheap fix would have been a one-off test, but the systemic fix is to make every future iterate ask the boundary question up-front. Self-adopting the rule into the skill keeps it from being a tribal-knowledge gotcha.
- **Consequences:** Future iterates touching .env, hooks.json, settings.json, *_config.json, *_state.json (or matching producer/consumer keywords) get the boundary-probe gate automatically. Risk-floor min_complexity=small with enforces=round_trip_test. Path-match covers all known real-world cases; AST-pair detection deferred.
- **Rejected:** Add a generic round-trip mantra to Self-Review without taxonomy support — too easy to skip; risk-flag-driven gating ensures the prompt itself surfaces when probing is mandatory. AST-pair detection now — deferred per Implementation Plan because path-match catches all known cases empirically.

---

### ADR-025: Confidence Calibration phase added to iterate skill (ADR-025)
- **Date:** 2026-05-03
- **Section:** Iterate — feature: confidence calibration phase (Sub-Iterate B)
- **Context:** Confidence collapses without empirical anchor. On the 2026-05-03 env-iterate, 'are you confident?' was answered 'yes' twice — and twice a probe afterwards found a real bug. Three rounds established the asymptote: not done until at least one probe finds nothing.
- **Decision:** Add Step 7.5 Confidence Calibration to Path A between Self-Review and Full Code Review. Mandatory at medium+, Safety-enforced at small with touches_io_boundary, Advisory otherwise. Backed by references/confidence-anti-patterns.md (anti-pattern + asymptote + decision rule + cross-refs).
- **Commit:** PENDING
- **Rationale:** The 'are you confident?' question is unfalsifiable — same brain that wrote the bug being asked if it sees the bug. Replacing the question with named probes converts confidence-attestation into evidence-of-exhaustion at constant marginal cost (one probe ~5min vs hotfix cost).
- **Consequences:** Iterate runs at medium+ now have a structural calibration gate before F0. Self-attestation ('yes I'm confident') replaced by 4 named probes + findings in the iterate spec. Drift-protection tests (test_confidence_anti_patterns_doc.py + test_skill_phase_matrix.py) catch accidental section/row removal.
- **Rejected:** Generic 'review checklist' addition — too soft, no asymptote enforcement. Hard mandatory at all complexity levels — too heavy for trivial markdown edits where no producer/consumer pair exists.

---

### ADR-026: Multi-Session Discipline (ADR-026)
- **Date:** 2026-05-03
- **Section:** Iterate — feature: multi-session discipline (campaign iterate-skill-hardening Sub-Iterate C)
- **Context:** Two Claude Code sessions running in parallel against the same repo (main + .worktrees/<slug>) can race commits/pushes. The 2026-05-03 env-iterate demonstrated this: a non-canonical commit was created locally even though the canonical side had already announced it would integrate the fix. Phrasing read as open invitation, was actually zustimmung-check.
- **Decision:** Add session-role machinery: shared/scripts/lib/session_role.py (read/write/detect, atomic idempotent writes), shared/scripts/checks/check_session_role.py (push guardrail, exit 1 for secondary without SHIPWRIGHT_SECONDARY_PUSH_AUTH=1), SKILL.md B1c phase (parallel-session detection + designation prompt), B1a session-role addendum (secondary-by-default rule), F11 push gate (one-line check).
- **Commit:** PENDING
- **Rationale:** Session-role marker is a JSON producer/consumer boundary by ADR-024 taxonomy — round-trip tests + edge-case probes are mandatory. Atomic write (tmp.replace) mirrors autonomous_loop.py. Idempotent rewrite (same role + worktree → no-op) preserves audit trail set_at + set_by_session_id from the original write. Default permissive on missing marker reflects industry pattern for opt-in guardrails.
- **Consequences:** Future parallel iterates structurally avoid the race: secondary sessions get exit-code-1 at F11 push unless explicitly authorised. Default permissive on missing marker preserves single-session UX (most projects). 29 new tests in shared/tests/ cover round-trip + 8 boundary probe categories that apply to JSON markers (BOM, CRLF, non-ASCII, empty, idempotency, garbage bytes, unknown role, exit-code matrix).
- **Rejected:** File locking (concurrent writes from two processes): out of scope; the discipline IS the locking story (canonical writes, secondary reads). Hard-fail on missing marker: would gate every single-session project unnecessarily. Read role from env var instead of file: file persists across session restarts, env vars do not.

### ADR-027: Boundary Coverage Report
- **Date:** 2026-05-03
- **Section:** Test — feature: boundary coverage report observability tool (campaign iterate-skill-hardening Sub-Iterate D)
- **Context:** Sub-Iterate A (ADR-024) made `## Affected Boundaries` a first-class section in iterate specs and `is_io_boundary_change()` a first-class detector for the diff-driven `touches_io_boundary` risk flag. Without an audit hook, future iterates can silently skip the new section even when their commits touch IO files — drift the campaign was meant to prevent. D closes the loop by scanning iterate-spec history retrospectively.
- **Decision:** New tool `plugins/shipwright-test/scripts/tools/boundary_coverage_report.py` with `scan_specs`, `correlate_with_commits`, `render_markdown`, `render_json`, plus CLI. Wired into `/shipwright-test` via `--report-boundary-coverage` flag (Step 3.95 in test SKILL.md). Markdown table parser is the load-bearing boundary — all 8 probe categories from A's `boundary-probes.md` apply (BOM, CRLF, non-ASCII, alignment markers, whitespace, empty section, exact heading discipline, missing section). Drift signal fires when spec lacks `## Affected Boundaries` BUT either matched commits' `changed_files` OR spec text mentions IO-pattern files.
- **Commit:** PENDING
- **Rationale:** Read-only tool (no destructive writes outside `.shipwright/test-reports/` and `shipwright_test_results.json` merge), so failure modes are output-quality issues not safety issues. Path-match heuristic for round-trip detection is intentionally conservative (false-negatives, never false-positives). Reuses A's `is_io_boundary_change` directly (single source of truth for IO patterns; if A's patterns evolve D inherits them).
- **Consequences:** Smoke-test against live tree finds 17 specs scanned, 4 with `## Affected Boundaries` (= A/B/C/D), 7 drift signals on pre-A iterate specs (env-scaffold, hooks-quoting, suggest-iterate, performance-budgets) — exactly the audit hook intent. 19 new tests in `plugins/shipwright-test/tests/test_boundary_coverage_report.py`; iterate plugin's 146 tests + shared 1237 tests remain green (34 pre-existing failures in `test_phase_plugin_hooks_consistency.py` predate this campaign).
- **Rejected:** AST-pair detection on iterate-spec source files (deferred per A's same-rationale; path-match catches all 7 real drift cases). HTML table parser fallback (no real-world specs use HTML tables; fix forward if observed). Strict header-token validation (`Producer | Consumer | Format` exact-match) (rejected — too brittle against legitimate prose variations like `Producer (writes)` from A's own spec).

### ADR-028: Review-Driven Hardening (campaign iterate-skill-hardening Sub-Iterate E)
- **Date:** 2026-05-03
- **Section:** Iterate / Test / Shared — fix: 6 HIGH + 6 MEDIUM findings from per-sub-iterate code reviews + external_review.py + holistic external review on the campaign
- **Context:** After A/B/C/D shipped locally, retroactive code-reviewer subagents (4×) + `external_review.py --mode code` (4×) + 1 holistic external review surfaced 6 HIGH findings (4 empirically verified by reading shipped code) and ~12 MEDIUMs. The HIGH-severity items split into two groups: (a) real bugs (HIGH-2 worktree-blindness in `detect_parallel_sessions`; HIGH-4 missing test-runner integration for the boundary coverage report; HIGH-5 unscoped round-trip heuristic; HIGH-6 short-slug commit-correlation collisions); (b) spec/code disagreements where the shipped code was correct and the spec text was outdated (HIGH-1 AST-pair AC; HIGH-3 CLI signature). The campaign's dog-food meta-directive is the reason the reviews existed at all — A through D each declared the discipline that surfaces each other's bugs, but the runner contract didn't enforce review invocation, so the gaps weren't caught until human-driven post-ship reviews. Sub-Iterate F closes that runner-contract gap.
- **Decision:** Review-driven fixes only — no scope expansion. The 12 implementation steps in E's spec are the entire surface area. Real bugs are fixed empirically (HIGH-2 uses `git rev-parse --git-common-dir`, HIGH-5 scopes the heuristic to `changed_files ∩ test_*.py`, HIGH-6 requires `len(slug) >= 8` OR `\b{slug}\b` regex). Spec/code disagreements are resolved by amending the spec to match the shipped code with a `(deferred)` label + cross-reference to ADR-028 (HIGH-1 AST-pair) or by recording the richer shipped CLI as the canonical interface (HIGH-3). MEDIUM-A1 tightens the `RISK_TAXONOMY` regex patterns from bare verb prefixes (`parse_`, `load_`, `write_`, `\bdump\b`, `serialize`) to anchored function names + qualified stdlib calls (`\bparse_env\b`, `\bjson\.dump(s)?\b`, etc.). MEDIUM-C1 extracts the inline `python -c` snippets in B1c into shared CLI helpers (`shared/scripts/tools/detect_parallel_sessions.py` + `write_session_role.py`) so adopted target projects don't depend on a literal `shared/scripts/lib` path. MEDIUM-C2 makes `write_role`'s tmp file race-safe by construction via `tempfile.NamedTemporaryFile`. MEDIUM-D1 adds `--changed-files` to `record_event.py` (consumed by D's drift detection and HIGH-5's scoping). All changes carry tests.
- **Commit:** PENDING
- **Rationale:** Each fix has a concrete failure mode that the review surfaced empirically — these are not speculative hardening, they are confirmed bugs or load-bearing drift. The path-match-only deferral for AST-pair detection (HIGH-1) is justified by the empirical record: every known real-world boundary bug (the env-iterate's BOM + inline-comment) touched `.env` files in the diff, so the AST-pair complexity is not justified. The "unknown" tri-state for `round_trip_tested` (HIGH-5 fallback) preserves audit signal when legacy events lack `changed_files` rather than silently falsing them. The two-step Option A / Option B documentation for HIGH-4 keeps the API ergonomic (single `--merge-into` for typical use, separate `merge_boundary_coverage.py` helper for the standalone-artefact case).
- **Consequences:** 30 new tests added (iterate +17, test plugin +14, shared session_role +2, shared session_role_cli +3, record_event +6 — net +28 across the four runs after backing out re-counted ones). All previously green tests stay green: iterate 163 (was 146), test plugin 136 (was 122), shared 1240 of 1249 (the same 9 unrelated `test_artifact_path_canon`/`test_gitignore_canon` failures predate this campaign). Risk taxonomy is now meaningfully tighter — six negative-probe prompts (`load_user route`, `dump utility`, `parse_query helper`, `serialize method`, `rewrite the page header text`, `dashboard button color`) all return `[]` instead of `[touches_io_boundary]`, while the positive controls (`parse_env`, `json.dumps`, `settings.json`, `hooks.json`, `*_config.json`) still fire. The HIGH-2 empirical worktree probe (real `git worktree add`, write canonical marker in main + secondary marker in worktree, then call `detect_parallel_sessions` from the worktree path) now sees both markers — the bug C admitted in its Confidence Calibration "Edge cases NOT probed" list is closed.
- **Rejected:** Full AST-pair detection (HIGH-1 — deferred per spec). Reverting D's CLI to the original `--output {file|json}` mode-selector design (HIGH-3 — superseded; richer is strictly better). LOW-severity findings (cosmetic, perf-only, dead code) — explicitly out of scope per spec's "What's NOT in scope". File locking in `write_role` (MEDIUM-C2) — the unique-tmp-name pattern is race-safe by construction; locking would add complexity without benefit.

---

### ADR-029: ADR-029: sub-iterate-runner contract mandates Step 3.5 (External Plan Review) and Step 3.7 (Code Review Cascade)
- **Date:** 2026-05-04
- **Section:** Iterate — feature: Runner Contract Mandates Reviews (Sub-Iterate F)
- **Context:** Campaign iterate-skill-hardening A/B/C/D shipped without external plan review or code review cascade firing. The sub-iterate-runner agent contract jumped Step 3 (Build) directly to Step 4 (Finalization), skipping SKILL.md Step 4 (External LLM Review, mandatory medium+) and Step 8 (Code Review Cascade). The gap forced Sub-Iterate E to retroactively fix HIGH-severity bugs the reviews would have caught earlier.
- **Decision:** Patch sub-iterate-runner.md to mandate Step 3.5 (External Plan Review, medium+) and Step 3.7 (Code Review Cascade, medium+ OR risk flag OR diff > 100 LOC). Mirror Branch A/B/C semantics from references/iteration-planning.md and references/iteration-reviews.md. Result-JSON gains optional reviews.{plan,code,external_code} field with explicit skipped_* values. Schema reviews field optional for backwards-compat with pre-ADR-029 result.json files.
- **Commit:** PENDING
- **Rationale:** The campaign's own teachings (Boundary Tests, Confidence Calibration, Multi-Session, Coverage Report) cannot defend against contract gaps that the lifecycle itself permits. Closing the meta-loop.
- **Consequences:** Future campaign-spawned runners cannot silently skip plan-review or code-review cascade; gaps must be recorded explicitly. Drift-protection test (test_sub_iterate_runner_contract.py, 25 tests) freezes the contract structure. Internal code-reviewer subagent is delegated to orchestrator (campaign mode) since the runner has no Agent tool — runner records delegated_to_orchestrator status.
- **Rejected:** (a) Add Agent tool to runner so it can spawn code-reviewer itself — rejected because the runner already has Bash/Edit/Write and Agent would double-spend tokens. (b) Move review steps into Finalization F-phases — rejected because reviews must run before commit, not after.

---

### ADR-030: suggest_iterate hook is plugin-registered, not project-installed (retire hook_installer)
- **Date:** 2026-05-05
- **Section:** Iterate — bug: plugin-owned UserPromptSubmit hook
- **Context:** After ADR-019/020 fixed the carrier-shape and command-literal of the project-level installer, Claude Code surfaced an explicit error 'Hook command references ${CLAUDE_PLUGIN_ROOT} but the hook is not associated with a plugin' for every UserPromptSubmit on adopted projects. The variable only expands in plugin-context hooks; the install-into-settings.json model was structurally wrong.
- **Decision:** Register suggest_iterate.py in plugins/shipwright-iterate/hooks/hooks.json under UserPromptSubmit (Shape B + quoted + --no-project preserved verbatim). Retire hook_installer.py, check_a6_hook_installed verifier, validate_adoption._validate_hook, and seven SKILL.md auto-install stanzas. Spec FR-01.13/02/01 ACs realigned; FR-01.11 gains plugin-hook-ownership AC.
- **Commit:** PENDING
- **Rationale:** Strategy A (plugin-hook registration) avoids duplicating suggest_iterate.py + classify_intent.py into every adopted project (Strategy B) and uses the only Claude-Code-supported channel for ${CLAUDE_PLUGIN_ROOT}. Empirically validated by 5 pre-build probes + 11 round-trip tests + 1 cache-layout invariant.
- **Consequences:** Adopted projects no longer write .claude/settings.json on adopt. The hook fires from the plugin cache when shipwright-iterate@shipwright is enabled. Legacy adopted projects still carry the broken project-level entry until manual cleanup; SKILL.md replacement notes ship the cleanup snippet.
- **Rejected:** Strategy B (${CLAUDE_PROJECT_DIR} variable + per-project script copy): code duplication, staleness on plugin upgrades, plus suggest_iterate.py imports classify_intent.py via path arithmetic that only resolves inside the monorepo/cache layout — would have required copying that lib too.

---

### ADR-031: FR-table parser accepts 5-col adopt format + drift protection
- **Date:** 2026-05-05
- **Section:** Iterate — bug: rtm-fr-parser-multicolumn
- **Context:** Compliance RTM and drift-audit Group A/D both inlined a regex that only matched the 3-data-column Greenfield FR-table format (`| ID | Text | Priority |`). `/shipwright-adopt` produces 5-data-column tables (`| ID | Name | Priority | Description | Source |`), so every FR row in adopted specs silently failed both parsers — the per-requirement view of the RTM was empty for FR-01.10 and FR-01.13 (and every other adopted FR), and the drift audit could not reconcile spec-vs-reality for adopted projects.
- **Decision:** Widened `_FR_TABLE_RE` in both `shared/scripts/lib/drift_parsers.py` and `plugins/shipwright-compliance/scripts/lib/data_collector.py` to accept an optional 4th + 5th capture group; both consumers now select the FR's semantic body as `group(4) or group(2)` (Description if present, else Text). Added an integration-tests/ parametrized drift-protection suite (round-trip + 14 boundary probes) that runs the same fixtures through BOTH regexes — a future fix landing in only one will fail the suite.
- **Commit:** 389266ea332fd110f4602422b4eb4d1c29ba8219
- **Rationale:** Fix the regex in BOTH places + a parametrized drift-protection test rather than refactor to a single source of truth in this iterate. Memory pattern `feedback_artifact_migration_pattern.md` and `feedback_iterate7_scope.md` favor small bug fixes that stay small. The drift-protection test guards the duplication until a future iterate consolidates the parser.
- **Consequences:** Adopted projects' RTM now lists every FR with its Description in the per-requirement view. Greenfield 3-col specs unchanged. Pre-existing test_phase_plugin_hooks_consistency.py + test_artifact_path_canon.py + test_gitignore_canon.py failures (34 total) are unrelated to this change and are tracked separately. Future structural fix: collapse the duplicated parser into one source of truth (data_collector calls drift_parsers.parse_fr_table) — out of scope for this small bug.
- **Rejected:** (a) Refactor data_collector to call drift_parsers.parse_fr_table — bigger structural change; deferred to a future iterate. (b) Multi-format-detection that reads the table header — over-engineered for two known formats with stable column orders. (c) Strip everything after the priority column — would lose the Description text the RTM needs.

---

### ADR-032: Adopt writes shipwright_iterate_config.json with documented opt-out schema
- **Date:** 2026-05-05
- **Section:** Iterate — bug: adopt writes shipwright_iterate_config.json
- **Context:** shipwright-adopt wrote 6 of 7 documented config files. shipwright_iterate_config.json was missing — the documented opt-out surface for external_review.feedback_iterations and external_code_review.enabled (per iteration-reviews.md:140 and sub-iterate-runner.md:101). Reproduced on the self-adopted shipwright monorepo: ls shipwright_*.json showed 6 configs, no _iterate_.
- **Decision:** Add write_iterate_config() in plugins/shipwright-adopt/scripts/lib/config_writer.py and wire it into write_all() between build and compliance, before run (run still last). Schema: external_review.feedback_iterations=0, external_code_review.enabled=true, seeded_by_adopt=true, updated_at. Conservative defaults — plan/iterate-mode review opted out by default, code-review cascade on. Updated dry_run_reporter and SKILL.md (5→6 configs).
- **Commit:** PENDING
- **Rationale:** feedback_iterations=0 mirrors the existing flat key in plan_config; external_code_review.enabled=true keeps the cascade on (operator opts out per project as needed). Run config stays last so the audit-on-Stop hook never sees a half-formed Shipwright project.
- **Consequences:** Adopted projects now ship with the documented opt-out surface. Boundary Probe round-trip producer-consumer test added. Note: today no Python loader reads this file — it is documented in iteration-reviews.md as the source of truth but external_review_config.py reads only shared/config/external_review.json. Writing the file matches docs and prepares the ground for a future loader patch; backfill doc note added for already-adopted projects.
- **Rejected:** Inverting the cascade default to false (rejected — would silently disable the gate operators expect). Skipping the iterate-config write at adopt and writing it lazily on first /shipwright-iterate (rejected — adopt is the canonical surface where all opt-out config files appear).

---

### ADR-033: Verifier accepts CHANGELOG-unreleased.d entries and dashboard short-SHAs
- **Date:** 2026-05-05
- **Section:** Iterate — bug: finalization verifier drop-dir + dashboard commit-hash fallback
- **Context:** verify_iterate_finalization.py reported FAIL on CHANGELOG.md [Unreleased] empty + WARN on build_dashboard run_id missing for every iterate since the drop-directory CHANGELOG refactor. Both checks predate post-refactor file shapes — drop files live in CHANGELOG-unreleased.d/<category>/<run_id>_NNN.md and the dashboard renders short commit SHAs only.
- **Decision:** check_changelog_unreleased(project_root, run_id=None) now scans CHANGELOG-unreleased.d/<category>/ first; passes when a file matching <run_id>_*.md exists (or any non-.gitkeep file when run_id omitted). Falls back to legacy CHANGELOG.md [Unreleased] bullet check. check_build_dashboard_has_run_id(project_root, run_id, commit_hash=None) accepts the short SHA of commit_hash as proof of dashboard inclusion when the run_id literal is absent. run_all_checks threads run_id + commit_hash through both.
- **Commit:** PENDING
- **Rationale:** Drop-dir is the producer-owned canonical location for unreleased entries (write_changelog_drop.py). Short SHA is what update_build_dashboard.py writes — not the run_id literal — so the verifier was checking for a string the dashboard generator never emits. Accept both new + legacy formats so brownfield projects keep working.
- **Consequences:** F11 verifier returns 5 OK on the previous iterate (the adopt-iterate-config one) instead of 1 FAIL + 1 WARN. 6 new tests added; 26 existing tests stay green (legacy seed_project state still satisfies both checks via the run_id literal + CHANGELOG.md [Unreleased] paths).
- **Rejected:** Embedding run_id as HTML comment in dashboard rows (rejected — would require update_build_dashboard.py change AND the verifier change; cleaner to teach the verifier the existing dashboard shape). Removing the dashboard check entirely (rejected — iterate_history check covers run_id presence but dashboard check confirms the user-visible activity table actually rendered).

---

### ADR-034: load_review_config deep-merges per-project override; cascade helper added
- **Date:** 2026-05-06
- **Section:** Iterate — bug: external_review_config loads per-project shipwright_iterate_config.json
- **Context:** shipwright_iterate_config.json was documented as the project-level opt-out for external_review.feedback_iterations and external_code_review.enabled (iteration-reviews.md, sub-iterate-runner.md) but the loader external_review_config.py read only shared/config/external_review.json. ADR-032 added the producer (adopt write); the consumer side stayed broken. Iterate B closes the loop.
- **Decision:** load_review_config gains an optional explicit project_root parameter that, when passed, deep-merges <project_root>/shipwright_iterate_config.json over the shared default (sub-dicts merge recursively so sibling keys survive). Auto-discovery from cwd was prototyped but rejected — it polluted unit tests and made the merge magic. Three call sites updated to pass project_root: external_review.py CLI (--project-root, default cwd), check-external-review-keys.py CLI (--project-root, default cwd), setup-planning-session.py (--project-root, default cwd). New is_external_code_review_enabled(config) helper exposes the cascade gate, default True (cascade on unless explicitly disabled).
- **Commit:** PENDING
- **Rationale:** Explicit opt-in over auto-discovery: tests of load_review_config in any caller would otherwise be polluted by whatever per-project file exists in the working tree. Default true for cascade gate matches the conservative default established in ADR-032 (cascade ON; user opts out per project). Stderr warning on malformed JSON ensures operators see why their override didn't apply.
- **Consequences:** The opt-outs documented since 2026-04 are now real. Empirical end-to-end probe on shipwright self-adopted root: feedback_iterations 1->0, external_code_review.enabled True. get_external_review_status returns user_disabled (correct), is_external_code_review_enabled returns True (cascade still on, independent gate per docs). 10 new tests covering deep-merge, sub-dict preservation, missing/malformed fallback, round-trip, cascade helper independence.
- **Rejected:** Auto-discovery from cwd via shipwright_run_config.json marker (rejected — pollutes any test that doesn't isolate cwd; the existing test_load_review_config_explicit_path test failed under auto-discovery because the test runner cwd is the real shipwright repo, which has a per-project file). Adding project_root to is_external_review_enabled / get_external_review_status (rejected — keeps these helpers pure dict consumers; merging belongs in load_review_config).

---

### ADR-035: Drift-canon cleanup: ALLOWLIST self-adopted records, gitignore proximity, .shipwright/ self-adopt exception
- **Date:** 2026-05-06
- **Section:** Iterate — bug: post-migration canon cleanup
- **Context:** After the .shipwright/<artifact>/ migrations (2026-04-26..29) and self-adoption (2026-05-02), 9 canon tests stayed red on every run. Three classes of false positives: (a) self-adopted records (.shipwright/{adopt,agent_docs,compliance,planning}/...) referencing legacy paths in archived ADRs/diagrams/migration logs; (b) test_legacy_entry_kept_with_comment_after_migration expecting the 'legacy path' comment within +/-1 line of the gitignore entry, but the comment block had it 3 lines above; (c) test_shipwright_dir_is_ignored hardcoded 'must be in gitignore' even though self-adoption makes .shipwright/ a TRACKED directory.
- **Decision:** (1) ALLOWLIST gains four self-adopted records per migration: .shipwright/adopt/enrichment.json, .shipwright/agent_docs/architecture.md, .shipwright/agent_docs/decision_log.md, .shipwright/compliance/change-history.md, .shipwright/planning/iterate/**.md. agent_docs migration also adds shipwright_project_config.json (regex matches the dict key, not a path). (2) One inline HTML marker on plugins/shipwright-adopt/skills/adopt/SKILL.md:535 ('planning/iterate gate' is a phase phrase, not a path). (3) .gitignore: reorder comment block so 'legacy path - do not remove.' sits at idx-1 of each /agent_docs/, /designs/, /planning/, /compliance/ entry. (4) test_shipwright_dir_is_ignored skips the must-be-ignored assertion when 'git ls-files .shipwright/' returns tracked content (self-adopted repos), and instead asserts the inverse.
- **Commit:** PENDING
- **Rationale:** ALLOWLIST entries chosen over inline markers for self-adopted records because: (a) .shipwright/adopt/enrichment.json is JSON without comment syntax; (b) change-history.md and archived iterate plans are write-once historical records; (c) decision_log.md and architecture.md are append-only history where the diagram + ADR text legitimately mention the migration narrative. Comment-block reorder over alternative fixes (inline gitignore comment, widen test window): inline gitignore comments aren't supported by git; widening the window from +/-1 to +/-3 weakens the proximity guarantee for future maintainers. Test-side self-adoption check is the right asymmetric fix because user projects DO need the must-ignore rule.
- **Consequences:** 9 previously-failing canon tests now green: 4 path canon (planning/designs/agent_docs/compliance), 4 gitignore proximity (same migrations), 1 shipwright-dir ignore. Full shared/tests/ now 1270 passing (up from 1252) excluding only the still-pre-existing test_phase_plugin_hooks_consistency suite (separate iterate). End-user (non-self-adopted) projects keep the original .shipwright/ ignore rule via the runtime check, so the policy doesn't loosen for them.
- **Rejected:** Adding .shipwright/ to gitignore with !exception lines (rejected — git cannot re-include files inside an excluded parent per gitignore semantics, this is documented in the existing comment block at line 110-115). Widening the regex (?<![\w/.\-]) to exclude hyphen-prefix matches (rejected — would change semantics of the path canon for ALL files, higher blast radius than per-file allowlist). Removing legacy gitignore entries entirely (rejected per Gemini #2 retention rule).

---

### ADR-036: _hook_commands uses shlex.split to handle quoted-path commands
- **Date:** 2026-05-06
- **Section:** Iterate — bug: hooks-consistency parser handles quoted commands
- **Context:** test_phase_plugin_hooks_consistency.py reported 25 failures suggesting plugins were missing capture_session_id/phase_session_start/phase_session_stop hooks. Investigation showed all 8 phase plugins DO register the full chain in their hooks.json — the test's _hook_commands() helper used cmd.split() (whitespace) which left ADR-019/020 quoted-path tokens like '"/.../foo.py"' with trailing quotes, so token.endswith('.py') never matched.
- **Decision:** Switch _hook_commands() to shlex.split(cmd, posix=True). Falls back to whitespace split on shlex parse error so a malformed command can never silently disappear from the basename collection. Path basename extraction also handles Windows '\' separators (rsplit on '/' AND '\').
- **Commit:** PENDING
- **Rationale:** shlex.split is the right tool for shell-style tokenization including quotes. The fallback to whitespace split is defensive: if a hook command is so malformed shlex raises, we still want to surface SOME basename rather than zero (which previously caused the false alarm that motivated this iterate).
- **Consequences:** All 27 hooks-consistency tests now green (was 25 failed + 2 passed). No production code changed — this was purely a test-side parser bug introduced by the quoted-path migration in ADR-019/020. The underlying hook wiring was correct all along.
- **Rejected:** Stripping quotes manually (rejected — shlex handles edge cases like escaped quotes and nested quotes correctly). Updating hooks.json to remove the quotes (rejected — quoting is required by ADR-019/020 for paths-with-spaces on Windows; the test must adapt to production reality).

---

### ADR-037: F0.5 End-to-End Verification Gate (surface taxonomy + schema-enforced evidence)
- **Date:** 2026-05-06
- **Section:** Iterate — feature: e2e-verification-gate
- **Context:** Iterate skill at medium+ silently let backend-only diffs ship without empirical Surface verification. The 2026-04 webui regression hit production because file-path-gated browser verify (touches client/**) didn't fire for backend changes that affected the UI; spec-only authoring of E2E tests counted as a test run.
- **Decision:** Add a single normative gate F0.5 between F0 and F1 of iterate finalization. Surface taxonomy (web/cli/api/none) + production-time orchestrator surface_verification.py + post-commit audit check_surface_verification in iterate_checks.py. Schema-enforced evidence in shipwright_test_results.json.iterate_latest.surface_verification with four fail-closed conditions: missing block, tests_run==0, exit_code!=0 after 3 retries, surface=none without justification.
- **Commit:** e2e4a7517887a0637a6431769f560aef77b096fa
- **Rationale:** Single chokepoint avoids hooks-detection-spread. Two-layer validation (orchestrator non-zero exit + post-commit auditor) covers both prose-violation and silent skip. finalize_iterate.py is intentionally NOT extended because its best-effort contract would be broken by hard-failing.
- **Consequences:** F6 commit is now blocked at medium+ unless Surface was empirically driven through a running stack OR surface=none is explicitly justified in the iterate ADR. Step 11 split into 11a (Author) + 11b (Execute); spec-only authoring is regression-equivalent to no test. Phase Matrix E2E row renamed to 'E2E Verification (author + execute)' with 'always' at medium+ subsuming file-path detection — Backend-affects-Frontend rule.
- **Rejected:** (1) Behavior-Surface auto-detection — multi-week sidequest; matrix=always at medium+ subsumes the problem. (2) Skill-Iterate-Fixture-Project — deferred; skill iterates use surface=cli with pytest plugins/<plugin>/tests/. (3) /shipwright-test as Live-Smoke-Owner — dependency-inversion risk. (4) Build-phase integration — F0.5 in iterate is the SSoT for E2E gate at iterate-time.

---

### ADR-038: F0.5 empirical-test backfill: drift-schutz + real subprocess probes + CLI audit chain
- **Date:** 2026-05-06
- **Section:** Iterate — change: e2e-gate-empirical-tests
- **Context:** ADR-037 landed F0.5 with strong unit-test coverage but used --tests-run overrides everywhere. parse_tests_run never ran on real subprocess output, the four fail-closed conditions were never exercised through the verifier CLI, and there was no drift-schutz between Phase Matrix / F0.5 prose / design-and-testing.md.
- **Decision:** Add three test files that close those gaps empirically. test_skill_e2e_gate_consistency.py (14 tests) is the drift canary across the three SSoTs. test_audit_e2e_integration.py (9 tests) spawns verify_iterate_finalization.py as a real subprocess and asserts exit codes for each fail-closed condition. Four real-subprocess tests in test_surface_verification.py exercise parse_tests_run + retry-cap against actual pytest invocations (greedy-filter, happy path, failed-test, slow-failing process).
- **Commit:** 531171c61fad7a4f52ce8dbbe6ecae3339d5052c
- **Rationale:** Plan §Verification listed seven items; six are now reduced to deterministic tests. The seventh (real medium+ webui iterate end-to-end) is deferred — no Playwright fixture exists in this monorepo and building one is its own iterate.
- **Consequences:** F0.5 is now empirically validated end-to-end — parser, orchestrator, and CLI-level audit. The iterate skill's own test suite (226 plugin + 1349 shared) will fail loudly if any future edit drops the rename, the F0.5 section, the four fail-closed keywords, or the file-path-agnostic anchor in hooks-and-pipeline.md. Drift-test caught its own implementation bug (heading scan matching shell comments inside code fences) which is a free reminder that empirical probes find things unit tests can't.
- **Rejected:** Building a Playwright fixture project just for this iterate (cost too high; defer until first real webui consumer needs it). Substring-only drift checks (anti-pattern from ADR-025; anchored on heading regex with code-fence skipping instead).

---

### ADR-039: Wrap plugins/*/hooks/hooks.json under top-level `hooks` key for Claude Code 2.1.132+
- **Date:** 2026-05-07
- **Section:** Iterate — bug: hooks.json schema migration to Claude Code 2.1.132+
- **Context:** Claude Code 2.1.132 tightened plugin schema validation. `plugins/*/hooks/hooks.json` is now expected to wrap its event-name dict under a top-level `"hooks"` key. Existing files (post-ADR-019/020, all 12 plugins through v0.17.0) place event names at the document root. Result: plugin load fails with `Hook load failed: expected record, received undefined at path ["hooks"]` and NO hooks fire (`SessionStart`, `UserPromptSubmit`, `Stop`, `PreToolUse`, `PostToolUse`, `SubagentStop`). The SDLC pipeline silently breaks for every Claude Code 2.1.132+ user — same lurking failure mode as ADR-019/020/022 (hook-load failure → silent SDLC break) but the trigger is the harness version, not the install path.
- **Decision:** Mechanical structural rewrap of all 12 affected `plugins/*/hooks/hooks.json` files: wrap the existing event-name dict under a single `"hooks"` key. Contents (matchers, hook arrays, command strings, `${CLAUDE_PLUGIN_ROOT}` quoting from ADR-022) preserved byte-equivalent semantically. Bump every patched plugin's `.claude-plugin/plugin.json` `version` (10 × `0.2.0`→`0.2.1`, `shipwright-iterate` `0.4.0`→`0.4.1`, `shipwright-plan` `0.3.0`→`0.3.1`) so existing installs pull the fix via `claude plugin update`. `shipwright-preview` correctly NOT bumped — has no hooks.json. Add `shared/tests/test_hooks_json_wrapper.py` parametrized regression test asserting `parse(hooks.json)["hooks"]` is a non-array dict with event-name keys; catches future regressions at test time.
- **Commit:** pending
- **Rationale:** Same pattern as ADR-019/020/022 — Shape-only fixes that close lurking SDLC-breaking hook-load failures get a mechanical sweep + structural regression test. Empirical round-trip (`claude plugin update` + `claude plugin list` shows ✔ enabled) is the user's verify-step at install time; cannot be run in this iterate because the user's only PC has shipwright deinstalled pending the fix and no second machine is available. The structural conformance test is the strongest in-repo gate — the consumer is the closed-source Claude Code binary, not any code we own.
- **Consequences:** All 12 plugins load cleanly under Claude Code 2.1.132+ once the user runs `claude plugin update <name>@shipwright`. No behavior change for `command` strings — recursive `_collect_command_strings` in `test_hooks_json_quoting.py` traverses the new wrapper depth without modification, so AC-3 of ADR-022 stays green. New `test_hooks_json_wrapper.py` makes the wrapper schema part of the iterate Self-Review surface. shipwright-preview untouched.
- **Rejected:** (1) Backwards-compat shim (e.g. ship both shapes): rejected — Claude Code 2.1.132 is the supported floor; older versions are not a target. (2) Bumping the monorepo top-level version beyond per-plugin bumps: rejected — release-tag (`v0.17.0`) governs the marketplace bundle, per-plugin `version` governs `claude plugin update`; the next release tag bundles this fix automatically. (3) Idempotent JS-loop transformation in CI: rejected for now — the regression test makes the invariant explicit; a CI auto-fix tool can be a separate iterate if drift recurs.

---

### ADR-040: PreToolUse / PostToolUse matcher must be a string under Claude Code 2.1.132+
- **Date:** 2026-05-07
- **Section:** Iterate — bug: hooks.json matcher schema (follow-up to ADR-039)
- **Context:** After ADR-039 wrapped event names under `"hooks"`, `claude plugin list` against the post-merge cache surfaced a SECOND schema tightening: `PreToolUse` / `PostToolUse` matcher fields must now be strings, not the legacy object form `{"tools": ["Bash"]}`. `shipwright-build` reproduces empirically as ✘ failed to load with `Invalid input: expected string, received object` at `["hooks","PreToolUse",0,"matcher"]` and `["hooks","PostToolUse",0,"matcher"]`. `shipwright-compliance` carries the same broken shape but is not currently installed locally — pre-emptive fix. Repo-wide grep `"matcher":\s*\{` post-ADR-039 confirms exactly these two files are affected; the other 11 plugins either have no `PreToolUse`/`PostToolUse` or use catch-all groups (no `matcher` key).
- **Decision:** Convert object-form matchers to canonical 2.1.132 string form: `{"tools": ["Bash"]}` → `"Bash"`, `{"tools": ["Write", "Edit"]}` → `"Write|Edit"` (regex alternation, the documented idiom). Bump `shipwright-build` `0.2.1`→`0.2.2` and `shipwright-compliance` `0.2.1`→`0.2.2`. Extend `shared/tests/test_hooks_json_wrapper.py` with parametrized `test_pre_post_tool_use_matchers_are_strings` (12 plugins) so future plugin authors can't reintroduce the object form. NOT collapsing matchers to `"*"` — that would change hook semantics (e.g. `validate_command.sh` would suddenly run on every tool call, not just `Bash`); matcher scope is preserved.
- **Commit:** pending
- **Rationale:** Empirical: `claude plugin list` showed 5/6 ✔ enabled and `shipwright-build` ✘ failed to load with a clearly-named error; the fix is a 3-line text edit verified by extending the existing regression test; no behavioral change for hook execution. The previous iterate (ADR-039) silently shipped this drift past Self-Review because the test only checked the wrapper shape — extending the test to cover matchers closes that gap.
- **Consequences:** `claude plugin update shipwright-build@shipwright` + `claude plugin list` shows ✔ enabled (was ✘ failed to load). `shipwright-compliance` is preemptively patched so the next user who installs it doesn't hit the same wall. Test coverage is now 26 parametrized cases (12 wrapper + 12 matcher + 1 negative + 1 wrapper-aware) for `hooks.json` — the wrapper AND matcher invariants are both load-bearing. Next plugin author who adds `PreToolUse`/`PostToolUse` with the object form fails CI, not user install.
- **Rejected:** (1) Catch-all `"*"` for all matchers: changes hook semantics — `validate_command.sh` is scoped to `Bash` deliberately. (2) Splitting `Write|Edit` into two matcher groups: identical hook chain, alternation is the documented idiom, fewer groups. (3) Bumping all 12 plugins again: only build + compliance carry the broken shape; the other 10 don't need a version bump.

---

### ADR-041: known-issues scanner requires comment-context; remove dead save_session_config
- **Date:** 2026-05-09
- **Section:** Iterate — bug: known-issues scanner self-detection + cleanup
- **Context:** The TODO/FIXME inventory scanner (.shipwright/agent_docs/known_issues.md) self-matched its own marker tuple and regex pattern, drowning real markers. The on-disk file was also stale and showed 28 markers, mostly fixture noise. shipwright-plan also carried a deprecated save_session_config function with zero production callsites.
- **Decision:** Tighten the scanner predicate so markers only count when the prefix ends with a recognised comment opener (#, //, /*, <!--), or the line is a JSDoc continuation (^\s*\*) or a markdown bullet (^\s*-\s+). Add belt-and-suspenders self-skip via Path(__file__).relative_to(project_root) with hardcoded fallback. Delete save_session_config and its test; regenerate known_issues.md (drops 28 → 0 markers).
- **Commit:** 22a317c1db9c65448b356c84786e9d0f377a158a
- **Rationale:** Comment-context recognition is the structurally correct fix; tightening the regex's optional colon would still match tuple elements. Inline * was split out from JSDoc continuation after code review caught the a * TODO false positive. SQL/Lua/Haskell -- is excluded because their extensions are not in _SOURCE_SUFFIXES.
- **Consequences:** Markers in Python docstrings, regex string literals, tuple elements, and inline math (a * TODO) are no longer detected — these were dominant false-positive sources. Markers in multi-line block comments without a per-line * continuation are NOT detected (documented limitation; switch to JSDoc-style * continuation if needed). load_session_config + get_merged_config in plan/lib/config.py remain unused-but-kept; future cleanup is a separate iterate.
- **Rejected:** Tightening _MARKER_RE alone (would still match tuple elements with quote suffix). Hardcoded _SKIP_FILES set without dynamic resolution (drift risk on path refactor). Including markdown headings in the allowlist (out of scope per AC-2 and external review).

---

### ADR-042: Stop and SubagentStop hooks emit schema-compliant stdout
- **Date:** 2026-05-10
- **Section:** Iterate — bug: Stop-hook schema compliance
- **Context:** Claude Code surfaced 'Hook JSON output validation failed — (root): Invalid input' for all 35 Stop hooks at every session end. The Stop and SubagentStop schemas only permit 'hookEventName' inside 'hookSpecificOutput'; the existing hook scripts were emitting 'additionalContext' on stdout, which is valid only for SessionStart/PreToolUse/PostToolUse/UserPromptSubmit.
- **Decision:** Route Stop-hook diagnostics to stderr (matches the already-correct pattern in 'phase_session_stop.py' and 'master_stop_check.py'). For SubagentStop error sites in 'write-section-on-stop.py', emit top-level 'decision: "block"' on stdout so subagent halts on transcript-extraction failure are preserved. Add a parametrized 'shared/tests/test_hook_output_schema_compliance.py' that discovers every hook in every 'plugins/*/hooks/hooks.json', runs each with a realistic stdin payload, and validates per-event schema constraints — guarding all 7 supported events against future drift.
- **Commit:** PENDING
- **Rationale:** Stderr is the only valid channel for hook-side diagnostics on events whose hookSpecificOutput schema is restrictive. The audit-first, event-aware test strategy (per-event schema map keyed off the official docs) is structurally drift-resistant: adding a new hook script that emits an invalid shape will fail the test before hitting production.
- **Consequences:** All 35 Stop-hook validation errors at session end are eliminated. SubagentStop error semantics preserved (decision:block halts the run). Diagnostic visibility unchanged — Claude Code surfaces hook stderr to the user. Audit test (88 cases) prevents regression.
- **Rejected:** (a) Top-level 'decision: "block"' for the observability scripts — would halt session stop on every audit/handoff, a regression worse than the current schema warning. (b) Swallowing the validation error via try/except — validator runs in Claude Code AFTER our script exits; only the producer can fix it. (c) Moving diagnostics into PostToolUse hookSpecificOutput — wrong event semantics (session lifecycle, not tool calls).

---

### ADR-043: Adopt scaffolds profile-aware CI + Claude-Review workflows with cross-platform matrix default
- **Date:** 2026-05-11
- **Section:** Iterate — feature: adopt-ci-scaffolders
- **Context:** Two GitHub Actions templates (ci-nextjs.yml.template + claude-review.yml.template) authored in v0.1.0-era commits c3a6d2f + 8aac61d were never wired into adopt — only security.yml.template got scaffolded (May 2026). shipwright-webui v0.8.5+ has been CI-red for 9 push-runs on main because its hand-written ci.yml only runs on ubuntu-latest, masking Windows-pathing test failures (4 path-self-heal tests expected 'C:\Users\Test\.local\bin' but got '.' under POSIX path.dirname).
- **Decision:** Complete the abandoned scaffolding initiative AND ship cross-platform OS matrix (ubuntu-latest + windows-latest, fail-fast: false) as the convention-locked default for every CI template. New scaffolders ci_workflow_scaffolder.py + claude_review_workflow_scaffolder.py mirror security_workflow_scaffolder.py. Three profile templates (supabase-nextjs, vite-hono, python-plugin-monorepo) with explicit permissions: contents:read floor. Distinct reason codes profile_unresolved vs no_template_for_profile surface snapshot-parsing failures upstream. Wired as Steps E.14 + E.15 in generate_adoption_artifacts.py.
- **Commit:** 616044bc2ab78f70638ce79208361d91826c30e2
- **Rationale:** Mirror the proven security_workflow_scaffolder.py pattern verbatim — same module-loading by file path, ScaffoldResult TypedDict, idempotency contract. External LLM review (Gemini + GPT) applied 14 of 15 findings pre-build: setup-uv@v3 for Python template (#G1), explicit permissions floor (#G3+#O13), drift-test on parsed YAML absence not 'commented or absent' (#O5), distinct profile reason codes (#O12), parametrized subprocess tests across all 3 profiles (#O7), expanded path-helper edge cases including UNC+empty+undefined (#O9), Vitest empirically integrated into pytest suite (#O10), shared copy_template_if_absent helper extracted (#O11).
- **Consequences:** Future brownfield adoptions land cross-platform CI from day one. Drift test (32 parametrized assertions) pins every template against the constants — adding a profile requires registering in TEMPLATE_BY_PROFILE AND authoring the template, or the test fails loud. Existing already-adopted projects (this monorepo, webui) keep their hand-written ci.yml due to idempotency (already_exists). webui-repo fix to use path.win32 happens in a separate PR there using the new path-helpers.ts.template as canonical reference.
- **Rejected:** Single-template + slot-filling — rejected as premature abstraction (three profiles have structurally different job topologies: vite-hono has 2 workspaces, supabase-nextjs has security+deploy chain, python-plugin-monorepo has uv setup). Auto-migrating already-adopted projects' CI — rejected, idempotency wins (preserves operator's existing CI). cli-compat.ts itself as a template — rejected, runtime-infrastructure not scaffolding. Force claude-review.yml dormant — rejected, pull_request trigger IS its purpose.

---

### ADR-044: Silent skips loud-fail in CI + Step 6 governance rules
- **Date:** 2026-05-11
- **Section:** Iterate — change: test-hygiene-and-skill-rules
- **Context:** Six binary-skip sites + 7 cross-plugin import-skip sites in shared/tests + iterate plugin tests were silently masking missing toolchains and sys.path collisions; no SKILL.md rule existed to prevent recurrence. Test infra was greening on no-op tests and missing-binary skips.
- **Decision:** Convert all silent skips to a CI-gated hard-fail (CI=truthy → pytest.fail with actionable install hint; local → skip as before). Add reverse-direction drift test for TEMPLATE_BY_PROFILE (every ci-*.yml.template on disk has a registry entry). Inline 3 governance bullets into SKILL.md Step 6 (Test-Update-Klausel, Registry-driven SSoT meta-test rule, Silent-skip CI-discipline rule). Annotate the no-op _wait_for_service test with @pytest.mark.skip + behavior-shaped reason.
- **Commit:** ad739c15cc2a93e569ad2fae382da340713c99d7
- **Rationale:** Approach (b) over (a): cross-plugin sys.path collision is structural (every plugin defines its own lib/). Runtime module isolation via importlib would touch every test and break standalone plugin sessions. CI gate is cheaper and louder. AC-6 (shared shared/scripts/lib/test_hygiene.py helper) deferred — wait for SKILL.md rule maturity before centralizing the helper.
- **Consequences:** Silent skips become loud in CI without affecting local dev workflows. Future orphan templates surface at PR time. Future iterates that change test infra are required by Step 6 to update SKILL.md in the same diff. Module-level pytest.fail in test_path_helpers_template_vitest.py relies on undocumented pytest behavior (raises Failed = collection error, non-zero exit); pinned at source level via test_silent_skip_ci_discipline.py rather than runtime.
- **Rejected:** (a) sys.path rewrite — high blast radius, every test affected, breaks single-plugin pytest sessions. blanket-fail without CI gate — would break dev machines without semgrep/gitleaks/trivy installed. separate references/test-hygiene.md file linked from Step 6 — invites the rules being skipped; inline keeps them in front of the eyes during build.

---

### ADR-045: Centralize CI-discipline helpers + Self-Review § 8 static probe
- **Date:** 2026-05-11
- **Section:** Iterate — change: test-hygiene-helper-and-self-review-wiring
- **Context:** PR #26 / ADR-044 deferred AC-6 (helper centralization) pending SKILL.md rule maturity. After one release with the inline-helper duplication + DR-1 enforcing parity, the rules stabilized and centralization is now safe.
- **Decision:** Move helpers to shared/scripts/test_hygiene.py (top-level under shared/scripts/, NOT under lib/ to avoid the plugin-local lib/ namespace collision verified empirically: test_aikido_client.py + test_oss_backend_smoke.py in same plugin session). Ship a separate CLI at shared/scripts/tools/scan_test_hygiene.py. Wire as Self-Review § 8 in iteration-reviews.md (Mandatory at medium+, advisory below). DR-1 flips to enforce centralization.
- **Commit:** 1a52f4cd224fd33c8444c784d747bfb59ccde152
- **Rationale:** Approach chosen after external + code review: (a) AST not regex (substring matching at producer/consumer boundary admits false positives); (b) scope topology not line geometry (formatters spread fail+skip across many lines); (c) top-level under shared/scripts/ not lib/ (plugin lib/ namespace packages collide with shared lib/ regular package once both are on sys.path).
- **Consequences:** Inline helpers extinct across the 4 affected files. Future iterates that introduce new silent skips will hit Self-Review § 8 and either fix or document with the # test-hygiene: allow-silent-skip marker. AST + tokenize hybrid: scope-topology rule (same FunctionDef body has both skip + CI-gated fail) replaces the prior ±5-line heuristic. CI --diff resolves origin/HEAD defensively and falls back to working-tree diff when HEAD == origin/<default> (false-green elimination).
- **Rejected:** (1) Keep helpers inline + only ship probe — would leave SSoT split; DR-1 became flipped enforcement instead. (2) Place lib under shared/scripts/lib/ — verified to break plugin pytest sessions due to namespace-package vs regular-package conflict on the lib/ name. (3) AST-only suppression detection — comments are stripped by ast.parse; tokenize required. (4) ±5-line proximity heuristic for guard detection — admits false positives and false negatives; scope topology is the right model. (5) Auto-apply --fix mode — riskier surface, deferred.

---

### ADR-046: Triage Inbox Pattern (Iterate 1a): pre-backlog JSONL intake + 2 producers + promote bridge
- **Date:** 2026-05-11
- **Section:** Iterate — feature: triage-inbox-1a
- **Context:** Findings from hooks/scans/audits flooded the WebUI ExternalTask backlog (sdk-sessions.json) every session because there was no pre-backlog buffer. C1/C5/W3 Phase-Quality FAILs and Compliance audit findings re-fired on every Stop without dedup. The triage and backlog stores need different lifecycles (raw findings vs operator-claimed tasks) and different dedup keys (finding_code vs task_id).
- **Decision:** Introduce a per-project pre-backlog intake store at .shipwright/triage.jsonl (append-only JSONL with history events, gitignored, last-status-wins by file order). Add shared/scripts/triage.py outside lib/ (ADR-045 lib/ namespace rule applies) as the storage SSoT. Wire 2 producers (Phase-Quality Stop-hook + Compliance audit_detector.mirror_findings_to_triage) and 1 consumer (aggregate_triage_on_stop, last Stop-hook). Provide promote-bridge CLI (triage_promote.py) for non-webui repos; WebUI Triage-Tab in Iterate 3 will share the same schema. Wire format is camelCase to match shipwright-webui ExternalTask + leadwright extension. Severity→Priority + Source→Domain mapping is pure (no I/O, no config).
- **Commit:** a74ae59818acd51e0c8b756356d11f0f9d16b02f
- **Rationale:** Triage and Backlog have fundamentally different concerns (dedup keys, lifecycle, audience). Splitting them keeps the inbox as the noise absorber and the backlog as the curated work list. camelCase wire format keeps the leadwright daemon (which already consumes sdk-sessions.json) able to read promoted items without renames. Storage outside shared/scripts/lib/ avoids the sys.modules['lib'] collision (ADR-045) — audit_detector.py is in a plugin namespace and imports triage cross-namespace.
- **Consequences:** Findings now have a deduped, history-tracked staging area before promotion. Phase-Quality 24h-window dedup (per-commit re-fire after stale) + Compliance cross-session unbounded dedup (same finding stays as one item until resolved). External review caught a concurrency race (dedup-scan outside lock) and a stale-dedup window for compliance — both fixed. Operators can promote via CLI today; Iterate 3 will add the WebUI button. backlog ExternalTask schema gets a new optional promotedFromTriageId field via Iterate 1b (separate, in shipwright-webui repo).
- **Rejected:** (1) Single-store with status field on ExternalTask — rejected because dedup-by-finding-code would conflict with task-id uniqueness. (2) Putting triage.py under shared/scripts/lib/ — violates ADR-045 cross-plugin import constraint. (3) camelCase for Python identifiers too — violates PEP 8 and existing record_event.py / phase_quality.py idiom; kept wire format camelCase only with snake_case Python via a thin adapter.

---

### ADR-047: Triage producers iterate 2: security + performance + F0.5 + drift wiring
- **Date:** 2026-05-14
- **Section:** Iterate — feature: triage producers iterate 2 (security + performance + F0.5 + drift)
- **Context:** Iterate 1a (ADR-046) established the triage inbox storage API and 2 producers (Phase-Quality, Compliance). The roadmap listed 5 more producers; this iterate ships 4 of them and explicitly defers the 5th.
- **Decision:** Wire 4 thin `_emit_*_to_triage` helpers calling `append_triage_item_idempotent`. Security + performance + F0.5 use `match_commit=True`, `window=24h`. Drift uses `match_commit=False`, `window=None` (mirrors compliance). CI producer + F0.5 `missing_block` condition are DEFERRED to a later iterate (no autonomous data source / writer-cannot-detect-own-absence respectively).
- **Commit:** aab9bd7
- **Rationale:** Producer pattern locked by iterate-1a: each producer is a thin caller of the SSoT helper. Centralizing into one big emit-helper would force a flag-laden API; per-producer adapters stay readable. Deferring CI follows the 'no autonomous data source' rule; deferring missing_block follows the 'helper site is where the detection happens' rule (surface_verification.py is the writer, can't detect its own absence).
- **Consequences:** Operator inbox now receives 4 additional finding sources without flooding the backlog. Each producer is best-effort: per-finding exceptions log to stderr, top-level wrappers never re-raise. KNOWN_SOURCES tuple extended (informational only — source is free-form). 40 new tests in shared/tests/ (mirroring iterate-1a's producer test pattern). Test failures count unchanged from iterate-1a baseline (7 pre-existing, 0 new).
- **Rejected:** Centralized one-helper-for-all (rejected: flag-laden API); hook-level emit from Stop chain (rejected: security/perf runners don't run in Stop); stubbing the performance producer because handoff said 'if runner does not exist' (rejected: runner exists at plugins/shipwright-test/scripts/lib/performance_check.py).

---

### ADR-048: Worktree-aware RTM data collection: 6-column FR tables + git-common-dir event log
- **Date:** 2026-05-15
- **Section:** Iterate — bug: RTM 6-column spec + worktree event-log fixes
- **Context:** Running /shipwright-iterate on a brownfield project adopted via /shipwright-adopt produced an RTM showing a false 'Traceability coverage 0%', which the check_rtm_coverage pre-commit hook soft-blocked. Two shipwright-compliance bugs: (A) the FR-table regex parsed only 3- and 5-column tables, never the 6-column adopt format; (B) data_collector read shipwright_events.jsonl from project_root, but it is gitignored so worktree finalization saw an empty event log. Run-ID iterate-2026-05-15-rtm-adopt-worktree-fix.
- **Decision:** data_collector.py: (A) _FR_TABLE_RE now tolerates any number of trailing columns after the optional Description+Source pair; (B) new _resolve_events_path() resolves the log via 'git rev-parse --git-common-dir' so worktree runs read the main repo canonical log. rtm_generator.py and check_rtm_coverage.py left unchanged.
- **Commit:** 9f17372f58895986f0d9a43038007c79a15e9237
- **Rationale:** Trailing-column tolerance keeps the existing capture groups (body stays group 4), so 3/5-col behavior is provably unchanged (old-vs-new regex probe: identical on 13 cases). Reusing the established git-common-dir pattern keeps the worktree fix consistent with session_role and detect_parallel_sessions.
- **Consequences:** Adopted/brownfield projects now get accurate event-sourced RTM coverage; worktree-based iterate finalization no longer false-blocks commits. 3-/5-column parsing and non-worktree event-log reads are byte-identical to before. 312 compliance tests pass; F0.5 cli surface verified.
- **Rejected:** Importing session_role._resolve_main_repo_root cross-plugin (fragile sys.path coupling to a private shared function; ~15-line duplication is cheaper). Touching the check_rtm_coverage hook (the hook is correct — it read bad input).

---

### ADR-049: Unconditional worktree isolation for /shipwright-iterate
- **Date:** 2026-05-16
- **Section:** Iterate - change: unconditional worktree isolation
- **Run-ID:** iterate-2026-05-15-iterate-worktree-isolation
- **Context:** Parallel /shipwright-iterate sessions shared one working tree; each ran git checkout -b without committing, so three change-sets mixed uncommitted in one tree. Worktree isolation was opt-in (a detected Parallel menu only); branch base was stale local main; F11 merged in the shared tree; ADR-number, CHANGELOG and iterate_history were merge/race hotspots. Incident 2026-05-15.
- **Decision:** Every iterate run is unconditionally and structurally isolated. New B1a step setup_iterate_worktree.py always creates .worktrees/<slug> plus branch iterate/<slug> from freshly-fetched origin/<default>. F0/F11 check_iterate_isolation.py leak-guard fails closed on a main-tree leak. F11 pushes the branch and opens a PR (no merge-to-main). F3 writes a run-id-keyed decision-drop; aggregate_decisions.py assigns ADR-NNN at the serialized changelog step. The session-role machinery (4 scripts) is deleted.
- **Commit:** (assigned post-merge)
- **Rationale:** Isolation must be structural, not detected: a session that stalls before writing a marker is invisible to marker-based detection. Logic lives wholly in the skill plus its tool scripts so every launcher (webui, VS Code, CLI) gets byte-identical behavior.
- **Consequences:** Parallel iterates can no longer cross-write or collide on ADR numbers, CHANGELOG or iterate_history. Each run pays worktree rehydration cost (npm install / uv sync). Iterate runs never touch the main tree. C4 canon check and the finalization verifier are now decision-drop/run-id aware; the iterate Stop hook is worktree-aware. Self-review plus a full code review (7 findings, all addressed) completed.
- **Rejected:** Conditional Parallel mode plus session-role markers (the prior design, itself a race). A merge-time ADR-renumber git hook for H (launcher/infra logic, not deterministically unit-testable) in favor of the decision-drop pattern mirroring the proven F4 changelog-drop.

---

### ADR-050: Worktree-aware event-log resolution
- **Date:** 2026-05-16
- **Section:** Iterate — bug: events.jsonl worktree-awareness + dashboard run_id WARN
- **Run-ID:** iterate-2026-05-16-fix-events-worktree-aware
- **Context:** Under /shipwright-iterate worktree isolation, F7 recorded its work_completed event at the literal --project-root (the ephemeral worktree), so the event was discarded on `git worktree remove` and never reached the main repo's shipwright_events.jsonl. The F11 verifier's check_events_has_commit read the same empty worktree copy. Separately, the dashboard run_id check WARNed every iterate because F5b renders build_dashboard.md before the F6 commit SHA exists.
- **Decision:** Added shared SSoT helper events_log.resolve_events_path (git rev-parse --git-common-dir) and wired the worktree-reachable accessors to it: record_event.py (F7 read/append + lock path), verifiers/iterate_checks.check_events_has_commit (F11), config.read_events (F5b dashboard). The F0/F11 leak-guard (worktree_isolation.py) exempts shipwright_events.jsonl(.lock) as a designed main-tree write. F5b embeds the iterate run_id in the dashboard header so the verifier passes deterministically.
- **Commit:** (assigned post-merge)
- **Rationale:** Models compliance's data_collector._resolve_events_path but hardens it: drops --path-format=absolute (a Git 2.31+ dependency that would silently fall back on older git) and emits a warnings.warn diagnostic on the git-failure fallback so silent data loss is visible; strips GIT_DIR/GIT_COMMON_DIR/GIT_WORK_TREE so resolution stays pinned to project_root. The deterministic run_id embed replaces a brittle mtime heuristic.
- **Consequences:** F7 events now reach the main repo's event log and the F11 verifier finds them; the dashboard WARN is gone. Tracked-events repos (the Shipwright monorepo itself) get an uncommitted 'M shipwright_events.jsonl' in the main tree post-F7 — the existing chore-commit pattern captures it. A drift meta-test (test_events_log_ssot.py, forward+reverse) guards the SSoT. Self-Review: 7-point pass; 2629 tests green + 0 regressions (7 pre-existing baseline verified on origin/main); external + code review findings applied.
- **Rejected:** mtime-600s dashboard freshness heuristic (brittle to clock skew/slow CI — external review). Re-rendering the dashboard between F7 and F11 (leaves build_dashboard.md dirty post-commit, never in the PR — fights atomic-F6). Stale-lock bypass for the centralized .lock (gold-plating — _FileLock uses crash-safe OS advisory locks). Migrating data_collector to the shared helper (cross-plugin import architecture change, out of scope; a parity test guards drift instead).

---

### ADR-051: Adopt seeds External Review on; drops dead plan-config key
- **Date:** 2026-05-16
- **Section:** Iterate — bug: adopt external-review config defaults
- **Run-ID:** iterate-2026-05-16-fix-adopt-review-config
- **Context:** shipwright-adopt's config_writer seeded shipwright_iterate_config.json with external_review.feedback_iterations: 0, which the shared resolver maps to status 'user_disabled' — External Review silently skipped for every onboarded repo. It also wrote a flat 'external_review_feedback_iterations' key into shipwright_plan_config.json that had zero readers repo-wide.
- **Decision:** write_iterate_config now seeds feedback_iterations: 1, consistent with the shared default (shared/config/external_review.json) and greenfield /shipwright-project. write_plan_config no longer writes the dead flat key. Stale '=0' descriptions in dry_run_reporter.py and the adopt SKILL.md were corrected; the residual dead key was removed from the monorepo's own shipwright_plan_config.json.
- **Commit:** (assigned post-merge)
- **Rationale:** A 0 seed claimed the user opted out when Adopt did so unilaterally and unannounced; brownfield code is higher-risk and benefits more from review. The flat plan-config key was never read — making plan-phase review project-steerable would require extending the resolver, deliberately out of scope for this bugfix.
- **Consequences:** Adopted (brownfield) repos get External Review enabled by default, like greenfield. The no-API-key case is handled by the resolver's 'missing_keys' -> interactive prompt (Branch B), not a silent opt-out. Two regression tests guard both defects (producer->file->resolver round-trip; plan-config dead-key absence).
- **Rejected:** Keep feedback_iterations: 0 (disguised opt-out, contradicts greenfield default). Extend the resolver to read a plan-config key (scope expansion — only dead-key removal was in scope).

---

### ADR-052: Triage detectors: canonical drift dedup keys + drift/F0.5 auto-resolve
- **Date:** 2026-05-16
- **Section:** Iterate — bug: triage detector dedup + auto-resolve
- **Run-ID:** iterate-2026-05-16-fix-triage-dedup-resolve
- **Context:** The drift SessionStart detector built triage dedup keys from raw filesystem paths. os.path.abspath does not canonicalize Windows drive-letter case, so the same CLAUDE.md produced c:\... and C:\... keys across runs and the exact-string match in append_triage_item_idempotent appended a duplicate item. Separately, check_drift.py and surface_verification.py only ever appended: once a drift cleared or an F0.5 surface check passed on retry, the stale triage item lingered forever as noise.
- **Decision:** Canonicalize the content-drift dedup-key path in the producer via os.path.normcase(os.path.realpath()), in a new _canonical_anchor helper; triage.py is left untouched. Add a resolve pass to check_drift.py and surface_verification.py modeled on the auditResolved pattern in audit_detector.py: each run dismisses still-open items from that producer whose dedup key left the current finding set, with reason driftResolved or f05Resolved. The drift resolve is scoped to :timestamp and :content keys because artifact_sync.py shares source=drift; the F0.5 resolve is scoped to the (run_id, surface) key prefix.
- **Commit:** (assigned post-merge)
- **Rationale:** Path canonicalization belongs in the producer, not in triage.py, because the storage layer also dedups non-path compliance codes (A5.0, B7, E1, G2) that must never be case-folded. The resolve pass mirrors the established auditResolved pattern for framework consistency. source=drift is shared by two producers, so the drift resolve scopes by dedup-key suffix; F0.5 keys embed run_id and surface so a re-run on a different surface cannot retract another surface genuine failure.
- **Consequences:** Drift no longer double-reports one CLAUDE.md across drive-letter casings, and cleared drift plus recovered F0.5 checks self-retract their triage items. Pre-fix duplicates and stale items auto-dismiss on the next detector run rather than being retroactively merged. check_drift._emit_drift_to_triage lost its empty-findings early return, so every SessionStart now runs a read_all_items scan: acceptable, since triage.jsonl is small and not yet compacted.
- **Rejected:** (1) normcase inside append_triage_item_idempotent: would case-fold non-path compliance keys. (2) Retroactively merging already-written duplicates: out of scope; the resolve pass dismisses them as stale instead. (3) A generic resolve helper in triage.py: audit_detector.py keeps its resolve inline, so per-producer inline keeps the pattern consistent. (4) check_drift resolving every source=drift item: would wrongly dismiss artifact_sync.py :artifact items.

---

### ADR-053: Enforce spec-impact classification on every feature/change iterate
- **Date:** 2026-05-16
- **Section:** Iterate — feature: spec-impact gate
- **Run-ID:** iterate-2026-05-16-spec-impact-gate
- **Context:** The iterate 'Step 2: Spec Update (always)' contract was prose-only and unenforced — empirically ~27 of 28 iterates never touched spec.md, so whole subsystems (Triage Inbox, F0.5 gate) landed with no FR and the build dashboard showed feature rows with an empty FRs column.
- **Decision:** Every FEATURE/CHANGE iterate classifies spec impact as ADD/MODIFY/REMOVE/NONE. Two enforcement layers: record_event.py exits 1 for a feature/change iterate event naming no FR without a spec_impact=none justification; the F11 verifier check_spec_impact_recorded fails a run whose commit touched no planning spec.md. Group D5 audits the historical backlog. Removed FRs move to a Removed Requirements section the FR parsers exclude.
- **Commit:** (assigned post-merge)
- **Rationale:** A soft contract violated 27/28 times is not a contract. The verifier tolerates legacy events (no spec_impact) by falling through to a git-diff check, so it composes with un-migrated history.
- **Consequences:** Feature/change iterates fail-closed at F7/F11 unless they touch a spec.md or justify spec_impact=none. BUG iterates are exempt. SKILL.md Step 2 restructured; F7 gains --spec-impact. The ~5 pre-existing feature events with no FR linkage now surface as Group D5 findings — expected backlog, backfilled separately.
- **Rejected:** A Status column on the FR table (rejected — needs migrating every spec + the RTM parser; a separate Removed Requirements section needs neither). Leaving Step 2 as prose (rejected — that is the failed status quo).
