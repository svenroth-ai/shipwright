{
  "run_id": "iterate-2026-08-01-cache-sync-not-distributed",
  "date": "2026-08-01T08:22:34.135028Z",
  "event_at": "2026-08-01T07:34:46.307762+00:00",
  "type": "bug",
  "complexity": "small",
  "branch": "iterate/cache-sync-not-distributed",
  "spec": ".shipwright/planning/iterate/2026-08-01-pin-python-311.md",
  "tests_passed": true,
  "adr": "iterate-2026-08-01-cache-sync-not-distributed",
  "test_completeness": {
    "status": "complete",
    "behaviors": [
      {
        "behavior": "a git-tracked, not-distributed file no longer counts as missing_in_cache",
        "disposition": "tested",
        "evidence": "test_repo_side_only_filtering PASSED; the DRY-hoist mutation FAILS it"
      },
      {
        "behavior": "the CACHE side is NOT filtered, so a stale synced copy stays visible",
        "disposition": "tested",
        "evidence": "test_repo_side_only_filtering PASSED (asserts walk_tracked_files still sees it); hoist mutation FAILS it"
      },
      {
        "behavior": "compare_tree returns state=ok for a repo-only pin, and still counts a cache-only copy",
        "disposition": "tested",
        "evidence": "test_a_tracked_pin_absent_from_the_cache_is_not_drift PASSED; hoist mutation FAILS it"
      },
      {
        "behavior": "every literal name the sync's COPY blocks refuse is in NOT_DISTRIBUTED",
        "disposition": "tested",
        "evidence": "test_the_comparator_knows_exactly_what_the_sync_refuses_to_copy PASSED; emptying NOT_DISTRIBUTED FAILS it"
      },
      {
        "behavior": "that scan reads the copy blocks only, never the prune blocks",
        "disposition": "tested",
        "evidence": "same test, scoped via _FIND_BLOCK/_COPY_SOURCES; test_the_prune_paths_do_not_exclude_it pins the other half"
      },
      {
        "behavior": "a filter that empties a non-empty listing refuses instead of reporting state=ok",
        "disposition": "tested",
        "evidence": "test_a_filter_that_empties_the_listing_refuses_instead_of_reporting_ok PASSED; removing the guard FAILS it"
      }
    ],
    "counts": {
      "testable": 6,
      "tested": 6,
      "untestable": 0,
      "untested_testable": 0
    },
    "enumeration_basis": {
      "acs": 2,
      "covered_acs": 2
    },
    "note": "Two acceptance criteria - the false drift is gone, and the repo-side-only asymmetry cannot be refactored away silently - expanded into 6 behaviors. One known gap is recorded in the review record rather than here as a behavior: the two-list pin covers literal -name exclusions only, not *-globs or -not -path. No such exclusion exists today."
  },
  "surface_verification": {
    "surface": "cli",
    "tests_run": 48,
    "exit_code": 0,
    "evidence_path": ".shipwright/runs/iterate-2026-08-01-cache-sync-not-distributed/surface_verification.json",
    "detail": "Drove the cache-sync modules as one root via the real pytest. The decisive evidence is the checker itself against the REAL cache: 14 plugin trees reported drift before, 0 after (the one remaining tree is genuine drift from this worktree's own edited test file, which a post-merge re-sync clears)."
  }
}
