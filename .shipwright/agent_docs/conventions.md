# Conventions — shipwright

## Linter / Formatter

- **Linter**: _none detected_
- **Formatter**: _none detected_
- **TypeScript strict**: no
- **.editorconfig**: _none_

## Project-specific rules

Python 3.11+ with uv as package manager. All scripts are invoked via uv run. Hooks resolve plugin paths via ${CLAUDE_PLUGIN_ROOT}. Config files written to target projects use the prefix shipwright_ (e.g. shipwright_run_config.json) and environment variables use SHIPWRIGHT_ (e.g. SHIPWRIGHT_SESSION_ID, SHIPWRIGHT_PLUGIN_ROOT). Commits follow Conventional Commits with the plugin name as scope (e.g. fix(adopt): ..., feat(security): ...). Branches for self-monorepo work follow iterate/<short-kebab-description>. After any push that touches plugin-side files, scripts/update-marketplace.sh syncs the runtime plugin cache. Linting uses ruff; type-checking uses pyright; tests use pytest. The canonical user-facing documentation is docs/guide.md; the canonical hook + pipeline reference is docs/hooks-and-pipeline.md. CLAUDE.md captures operational rules; generated agent_docs link back rather than duplicating these sources of truth.

## Commit messages

- Use Conventional Commits: `feat:`, `fix:`, `refactor:`, `docs:`, `test:`, `chore:`
- Scopes should reflect module boundaries (e.g., `feat(auth): ...`)

## Files

- Keep files under 300 lines; split larger modules.
- Tests live alongside implementation with `.test.*` / `_test.*` suffix OR in a `tests/` directory — whichever is consistent with the rest of the codebase.

## Learnings

> **One line per learning, no bold paragraphs** — always-loaded Layer-1 context. Format: `- (YYYY-MM-DD) <phase> — <one-line rule/gotcha>. → <run_id|ADR-NNN>`. **Budget ≤ 600 chars; if a learning won't fit one line it is a *decision* — write an ADR and leave a one-line pointer here.** Enforced repo-agnostically by the F11 verifier + `shared/scripts/tools/check_agent_doc_budget.py`; see `references/reflection.md`. Full rationale for compacted entries is in [`../planning/adr/_archive-agent-doc-updates.md`](../planning/adr/_archive-agent-doc-updates.md).
- (2026-07-28) iterate/security — detection that changes no decision is not detection: the first cut spotted a rule-less gitleaks config, wrote the reason into the coverage row's `detail` and left the status `covered`, so `is_complete()` stayed true and the card still said every class was checked beside a detail saying the scan looked for almost nothing. Force the STATUS — but only for a class that would otherwise be `covered`, since `degraded` outranks `not_requested`/`not_available` and overwriting either puts a false statement in the manifest. → iterate-2026-07-27-security-scope-and-parity
- (2026-07-28) iterate/compliance — when a finding offers two fixes, measure which ones are POSSIBLE before choosing between them: "restrict `grade_snapshot` emission to main-lineage regens" read as the tidier option until `git log -S <event-id>` showed 18/18 sampled snapshots arrived via an iterate PR squash commit, i.e. 0 of 183 would have survived the filter. The alternative was not worse, it was empty — and an option that silently produces nothing looks identical to a quiet project. → iterate-2026-07-28-grade-snapshot-lineage
- (2026-07-28) iterate/verification — a re-scan proves nothing until you check what it SCANNED: `prompt_injection_scan.py --path` takes a DIRECTORY, so passing the single changed file returned `files_scanned: 0, total_findings: 0` — byte-indistinguishable from a clean fix. Run the negative control in BOTH directions and assert the population is identical (419 files before and after, 1 finding → 0); a zero-finding result whose denominator is also zero is a vacuous pass, not evidence. → iterate-2026-07-28-pathlib-import-normalize
- (2026-07-28) iterate/security — an npm `maintenance-vN` dist-tag means that line is MAINTAINED, not that a CVE was backported to it: brace-expansion 2.1.2 was the head of `maintenance-v2` yet shipped 2026-07-08, fifteen days BEFORE the 5.0.8 fix, so reading the tag as a backport would have dismissed a real high as a false positive. Decide from publish dates and the INSTALLED source (grep the symbol the advisory says the fix added), never from the tag. → iterate-2026-07-28-security-pyasn1-bump-brace-accept
- (2026-07-27) iterate/review — a rule everyone routes around is evidence about the RULE: the external code review fired "iff the internal subagent fired", and 15 of 27 records recorded `code=not_run` while running the external one anyway. Nobody filed that as a defect because the outcome was fine each time. Count the overrides before trusting a contract — universal non-compliance with a safety rule usually means the rule mis-states the intent, not that everyone is careless. → iterate-2026-07-27-review-floor-not-chained
- (2026-07-27) iterate/review — "all types answered" is bookkeeping, not review: `check_review_record` verified only that no type was left `pending`, so all five could be `not_run` with dispositions and F11 went green. When a gate's subject is "did X happen", assert on the terminal VALUE, not on the absence of `pending` — and refuse `not_applicable` as satisfaction, else the gate is passable by re-labelling. → iterate-2026-07-27-review-floor-not-chained
- (2026-07-27) iterate/review — decoupling two chained rules exposes contradictions the chain was hiding: making the external cascade independent left a blanket "for trivial/small it does NOT run" beside a trigger listing risk-flags and >100-line diffs, so a small risky run both fired and did not. The chain had resolved it implicitly. After removing a dependency, re-read every sentence that assumed it. → iterate-2026-07-27-review-floor-not-chained
- (2026-07-27) iterate/imports — a FAILED submodule import still binds its parent: `from scripts.lib.x import y` raising ModuleNotFoundError leaves `scripts.lib` cached in sys.modules on whichever dir won, and a regular package never re-resolves. That is why compliance.py's prepend succeeded (`scripts.__path__[0]` WAS compliance's) yet the import still failed. Namespace packages do re-resolve; regular ones do not, and 10 of 14 plugins ship `scripts/lib/__init__.py`. Diagnose by printing `sys.modules[pkg].__file__`, never by reading sys.path. → iterate-2026-07-27-pytest-root-composition
- (2026-07-27) iterate/review — a source-only reviewer's mechanism claim is a hypothesis until executed: two "medium" findings that the new root conftest leaks `<repo>` onto sys.path in plugin sessions were both FALSE — a 4-line probe under `cd plugins/x && pytest tests/` showed it is never loaded, since each plugin owns `[tool.pytest.ini_options]` so rootdir is the plugin. Two others (subdir-relative args; a conftest-name collision firing before the guard) were TRUE, confirmed the same way. Probe before accepting OR rejecting. → iterate-2026-07-27-pytest-root-composition
- (2026-07-28) iterate/security — a parser must agree with the producer about what ends a line: `str.splitlines()` breaks on nine characters git does not, and subprocess `text=True` rewrites a lone CR to LF BEFORE any parser runs, so either alone lets an untrusted diff forge a `diff --git` header from inside a hunk. Corollary — a sanitiser must neutralise the SAME alphabet its parser ignores, or the characters that survive parsing land unescaped in the sink. → iterate-2026-07-27-pr-review-forged-boundary

- (2026-07-27) iterate/design — before fixing a two-phase write (write, then consume the inputs), probe the OTHER partial state: consuming is not atomic either, so the obvious fix — replace the section a re-run finds — would have overwritten a 3-bullet record with the 1 bullet the surviving drops rendered, DELETING released history. The duplicate it replaced loses nothing. Replace only when it cannot lose anything; otherwise stop and name the disagreement. → iterate-2026-07-27-changelog-aggregator-idempotency
- (2026-07-27) iterate/review — an external panel that approves is not a second opinion: GPT+Gemini approved this diff, then the internal cascade found a HIGH-severity honesty defect (a widened acceptance criterion promising a refusal the OTHER writer never implements, contradicted by its own green test) plus two assertions that could not fail (`"2" in message` against a tmp path containing a 2). Widening a criterion obliges re-reading every implementation it now covers. → iterate-2026-07-27-changelog-aggregator-idempotency
- (2026-07-27) iterate — `importlib.util.spec_from_file_location` picks a loader by SUFFIX and never stats the path, so for a `.py` it always returns a spec: a `if spec is None` guard carrying the friendly "file missing" message is DEAD CODE and the caller gets a bare FileNotFoundError. Check `path.is_file()`, and pop `sys.modules[name]` if `exec_module` raises or the memoised half-built module poisons every later call. → iterate-2026-07-27-changelog-aggregator-idempotency
- (2026-07-27) iterate/scope — "we surface scanner failures" covered the CRASH and never the ABSENCE: a fataled tool records a scan_error and fails the run, but a tool that was never installed produces no leg at all, so a one-scanner machine reported clean for every class it could not check. Auditing a "we surface failures" claim means asking separately what happens when the thing never starts. → iterate-2026-07-27-security-coverage-manifest
- (2026-07-27) iterate/build — prefer a DERIVED manifest over a backend-populated channel: coverage is fully determined by `(capabilities, scan_types, scan_errors)`, all already held by the caller, so a pure `build_coverage()` is something no backend or test mock can forget to fill — a forgotten channel defaults to an empty list that reads as reassuring, a derivation cannot. → iterate-2026-07-27-security-coverage-manifest
- (2026-07-27) iterate/review — "not reported" and "incomplete" are different truths: appending a synthetic row to an EMPTY coverage manifest converted *we do not know what was checked* into *we know, and one class is outstanding*. Add a row only when it carries information, never merely to avoid an empty list. → iterate-2026-07-27-security-coverage-manifest
- (2026-07-27) iterate/review — an artifact WE write is untrusted once a caller can hand it back: `--input` / `--input-from-cache` made the coverage manifest caller-supplied, and its labels reach an operator-facing report. Hardening only the obviously-external values (`--repo`, a path) and trusting our own format was the gap. → iterate-2026-07-27-security-coverage-manifest
- (2026-07-27) iterate/F11 — the PR-Review gate fails closed over 200,000 diff chars, and a 4-item card measured 288,963 (1.44x). Measure the reviewable diff with the gate's own `filter_generated_paths` BEFORE deciding between splitting and overriding; excluding generated files alone left 252k, so a split was the only honest route. → iterate-2026-07-27-security-coverage-manifest
- (2026-07-27) iterate/debug — when a fix removes a contention failure, RE-MEASURE before believing it: hardening the writer here moved the failure to the reader (more writes succeeding = more delete-pending windows), and the second face only appeared on the re-run. A single post-fix green run would have shipped it. → iterate-2026-07-27-run-unit-parallel-race
- (2026-07-27) iterate/evidence — at a ~2% failure rate, 0-in-72 is what an UNFIXED build yields 22% of the time ((47/48)^72); rule-of-three caps the 95% bound at 4.2%, above the pre-fix estimate. Rate data corroborates, it does not demonstrate — make a deterministic repro (100% → 0%) the load-bearing evidence and say which is which. → iterate-2026-07-27-run-unit-parallel-race
- (2026-07-27) iterate/design — a "what changed?" check needs a BOUNDARY, and a phase without a commit has none: the design touch check used `git diff HEAD` + `ls-files --others`, but nothing in the pipeline commits before build, so every untracked spec.md the project phase wrote counted as this round's edit and ANY behaviour declaration passed. Port the boundary, not just the predicate — snapshot at round start. → iterate-2026-07-27-requirement-writeback-loop
- (2026-07-27) iterate/test-design — a corpus property test that applies a writer ONCE proves only the INSERT path; the REPLACE path (the only one that deletes a span) had zero byte-preservation coverage and shipped a regression deleting trailing prose and the Keep-a-Changelog link footer. Parametrize such a corpus over the OPERATION (1 run / 2 runs / revised 2nd run), not only over inputs — breadth of fixtures hides an absent operation. → iterate-2026-07-27-changelog-writer-preserve-history
- (2026-07-27) iterate — bounding a document section at "the next heading or EOF" makes the LAST section swallow everything below it, so a re-run deletes the trailing footer; bound it at what the section actually OWNS (blank / deeper-heading / list lines). Stopping early can only strand content, never delete it — prefer the direction whose failure mode is recoverable. → iterate-2026-07-27-changelog-writer-preserve-history
- (2026-07-27) iterate/probe — a probe reporting "finding does not reproduce" must itself be proven able to detect it: mine tested for a literal backslash-r-backslash-n (f-string over-escaping) and wrongly cleared a REAL LF→CRLF whole-file rewrite. A negative probe needs the same mutation check as a green test. → iterate-2026-07-27-changelog-writer-preserve-history
- (2026-07-27) iterate/bug — a test asserting a CLI argument is REQUIRED but never that it is USED manufactures false confidence: rollback.py insisted on `--target-ref`, discarded it, and reported success naming a version it never sent — green from the day it shipped. Assert the OUTBOUND call carries the value (record calls, not the return payload). When an argument has nowhere to go, read the API contract before adding a pass-through: `vcs/update` takes no ref, so it had to be pinned on the VCS project first. → iterate-2026-07-27-rollback-uses-target-ref
- (2026-07-27) iterate — a schema field with no reader is a promise nobody keeps: all three deploy profiles had declared `poll_interval_seconds`/`max_wait_seconds` since they were written while the liveness check still asked once with a fixed 10s limit. Grep for the consumer in the same diff that adds a shared-schema field, or record it as unimplemented; and when wiring one up later, keep the no-config default identical to today's behaviour so existing callers do not silently change speed. → iterate-2026-07-27-rollback-uses-target-ref
- (2026-07-27) iterate/F0 — a gate that deliberately does NOT stop still owes a durable record: put the teeth on the RECORDING (green suite + unwritable card = rc 3), never on the verdict, and let the APPEND (fsync under the store's own lock) be the authority so a damaged store elsewhere cannot false-STOP. Complements test_f0_5_no_triage_emit: a gate that DOES stop files nothing, because that is the board's job. And a card's reproduce command must be the argv that actually ran - a plausible one is worse than none, because it is trusted. -> iterate-2026-07-27-f0-race-triage
- (2026-07-27) iterate/review — a fix applied AFTER a code review is UNREVIEWED code: while fixing the code reviewer's "don't re-declare the banner regex" finding I replaced an absolute path bootstrap with `from scripts.lib._provenance import …` in a file that other plugins load BY FILE PATH, turning a green local suite into a CI-red (the ADR-045 collision). The doubt reviewer caught it only because it re-read the FILES, not the reviewed diff. Re-run the affected suites after every review fix. → iterate-2026-07-27-artifact-state-stamping
- (2026-07-27) iterate/review — narrowing a diff to beat external-reviewer truncation invents false findings: a 1691-line diff came back at 29 and 504 chars (not a review), so I re-sent a production-code-only slice — and its two highest findings were "the tool is wired to nothing" and "no tests exist", both true of the slice and false of the change. Label a subset review as one, or split by file group instead of dropping files. → iterate-2026-07-27-artifact-state-stamping
- (2026-07-27) iterate/build — a status word inside an identifier silently forges a verdict when a token line is parsed by substring: `"clean" in line` read an UNRESOLVED dirty flag back as a confident `False` for any run id ending `-cleanup` (which is what SIMPLIFY-mode runs are called). Parse whitespace-delimited banners by exact token, and refuse interior whitespace in the values so the token invariant isactually true. → iterate-2026-07-27-artifact-state-stamping
- (2026-07-27) iterate/review — a retraction is as prone to overclaiming as the claim it replaces: this run retracted "the GUI is a thin wrapper" and shipped a NEW falsehood in its place ("the Command Center stores a reason unvalidated"), refuted by the very 20 lines of TypeScript it cited as proof — which cap at the same 500 and strip the same control-char class. Re-read the cited evidence when writing the correction, not only when finding the error. → iterate-2026-07-27-triage-defer-review-followup
- (2026-07-27) iterate/review — RETRACTION: the Triage GUI is NOT "a thin wrapper over the same library helpers" (this file, ADR-057, three shipped docs). It is a separate implementation in `shipwright-webui` mirroring `triage_promote`'s semantics, and the two diverge — its snooze route permits a reason-less park the CLI rejects, which is why the renderer needs a `(no reason recorded)` fallback. ADR-057 is corrected forward-only, by this run's decision-drop, not by editing its entry (an iterate must not write decision_log.md). → iterate-2026-07-27-triage-defer-review-followup
- (2026-07-27) iterate/review — a shared sanitizer is wrong for half its callers when its policy is keyed to one caller's SHAPE: `strip_control_chars` keeps `\n`/`\t` because launch payloads are multi-line, so reusing it for a single-line title/reason let a stored newline forge a whole listing row — and the acceptance test passed anyway, because it planted ESC/BEL, exactly the characters that sanitizer does strip. Pick by the field's shape (inline vs multi-line), and probe injection with the character the current implementation KEEPS. → iterate-2026-07-27-triage-defer-ci-cap
- (2026-07-27) iterate/review — a truncated external review is not a clean pass: one reviewer's reply stopped at 523 chars just after quoting a block and saying "there is a BUG here", never naming it, while `reviews_succeeded: 2` read like two opinions. Record it as truncated and hand-inspect the quoted block instead of counting it — doing so here found a display-sanitized value deciding a control-flow branch. → iterate-2026-07-27-triage-defer-ci-cap
- (2026-07-27) iterate/review — a gate that reports 100% missing is read as noise, not as a broken gate: `check_s5_fr_coherence` called all 19 of this repo's requirements "missing acceptance" for months because `spec_parser` only knows the `**Acceptance Criteria:**` label while both generators emit `### FR-XX.YY` + bullets. Before building a check "because none exists", RUN the one that claims to — a false-positive gate and an absent gate look identical from the dashboard, and only one of them is cheap to fix. → iterate-2026-07-27-project-granularity-basis
- (2026-07-27) iterate/build — a doc rule is only as consistent as its stalest COPY: correcting `fr-authoring.md` + `requirement-elicitation.md` + the generation template still left the same `Basis: assumed` gloss contradicting them in `lib/fr_basis.py`'s docstring and both iterate path references. Grep the old wording to enumerate the surfaces; reasoning about which files "should" carry a rule is how the fourth copy hides. Pin every surface found, not the ones you designed for. → iterate-2026-07-27-project-granularity-basis
- (2026-07-27) iterate/review — a matcher for "does X have a block" must anchor on the id STARTING the heading: `.*\bFR-01.01\b` let `### Notes for FR-01.01` plus any bullet satisfy the requirement, a false GREEN inside the check built to catch missing criteria. Test the discussion-heading case explicitly; the safe failure direction for an advisory check is a false warning, never a false pass. → iterate-2026-07-27-project-granularity-basis
- (2026-07-27) iterate/adopt — a sentinel module loader must register in `sys.modules` BEFORE `exec_module`: stdlib `dataclasses` resolves `cls.__module__` through `sys.modules` at class-creation time, so `spec_table._load_shared` raised `AttributeError` the moment it was pointed at `fr_basis` (which has a `@dataclass`). `validate_adoption._jsonl_records` already had the right order and the comment saying why. Unwind on failure keeps the no-half-memo property. → iterate-2026-07-27-adopt-derived-catalogue
- (2026-07-27) iterate/adopt — a LAZY import is not automatically ADR-045-safe: deferring `from lib.spec_table import ...` only defers WHICH `lib` is bound, and compliance's Group-I round-trip imports adopt's `_render_spec_md` after binding its own, so the chain vanished. Path-load under a sentinel with BOTH `scripts/` and `scripts/lib` on sys.path — fixing only the first hop moves the failure one import down. Only the cross-plugin F0 sees it. → iterate-2026-07-27-adopt-derived-catalogue
- (2026-07-27) iterate/review — a fail-closed READER is worthless if the consumer bypasses it: Step H pulled `doc["unconfirmed"]` out with a bare `json.loads`, skipping every integrity check at the one place the count is published. Give the module a `read_summary(project_root)` and make BOTH the handover and the validator go through it; an optional cross-check is dodged by deleting the field, so require the totals. → iterate-2026-07-27-adopt-derived-catalogue
- (2026-07-27) iterate/review — splitting a card is a correctness pass, not just packaging: cutting this one in two exposed that PR1's commit body and handoff banner named a Triage card only PR2 files. Rule when splitting: each PR may claim ONLY what it does, and a validator ships with the step that writes the artifact it demands — otherwise the tree is broken between merges. → iterate-2026-07-27-adopt-derived-catalogue
- (2026-07-27) iterate/review — closing a data boundary at ONE field is not closing the boundary: the failure entries were whitelisted, then `baseline_source` persisted the raw `command` (`TOKEN=… pytest`), then `source`/`command` turned out to be `str()`-coerced so an object's repr got persisted. Three rounds, one boundary. Ask what ELSE reaches the committed artifact, and type the fields that name it. → iterate-2026-07-27-adopt-inherited-baseline
- (2026-07-27) iterate/adopt — write the artifact BEFORE the triage cards that cite it: the gap cards say "full list in shipwright_known_failures.json", so filing them first means a failed write leaves durable cards pointing at a file that does not exist. Order the side effects by what survives the run. → iterate-2026-07-27-adopt-inherited-baseline
- (2026-07-27) iterate/docs — a `str.replace` doc edit is a SILENT no-op when the search text is not in that branch's base: splitting a PR moved `docs/guide.md` onto a base without the combined branch's wording, so the user-facing documentation never shipped while the sibling doc did (asserted, hence caught). Assert every doc edit, or diff the file afterwards. → iterate-2026-07-27-adopt-inherited-baseline
- (2026-07-23) iterate/build — a work_completed gap (passed<total) with NO explicit `tests.skipped` count is host-gated skips, not failures (green-at-merge Iron Law): key the D4 detective on `total-passed-skipped` but treat a skip-count-less gap as `failed=0` (charitable), NOT `total-passed` with skipped defaulting to 0 — the latter leaves every legacy gap flagged and never fixes the live false-positive that had D4 disabled here. Charitable-when-absent also matches the long-standing test-evidence renderer. → iterate-2026-07-23-tests-skipped-tracking
- (2026-07-23) iterate/review — when N readers share one arithmetic, extract it to ONE SSoT or a predicate WILL drift: the review found `_progression_result` split present/absent on `is None` while D4 + the dashboard used `isinstance(int)`, so a non-int skipped crashed one reader while another silently passed. A shared `shared/scripts/tests_block.py` (top-level per ADR-045, so the compliance plugin imports it without a `lib` collision) makes the divergence structurally impossible. → iterate-2026-07-23-tests-skipped-tracking
- (2026-07-21) iterate/F0 — a fail-CLOSED loader makes a near-miss fixture pass for the WRONG reason: a `test-traceability.json` fixture with `schema_version: 1` (then `collector_version` as an int) silently disabled D1's link proof, so the test asserting the proof still bites went green having exercised nothing. Validate a fixture against the real schema before trusting a test built on it. → iterate-2026-07-21-fr0115-coverage-bloat
- (2026-07-21) iterate/F0 — tighten the bloat baseline LAST, after review fixes: tightening mid-run pinned `group_d.py` at 408, then the review's own fixes pushed it to 412 and the anti-ratchet blocked my own numbers. Guard the update against `git show HEAD:` so a re-tighten can never exceed what origin already recorded. → iterate-2026-07-21-fr0115-coverage-bloat
- (2026-07-21) iterate — `classify_complexity` matches SCOPE keywords against the whole prompt, so merely NAMING a path inflates the estimate: `integration-tests/…` contains `integration`, a `SCOPE_MEDIUM_KEYWORDS` member, which classified a 2-line test-only edit as medium; the keyword-free re-run then falls through to the history median, also non-evidence. Settle scope with the diff-driven `risk_detectors` predicates, which are authoritative, before accepting either number. → iterate-2026-07-21-prompt-scan-dynamic-import
- (2026-07-20) iterate — before building distribution machinery for a "ship X to adopters" item, check whether the capability is already `--project-root`-parameterized: the accepted-risk `converge` CLI already resolved identity + register + triage from any target root, so "ship the register + converge to adopters" collapsed from a cross-plugin packaging effort to a docs section + a guard test. The deliverable of a "ship to adopters" item is often the DOC naming the operator-run path, not new code. → iterate-2026-07-20-accepted-risk-adopter-converge
- (2026-07-20) iterate — sweeping a defect CLASS is not "convert every site the grep matches": weigh each site's REACHABILITY. The run-loop telemetry reader matches the one-record-per-line idiom exactly, but its file is gitignored (never `merge=union`) and single-writer always-`\n`-terminated, so the two-complete-records concatenation the SSoT recovers can never occur there — converting it is pure churn against a behaviour-preserving invariant. Decline with evidence (gitignore + no union attribute), don't convert on class-membership alone. → iterate-2026-07-20-events-record-boundary-remainder
- (2026-07-20) iterate — a drift test that pins a verifier's pattern copy `==` its SSoT (`test_ci_supplychain_patterns_sync`) guards ONLY those two literal copies; a third copy — or a SKILL.md prose summary of the same set — stays unpinned, so grep a distinctive token (`renovaterc`) to prove no third copy exists before trusting the sync test as full coverage. And when you reverse a deliberate `EXCLUDES X` fence, rewrite that comment in the SAME diff so the next reader sees current reasoning, not the retired one. → iterate-2026-07-20-ci-supplychain-shipped-templates
- (2026-07-19) iterate/F3 — `write_decision_log.py --architecture-impact` lazy-imports `lib.agent_doc_shape` and raises `ModuleNotFoundError: lib` unless `shared/scripts` is on `PYTHONPATH`. It fails AFTER appending the ADR row, so the run is left half-written: decision_log has the entry, conventions.md has no `## Convention Updates` bullet, and F11 `check_architecture_documented` then fails. Revert decision_log.md and re-run with `PYTHONPATH=$(pwd)/shared/scripts` rather than hand-writing the bullet. → iterate-2026-07-19-traceability-derived-view
- (2026-07-19) iterate/F3+F11 — `write_decision_log.append_decision` takes `run_id=` and emits the `- **Run-ID:** <run_id>` line that F11's `ADR recorded + present` check resolves run_id-to-ADR through, but the CLI exposes NO `--run-id` flag. So a CLI-driven F3 always omits it, and F11 then FAILS whenever the F5c iterate entry carries the bare run_id as `adr` (which is the documented form). Add the line to the ADR entry by hand after F3, before F6. → iterate-2026-07-19-traceability-derived-view
- (2026-07-19) test-design — a module using relative imports (`from ._x import`) CANNOT be loaded by file path under a sentinel name: it has no parent package, so `exec_module` raises ImportError. A parity test that catches that in a `pytest.skip` reports green while asserting nothing — seen here as 7/7 silently skipped. Cross-realm comparison belongs in a subprocess, plus a guard test that fails if the comparison stops running. → iterate-2026-07-19-traceability-derived-view
- (2026-07-20) compliance — `plugins/shipwright-compliance/scripts/audit/_group_d_traceability.py` sits at 298 lines with TWO of headroom; plan the extraction before editing it, not after the gate says so. Noted here rather than in the file because a warning about a 2-line budget that itself costs a line is self-defeating. → iterate-2026-07-20-converge-table-shape (ADR-108)
- (2026-07-20) iterate — a marker inside a serialized cell needs its SEPARATOR pinned by the producer: `unit (inferred)` and `unit(inferred)` differ by one space, and the second parses to ZERO required layers while keeping advisory provenance, so the requirement silently loses its coverage obligation. Give the format one render function and import the matching regex from the same module. → iterate-2026-07-20-converge-table-shape (ADR-108)
- (2026-07-27) compliance — a tolerant reader that folds "absent" and "unreadable" into one `None` makes the renderer state something the project cannot know: a corrupted record rendered as "never run" is a false claim, not a safe default. Return a tagged state (`absent`/`valid`/`invalid`) whenever the *absence* of data is itself a reported fact. → iterate-2026-07-27-disclose-audit-last-run
- (2026-07-27) compliance — a "latest run" slot that also has to answer "when was the WHOLE thing last done?" needs two records, not one: a narrower run overwrites the broader one and the document then under-reports forever. Keep `last_<x>` and `last_full_<x>` separately, and promote the old value on the first narrow write after the upgrade or the transition itself loses the history. → iterate-2026-07-27-disclose-audit-last-run
- (2026-07-27) compliance — a new module reached through `lib.collectors` must use RELATIVE imports: callers that put `…/scripts` on `sys.path` import it as `lib.*`, where an absolute `scripts.lib.…` does not resolve (ADR-045). The break surfaces far away — a green plugin suite with a red FR-table integration test. → iterate-2026-07-27-disclose-audit-last-run
- (2026-07-27) compliance — leaf value types and the aggregate handed to renderers belong in separate modules: a leaf changes when one artifact's shape changes, the aggregate changes whenever a NEW FACT must reach the renderers, and the second rhythm is far faster (three cards added `timestamp`, `run_id`, `audit_freshness_note` to `ComplianceData` in as many weeks). Keeping them together turns every new fact into a budget fight. → iterate-2026-07-27-disclose-audit-last-run
- (2026-07-27) iterate — when two cards are split to run in parallel and one reserves a seam for the other, take the seam on merge instead of resolving to your own version: #448's provenance block absorbed this card's line with zero renderer edits, which was strictly better than the header-suffix design the conflict was hiding. Read what the other side BUILT, not just what it changed. → iterate-2026-07-27-disclose-audit-last-run
- (2026-07-20) iterate — a placeholder default is not evidence: `generate_adoption_artifacts` defaults an unmatched `source_file` to the em dash `—`, and a truthiness test on it labelled every crawl-only page `code` instead of `observed`. When a display placeholder becomes a semantic input, test for the placeholder set explicitly. → iterate-2026-07-20-converge-table-shape (ADR-108)
- (2026-07-20) iterate — when a serialized format has a named PRODUCER, probe the round-trip through THAT producer; hand-written escape fixtures only prove the reader agrees with the test author. A 12-value probe through `markdown_table.escape_cell` failed 4 cases and exposed a doubled-backslash defect that four of five FR-table parsers had shared unnoticed for months (ADR-107).
- (2026-07-20) iterate — making a composite key a pure function of one field REMOVES the disambiguation the other provided, so fail CLOSED where a tolerant merge would silently delete a record — but scope the refusal to the states that are actually defects (two ACTIVE rows, not a tombstone), and check whether the raise aborts a sibling loop. A key-shape regex pins shape, not agreement (`01::FR-02.03` matches); a non-`ValueError` class keeps it from being swallowed as infra. → iterate-2026-07-20-namespace-from-requirement-id
- (2026-07-19) iterate - the artifact-path-canon and bloat content lints scan git-TRACKED files only, so a NEW file is invisible to them until `git add`. Run them after the LAST file is staged (i.e. after F5b/F5c write their artifacts), never at F0: a green F0 on an unstaged new module is a false green that surfaces as red CI. -> iterate-2026-07-19-one-discovery-function
- (2026-07-19) iterate - a module that is loaded by FILE LOCATION (audit_adapters) can never import a sibling: no package context, so both `from .x` and `from lib.x` raise. Check HOW a shared module is loaded before giving it a new import, and check whether the caller reaches THAT function through the location load. -> iterate-2026-07-19-one-discovery-function
- (2026-07-19) iterate — a triage card's "VERIFIED ALREADY CORRECT, do not redo" is a claim to re-measure, not a fact to inherit: two of three template assertions were false, incl. a shipped security job whose action 404s. Had I trusted it, the guard would have been red on its own PR. → iterate-2026-07-19-adopter-pinning-posture-rule
- (2026-07-19) iterate — a write permission can be proven WITHOUT a destructive test: PATCH the real endpoint with a deliberately invalid body. A 403 precedes body validation, so a 422 ("not a member of […]") proves authn+authz passed while mutating nothing. Phase 1 had recorded this as unanswerable short of overwriting a real alert comment. → iterate-2026-07-18-accepted-risk-alert-convergence
- (2026-07-19) iterate — a briefed site enumeration is a STARTING point, never the scope: "six read sites" was six of sixteen. Sweep the class yourself (`grep` the idiom, not the filenames), and when a review names two of three in one file, convert the third. → iterate-2026-07-19-events-record-boundary-readers
- (2026-07-19) iterate — a shared module reached by BOTH a package import and a by-file-location sentinel load must import its siblings BY PATH: `from lib.X` pollutes `sys.modules['lib']` for the caller, and `from X` needs a sys.path entry no loader inserts. Register in `sys.modules` BEFORE `exec_module` or `@dataclass` creation dies. → iterate-2026-07-19-events-record-boundary-readers
- (2026-07-19) iterate — re-read the spec after every mid-build decision change: five review rejections here were all spec-vs-code drift, never code defects. A number you recall is not a number you measured — regenerate tables from the tree. → iterate-2026-07-19-events-record-boundary-readers
- (2026-07-19) iterate — before building a tool to act on "the matching items", census what the producer actually EMITS: the code-scanning triage surface is one repo-wide `gh-security:{owner}/{repo}` aggregate, so "dismiss the matching triage item" would have silenced every security finding in the repo. A card can name a surface that does not exist. → iterate-2026-07-18-accepted-risk-alert-convergence
- (2026-07-19) iterate — `subprocess.run(text=True)` decodes with the LOCALE codec (cp1252 on Windows), so any `gh` payload with a non-Latin-1 byte raises and the call reads as a failed fetch. Always pass `encoding="utf-8"`. Only a live run surfaced it; every unit test passed because the seam was mocked. → iterate-2026-07-18-accepted-risk-alert-convergence
- (2026-07-18) iterate — a GitHub code-scanning dismissal DOES persist across re-scans, but `updated_at` does not advance on re-observation, so the obvious probe (`updated_at > dismissed_at`) returns zero and reads as "never seen again" — a false NEGATIVE that would have inverted the design. Read `most_recent_instance.commit_sha` instead. This falsifies the recorded "reappear each scan" rationale (decision_log:3319/:3332); what those ADRs actually rejected — dismissal INSTEAD of a fix — stands. → iterate-2026-07-18-accepted-risk-register
- (2026-07-18) iterate - an append-only log's newline invariant was a per-writer CONVENTION with no enforcement point: the writer terminated its own line but never checked the file it appended TO, so one unterminated predecessor put two records on one line and the reader SKIPPED it - corruption read as ABSENCE and an operator dismissal vanished. Probe termination at the append boundary; recover boundaries PARTIALLY (all-or-nothing re-drops the valid prefix); scan INSIDE the lock before an atomic replace. -> iterate-2026-07-18-outbox-newline-corruption
- (2026-07-18) iterate — a change that makes a previously-orphaned tag COUNT must be checked against every gate that regenerates the same artifact, not just the one it was written for: fold resolution satisfied D-orphan but repealed the F11 removal gate's anti-supplement invariant, so `## Removed` + one fold row would dismiss a removed feature's still-dead-tagged tests. Rule — retirement beats folding, and a new provenance field nobody READS (`resolved_from`) is a sign a consumer was missed. → iterate-2026-07-18-fr-fold-map-resolution
- (2026-07-18) iterate — when a change slipped through DESPITE full review (webui #285: medium iterate + external plan review, still reversed an accepted risk), adding `mandatory_review` to a risk flag repeats the control that already failed. Force a RECORDED STATEMENT instead — an ack naming the decision, bound to run_id AND a content fingerprint (path-only lets an author ack "adds a ruff step" then slip in `pull_request_target:`). Distinguish "not a git repo" (SKIP) from "diff unobtainable" (FAIL CLOSED) or sandboxed CLI tests break. → iterate-2026-07-18-ci-supplychain-risk-flag
- (2026-07-18) iterate — semgrep `non-literal-import` re-fires at EVERY new first-party `import_module` site (5th: audit_adapters, _lib_loader, _runners x2, _layer_coverage_regen); fix is always an adjacency-correct inline `# nosemgrep` as the LAST comment line before the call, never repo-wide `SHIPWRIGHT_SEMGREP_EXCLUDE_RULES` (blinds real dynamic imports). Watch the 300-line cap: a multi-line WHY block pushed 298->304, trading a security FP for a NEW bloat crossing — condense to stay <=300. → iterate-2026-07-18-nosemgrep-layer-coverage-regen
- (2026-07-18) iterate — a NEW `.json` compliance snapshot (vs the `.md` DERIVED_MDS) needs FOUR churn-reconciliation sites, not one: CHURN_ALLOWLIST (else the origin/main merge ABORTS), the regenerate staging loop (else dirty-tree), the integrate_main rollback set, AND the hooks-and-pipeline doc-table (both-direction sync test). Mirror `CI_SECURITY_SUMMARY`. → iterate-2026-07-18-churn-allowlist-test-traceability
- (2026-07-17) iterate — pre-existing skipped-test rot is invisible to the monorepo's own hygiene (diff-scoped Self-Review §8 + runtime `import_or_fail_in_ci`; no full-corpus CI gate) — only adopt's repo-wide inventory catches it. Resolve per ADR-044/045: CI-guard where the binary is CI-provisioned (git/uv/coverage), allow-marker where genuinely conditional, delete a no-op `pass` stub — never blanket-suppress. Scope any `fixtures/` inventory-prune to a `tests/` tree so real rot outside one stays visible. → iterate-2026-07-17-test-rot-cleanup
- (2026-07-17) iterate — reusing a whole-file presence oracle (`architecture_doc.run_id_documented`, deliberately lax for drift) to answer a stricter "is there already a BULLET here?" question false-skips when the token appears only in prose (e.g. Data Flow); section-scope the check to the target `## …Updates` header. → iterate-2026-07-17-arch-doc-refresh-harden
- (2026-07-16) iterate — a repo-wide backfill retrofit has two landmines: (1) SCOPE written tags to the `test_links` collector's manifest roots (`integration-tests/`, not `plugins/*/tests`) — tags outside them drop on the next `generate_file` regen, so plugin-test backfill needs a collector-scope extension FIRST; (2) EXCLUDE fixture dirs from the inventory — a naive scan swept the campaign's own golden mini-repos → 14 false "orphans" (synthetic FRs); the engine won't WRITE fixtures but DOES surface them. → iterate-2026-07-15-retrofit-monorepo-and-webui
- (2026-07-16) iterate — a SHARED-side test that must COMPOSE with a plugin collector (backfill → compliance `test_links` manifest) should drive it in a CLEAN SUBPROCESS (`sys.executable -c`, plugin `scripts` on its `sys.path`), not an in-process import: the ADR-045 `lib` collision is BIDIRECTIONAL in one pytest session (another test binds `sys.modules['lib']` first → order-dependent import fail); a subprocess sidesteps it. Read the event log via `events_log.resolve_events_path`, never a raw join (`test_events_log_ssot`). → iterate-2026-07-15-shared-backfill-engine
- (2026-07-16) iterate — a SHARED verifier that lazy cross-plugin-imports a plugin collector must save/clear/restore `sys.modules['scripts']` + force the plugin root to `sys.path[0]` (mirror ADR-045's `lib` loader): a naive `import scripts.lib…` resolved another plugin's already-bound `scripts` in the combined pytest session → CI-red/local-green; also an enforcing gate must REGENERATE its inputs (base+head manifests AND the evidence index from staged reports), never trust a committed/persisted artifact. → iterate-2026-07-15-removal-crosslayer-gates
- (2026-07-15) iterate — a frozen contract's invariants must be ENFORCED structurally, not just documented in prose: the adversarial panel turned every prose-only manifest-schema rule into a bypass (coverage `ok` on a skipped test; a `removed` FR keeping live tests; unit coverage from an e2e-layer link) — encode them with JSON-Schema `if/then`+`contains`; a tag token regex must whole-token-capture + lookahead-guard so `@FR-01.03x` can't expose a valid `@FR-01.03` prefix. Builder≠verifier surfaced it. → iterate-2026-07-15-contracts-and-harness
- (2026-07-15) iterate — an allowlist keyed to a SHAPE (`DERIVED_MDS` = `.md` only) silently omits same-role members of another shape: `.shipwright/compliance/ci-security.json` (a derived compliance snapshot from the SAME `_update_compliance` producer) sat outside `CHURN_ALLOWLIST`, so `classify()` marked it BLOCKING and aborted iterate merges. Admit derived snapshots by explicit path, and keep the churn sets in parity (classify-allowlist ⟷ regenerate ⟷ rollback-restore) so one member is handled the same everywhere. → iterate-2026-07-15-churn-allowlist-ci-security
- (2026-07-15) test — a test that drives a Shipwright runner (e.g. `performance_check`) with `--cwd` pointed at a TRACKED fixtures dir leaks the runner's project-root-relative artifact writes (`.shipwright/triage.jsonl` on any failed sub-check) into version control, so every serial `pytest tests/` dirties the tree. Point `--cwd` at `tmp_path` (throwaway); a fixture-teardown clean of the tracked dir is racy under xdist and leaks on crash. → iterate-2026-07-15-perf-test-triage-leak
- (2026-07-14) iterate — a drift gate is only real once a **negative control** makes it fail: reintroduce the exact bug and watch it go red. A "disclaimer-nearby" heuristic (per-line, then context-window) passed a planted regression twice here; the fix was a BINARY whole-file ban of the forbidden token (rationale lives once in a lib module, not each prompt). Scope the ban to the whole FILE, not a heading-section — a rename slips a section past the scan. → iterate-2026-07-14-phase-invocation-mode
- (2026-07-14) iterate — when a runner classifies a child process by exit code, remember the WRAPPER's rc is not the tool's: `uv run` returns 1 both for a pytest test-failure and for its own env-build failure. Do not sniff prose to tell them apart (pytest pluralises `error`→`errors`, so a fixture-level race that ERRORs every test is unmatched by `error\b` and misclassifies → false STOP). Prove the inner tool ran with an artifact it emits (a `--junit-xml` file), not a regex. → iterate-2026-07-14-f0-parallel-suite
- (2026-07-14) iterate — parallelising a gate can move a verdict in BOTH directions: a serial-only-red test can go green under concurrency (false GREEN, a weaker gate), not just green→red. A serial re-verify only closes the false-STOP direction; bound the false-GREEN direction with an independent serial run (keep CI serial) and GUARD that invariant (a test asserting CI has no `-n`/xdist), because prose is not a gate. → iterate-2026-07-14-f0-parallel-suite
- (2026-07-14) iterate — a "baseline" a gate compares against must live where the PR **cannot rewrite it** (read it from `origin/main` via `git show`/`ls-tree`), or it enforces nothing: a pin checked into the branch can be edited to match the very change it should have caught, which empties the diff and greens the gate. Corollary: `"\x1f".isspace()` is **True** in Python, so `str.strip()` eats a 0x1F record separator — match the sentinel *before* stripping. → iterate-2026-07-14-webui-render-contract
- (2026-07-11) iterate — a gitignore rule for a new `.shipwright/…` artifact must be added in TWO places: the framework `.gitignore` managed block AND the SSoT `shared/templates/shipwright-gitignore.template` (adopt/project propagate the template to consuming repos). `test_gitignore_template_congruent` fails-closed on divergence — F0 catches it, so mirror the rule into both up front. → iterate-2026-07-11-iterate-phase-timing
- (2026-07-10) iterate — a fresh iterate worktree's `uv sync` installs NO dev extras: `uv run pytest` fails "program not found" until `uv sync --extra dev` (root AND per-plugin). And: an informational note (e.g. an env-override "skipped" notice) must never travel a violations/findings channel — the consumer counts entries as failures; emit it beside the SUCCESS message instead. → iterate-2026-07-10-claude-md-invariant-index
- (2026-07-08) iterate — gpt-5.x rejects `max_tokens` on the direct OpenAI Chat Completions API; use `max_completion_tokens` (OpenRouter still normalizes `max_tokens`, so that path is left unchanged). And: a review/scanner gate that returns a success-look-alike on TOTAL failure is a silent no-op — make degradation distinguishable (non-zero exit + `degraded` flag), never hardcode `success:true`. → iterate-2026-07-08-ss6-external-review-fix
- (2026-07-07) iterate — when a DEFAULT triggers an outbound call, gate it on the ACTUAL destination host, not a broad "is it remote / does the host contain 'github'" heuristic. The grade network-on default keyed on `input_kind=='url'` alone would fire `gh repo view o/r` against **github.com** for a GitHub Enterprise clone (`github.mycorp.com`) — leaking the slug to a host the clone never contacted. Fix: a strict `is_github_com_remote` predicate; the broad `owner_repo_from_remote` stays only for the explicit opt-in path. → iterate-2026-07-07-grade-public-url-network
- (2026-07-07) iterate — an ADDITIVE measurability flag on a value contract that is REPLAYED from stored serialized instances (record/replay fixtures via `Type(**stored_dict)`) MUST default to the SCORED/measurable state, letting only the new opt-out path set it False. A "not measurable" default silently flips every pre-existing stored replay (which lacks the field) to n/a and changes its output — here it would have forced re-recording 7 network fixtures. Verify the round-trip preserves prior outputs BEFORE choosing the default. → iterate-2026-07-07-grade-local-honesty
- (2026-07-04) iterate — to make a secondary signal MODERATE a composite score as a WARN (not a gate), multiply then floor at the collapse threshold: `max(score*factor, min(score, floor))` never crosses a passing pillar into the F-collapse band and never deepens an already-collapsed one; IMPORT the floor from the gate module (don't duplicate the literal) so the WARN-vs-cap boundary can't silently drift. → iterate-2026-07-04-diff-coverage-grade-input-warn
- (2026-07-04) build — To make a lead-magnet readable by non-experts without forking a SHARED engine, add the audience copy at the RENDER seam (a dict keyed by id — engine untouched, so consumers sharing it don't drift). Keep spec-required-but-jargony fields (anchors, provenance) SURFACED inside a `<details>` expandable rather than dropping them (spec + transparency, off the face). PIN the honest hedges + an "every id has copy" fallback with tests — over-claiming in a marketing artifact gets debunked. → iterate-2026-07-04-grade-report-audience-copy
- (2026-07-04) build — A security-critical escape-only HTML renderer CAN carry an outbound link without weakening its "no untrusted string becomes a URL" invariant: use a single HARDCODED/allowlisted href (never model-derived), harden with `rel="noopener noreferrer"`+`target="_blank"`, run attribute values through the SAME control-strip+escape seam as text nodes, and pin it with a parser-based test asserting exactly one `<a>` whose href == the allowlisted URL (a user-click navigation is not an auto-fetch → strict CSP stays `default-src 'none'`). → iterate-2026-07-04-grade-cta-adopt
- (2026-07-04) build — Rendering untrusted strings into HTML: prefer an escape-by-default element builder (plain children escaped as text nodes; raw HTML only via an explicit `_Raw` marker) over per-field `html.escape` calls — it makes the trusted/untrusted boundary ONE auditable seam a forgotten wrap can't bypass, and pairs with a parser-based inertness test (assert the hostile payload contributed zero live DOM nodes, not merely an absent substring). → iterate-2026-07-04-grade-g3-html-report

- (2026-07-04) iterate — the Tier-3 pr_review gate truncates + fails-closed because it sends the FULL gh-pr-diff, ~82% of which is producer-REGENERATED artifacts (compliance MDs, agent-docs, lockfiles, append-log state) with no reviewable logic. Fix at the INPUT (drop generated file-sections before the truncation check via `pr_review_diff_filter.filter_generated_paths`), NOT by bumping `MAX_DIFF_CHARS`; disclose the excluded list in PR meta + comment. The fail-closed-on-truncation itself is correct — never weaken it. → iterate-2026-07-04-pr-review-exclude-generated-diff
- (2026-07-03) iterate — diff/patch coverage is PR-local: measure it into a gitignored transient (`.shipwright/coverage/diff_coverage.json`) + a grade-neutral dashboard INFO line; NEVER commit `coverage.diff` to a tracked file (stale on the default branch) and NEVER populate `coverage.total` on a single tier (it silently arms the W4 FAIL gate — that is a gate, not "informational"). Make the CI-gate guard diff-cover-aware (add to `GATE_COMMANDS`) so the non-gating step is allowlisted, not invisible. → iterate-2026-07-03-diff-coverage-measure-one-tier
- (2026-07-03) grade-G1 — Python `str.splitlines()` also breaks on `\x1e`/`\x1f` (ASCII record/unit separators), so parsing custom `\x1e`-delimited `git log` output with `.splitlines()` silently swallows the record marker — use `str.split("\n")`. Cross-plugin reuse of the compliance grade engine stays ADR-045-safe by importing the grader's own modules BARE and letting only the compliance root bind `scripts.lib`. → iterate-2026-07-03-grade-g1-projector
- (2026-07-03) iterate — semgrep redacts snippet fields (extra.lines) to "requires login" when run UNAUTHENTICATED (as CI does), so owner-scoped filtering of registry findings must read the source FILE at path:line, not extra.lines. Producer accepted-risk rule suppression is opt-in/default-off (SHIPWRIGHT_SEMGREP_EXCLUDE_RULES exact-id; ..._ACCEPT_GH_OWNED_ACTION_TAGS owner-scoped, third-party still flagged) so adopted repos are unaffected; owner-detection fails safe when unreadable. → iterate-2026-07-03-semgrep-noise-tailoring
- (2026-06-30) iterate/bug — SARIF suppression metadata lives ONLY on the raw result; `security_findings._findings_from_sarif` normalizes to `{severity}` and drops it, so a suppressed-finding fix MUST go at the parser, not the downstream `github_triage` producer (a producer-side `suppressions` filter is a no-op). Drop results whose `suppressions[]` has any non-`rejected` entry → a fully-suppressed scan yields `[]` (clean), so the auto-resolve gate fires. → iterate-2026-06-30-sarif-suppression-filter
- (2026-06-30) iterate — a composite control/quality score must gate its HEADLINE on the weakest load-bearing pillar (declining / dark-but-expected / collapsed), not just average — else a green "A" masks a decayed control (Goodhart). Keep a TREND penalty (a decline depresses a dimension, capped) SEPARATE from a COLLAPSE cap (F-band hard cap): never let the penalty feed the cap, or a mere decline false-F's any repo whose pre-penalty pillar sits just above the F line (caught only by an adversarial review, not the green test suite). → iterate-2026-06-30-control-grade-honesty
- (2026-06-29) iterate — Group-H H2's `detail` caps the displayed ratchet-suggestion list at 5, but `evidence[]` holds them ALL; re-scan the WHOLE baseline (`_bb._file_newlines` per entry) and tighten EVERY entry where actual < current, else H2 re-fires next audit with the undisplayed remainder. Edit byte-preserving (regex the `current` int, keep CRLF) — a `json.dumps` rewrite flips LF and churns all entries. → iterate-2026-06-29-tighten-bloat-baseline
- (2026-06-29) iterate — a compliance/artifact producer that writes a file which is gitignored in the monorepo but TRACKED in some adopted repos (e.g. webui tracks `audit-report.md`) must key its content to STABLE inputs; a per-call timestamp re-churns the tracked file on every routine regen (the FF-blocking drift class). Verify the no-op path (`new_text == text`) holds across repeat runs. → iterate-2026-06-29-compliance-exempt-not-deficit
- (2026-06-28) build — the compliance SBOM doc (`sbom_generator.generate`) is ASCII-only: `test_doc_is_ascii_even_with_fall2_deps` does `.encode("ascii")`, so the spec example artifacts' emoji/middle-dot must NOT appear in the real generator (use plain ASCII `-`/words). Also resolve a generated artifact's fields with SYMMETRIC robustness — version (uv.lock per-manifest + global-union fallback) AND license (all-venv scan) must both tolerate one stale/missing source, else dedup-by-installed-version re-breaks. → iterate-2026-06-28-sbom-honesty
- (2026-06-28) iterate — filesystem mtime is NOT a content-staleness signal in a git repo: checkout, branch-switch, worktree-creation, and release version-bumps all reset mtimes, so an mtime-vs-CLAUDE.md drift check fires on pure noise (worst under the worktree-per-iterate model). Drift/freshness detectors must compare CONTENT (or commit-time) — never mtime. Removed check_drift.py's timestamp detector; the content-drift detector (Structure block + npm-run vs filesystem) is the deterministic replacement. → iterate-2026-06-28-drop-timestamp-drift

- (2026-06-28) iterate — a lazy import can be load-bearing for MORE than the reason its comment states: `events_log.resolve_main_repo_root` must stay a call-time `from lib.repo_root import` even after #281 removed its import cycle, because Group-F loads events_log via `load_shared_lib` (importlib-from-file) to keep `lib` OUT of `sys.modules` (ADR-044) — a module-level import would pollute `sys.modules['lib']` mid-exec. Before hoisting a lazy `from lib.X`, check who `load_shared_lib`s it. → iterate-2026-06-28-events-log-lazy-rationale
- (2026-06-28) iterate — CodeQL `py/cyclic-import` counts function-level/lazy imports in its STATIC graph, so making an import lazy does NOT clear it (a `# noqa: PLC0415` lazy import was still flagged). Break a cycle by extracting the shared symbol the back-edge needs into a dependency-free LEAF module both ends import (re-export from the old home so callers are unchanged). Same shape clears `py/mixed-returns`: end the function in an explicit `return`/`raise`, never a `pytest.skip`/`fail` fall-through. → iterate-2026-06-28-codeql-import-cycles
- (2026-06-28) iterate — CodeQL scanned a bundled JS test fixture → ~1000 `js/assignment-to-constant` alerts; the durable fix is a config `paths-ignore` for test fixtures (mirrors ruff `extend-exclude`), NOT per-alert dismissal (those reappear every scan). Clear an intentional `py/implicit-string-concatenation-in-list` by making it explicit `+` (ruff selects no ISC rule, so no gate conflict); prove the refactor byte-identical by comparing compiled `co_consts` before/after (the compiler folds both implicit and `+` literal concat). → iterate-2026-06-28-codeql-fixture-noise

- (2026-06-28) iterate — a PreToolUse `Bash` gate runs on EVERY bash call, so its invocation must be robust and its body fail-OPEN, else a flaky/crashing check hard-blocks unrelated work. Two layers: `uv run --no-project` (skip the per-call project sync whose intermittent failure fail-closed as 'No stderr output') + route via `lib/hook_failopen.run_failopen` (exception → log `runtime/hook_errors.log`, return 0 ALLOW). The deliberate `return 2` soft-block survives (value, not exception). → iterate-2026-06-28-security-scan-hook-failopen
- (2026-06-20) iterate — a claim-before-work fan-out guard on a FALLIBLE producer MUST release the claim when the work fails (exception OR non-zero return code), else the failed winner starves every sibling of the once-per-event work for the whole TTL window. Check the producer's rc, not just exceptions; release-then-retry is only safe when non-zero paths are pre-write (no partial output). Lived: aggregate_triage_on_stop's triage_inbox.md regen. → iterate-2026-06-20-aggregate-triage-stop-fanout-dedup
- (2026-06-20) iterate — a hook's INVISIBLE path (empty pass / no-op side effect) can mask N× duplication on its VISIBLE path: the bloat-gate Stop block re-emitted 12× per event yet was filed "convergent" because its pass path is empty stdout. When auditing fan-out dedup, classify EVERY emit/side-effect path independently — convergence on the common case ≠ convergence on the blocking case. Put the once-per-event claim on the emitting edge, AFTER the no-op guards, so quiet invocations never consume it. → iterate-2026-06-20-bloat-gate-stop-fanout-dedup
- (2026-06-16) iterate — compliance Type-column producers read the raw event `intent`, which can carry a leaked free-text description (real class lives in `change_type`) or an adopted repo's git conventional-commit type; normalize at the READER (`shared/scripts/event_classification.normalize_intent`), don't trust the field. A merged-work `passed<total` gap is SKIPPED tests not failures ({passed,total} can't distinguish), so render skip-aware PASS/COVERED, never a gap-driven FAIL — real regressions surface via the open-triage override. → iterate-2026-06-16-compliance-rendering-fixes
- (2026-06-15) iterate — a derived cache consumed at a different lifecycle stage than it's written (SessionStart reads the on-Stop-only `_findings.md` digest) must re-apply the producer's canonical filter at the CONSUMER, not trust cache freshness — esp. for a user-facing signal (a stale digest resurfaced filtered-out sentinel-run FAILs = cry-wolf). Apply count caps AFTER the filter so dropped items can't starve real ones. → iterate-2026-06-15-sessionstart-sentinel-filter
- (2026-06-14) iterate — a prose-only "one-line entry" rule gets ignored unless it's a GATE that runs in every repo: the agent-doc budget was monorepo-pytest-only AND had a date-regex hole (run-id-slug dates parsed as undated → exempt). Fix = shared SSoT (`lib.agent_doc_budget`) + F11 verifier check (ships to adopted repos) + a paren-enclosed-date parser. → iterate-2026-06-14-agent-doc-entry-budget-gate
- (2026-06-14) iterate — a non-greedy `^- \*\*(.+?)\*\*` de-bold regex mangles any entry whose rule text holds an inner `**` (e.g. `client/**` in backticks) — it matches the inner marker as the closing one. Grep the section for stray `**` after a bulk de-bold. → iterate-2026-06-14-agent-doc-entry-budget-gate
- (2026-06-13) iterate — a plugin-local file importing a NEW shared helper must use the helper's UNIQUE top-level name (`from atomic_write import …`), NOT `from lib.atomic_write …`: a plugin that puts its own `scripts/lib` on sys.path poisons the `lib` package name (ADR-044), so `lib.<mod>` ModuleNotFounds under full-suite collection order (green in isolation). Shared-tree files keep `from lib.<mod>`. → iterate-2026-06-13-atomic-write-fsync-durability
- A "read the config to grab one flag" call can be a hidden WRITER: `load_run_config` implicitly runs the legacy-pipeline migration, which persists via `save_run_config` — so reading `standalone` BEFORE acquiring `run_config_lock` performed that migration write UNLOCKED (residual WP2/F11 window, legacy-only/idempotent/atomic). Add a `migrate=False` raw read for migration-invariant fields (`standalone` lives outside `pipeline`/`phase_tasks`); the migration's write then happens on the next in-lock load. (iterate-2026-06-13-runconfig-standalone-read)
- A Claude Code hook must deliver its reason on the channel the EVENT reads, which differs per event: PostToolUse exit-2 → STDERR (stdout is DISCARDED on exit 2); SessionStart → STDOUT `additionalContext` at exit 0, and SessionStart CANNOT block a session. The two security guards emitted JSON on stdout+exit 2 (lost) and the drift gate claimed an exit-1 'hard-gate' SessionStart can't perform (inert). Pin the channel per event with a test that subprocesses the REAL registered hook. (iterate-2026-06-13-hook-block-channel)
- (2026-06-13) iterate — OS advisory file-locks coordinate by lock-file PATH, not shared code: two lock impls mutually exclude iff they target the same `*.lock` path with the same `flock`/`msvcrt` primitive, and on Windows the lock file must stay 0 bytes (`msvcrt.locking` locks at the current offset, so `seek(0)` first). → iterate-2026-06-13-runconfig-atomic-writes
- A hook/CLI whose only real logic lives in `main()` (stdin/env parse, arg wiring) MUST be tested THROUGH `main()` at least once, not only via its pure `run()` core: the phase-session integration suite drove `run()` exclusively, so F1 — `main()` resolved identity from env vars NO launcher sets → the whole v2 lifecycle no-op'd — passed every test undetected. Drive `main()` with a realistic stdin payload + cleared env. (iterate-2026-06-10-phase-hook-lifecycle)
- Relocating a shared symbol to a new home + back-compat re-export: migrate only consumers whose import edit stays net-zero/≤limit; a grandfathered (>300 LOC) consumer must STAY on the re-export — adding a `from <new-home> import …` line ratchets its bloat past baseline (a hard CI gate), and growing other oversized files to chase import purity inverts a bloat-reduction iterate. A lazy (call-time) re-export breaks the home→consumer→home import cycle a module-level one would create. (iterate-2026-06-12-repo-root-resolver-relocate)
- A `from_dict` deserializer must coerce explicit-null via `or []` / `or {}`, NOT `d.get(key, default)` — the default only fires on an ABSENT key, so an event carrying `affected_frs: null` yields None and bricks every downstream consumer (here it crashed the whole compliance MD regen). 2nd occurrence in `collectors/_types.py` after `TestRunEvent.layers`; treat `or <empty>` as the house rule for nullable JSON list/dict fields. (iterate-2026-06-12-workevent-null-frs-coerce)
- The empirical gate enforced depth (asymptote) + breadth (coverage ledger) but not COMPOSITION (do cross-component pieces compose), so FRAMEWORK changes shipped unit-tested-but-integration-unproven (the merge cascade). New `cross_component` flag → required `category:"integration"` behavior at medium+, enforced NON-dodgeably by the F11 verifier RECOMPUTING the flag from the diff (not an agent-report). Detector SSoT in `classify_complexity`, drift-pinned copy in the verifier. (iterate-2026-06-12-cross-component-gate)
- Probe for an executable by TEST-RUNNING it (e.g. `"$c" --version >/dev/null 2>&1`), not `command -v` alone: on Windows `python3` is a Microsoft Store App-Execution-Alias stub that `command -v` finds on PATH but that exits 49 on invocation, so a `command -v`-only resolver under `set -euo pipefail` picks it and aborts at the first real call (`$(python3 -c …)`). `command -v` proves the name resolves, not that it runs. (iterate-2026-06-12-marketplace-python3-stub-probe)
- "Delivered" = the PR is actually MERGED with all Required Checks GREEN, confirmed by watching it to terminal — arming auto-merge (or pushing) and walking away is "shoot and forget"; a check can fail afterward and sit BLOCKED, red, un-merged. F11 runs `watch_pr_delivery.py` and fails closed on anything but merged. Corollary: the `shared/tests` F0 run misses iterate-plugin content-lints (the agent-doc 600-char budget gate), so re-run them after F2/F3a before push. (iterate-2026-06-12-delivery-watch)
- `merge=union` extends past the JSONL append-logs to the curated agent-docs whose `## …Updates`/`## Learnings` sections are bullet-prepended by parallel iterates: a DISTINCT `CURATED_DOC_UNION_PATHS` category keeps both bullets (server-side too), closing the prose half of the cascade the regenerate resolver can't touch; accepted caveat — union is silent on a same-line non-append edit. (iterate-2026-06-12-union-curated-agent-docs)
- A whole-set checker over shared, gitignored, cross-branch-accumulating state must scope to per-tree ownership (run_id in the committed `shipwright_events.jsonl`), and "fail-open when the ownership ledger is absent" is what keeps it from breaking hermetic tests + CI; reproduction needs a tree whose *tracked* state trails the *accumulated* gitignored state, not a clean fork. (iterate-2026-06-12-arch-drift-test-scope)
- A projection over a human-authored markdown skeleton must tolerate inline emphasis, and "verify X regenerates without downgrade" is not a no-op AC. → archive
- The `check_security_scan` PreToolUse:Bash deploy-gate substring-matches its trigger words inside *argument prose*, not just argv[0]. (iterate-2026-06-10-status-projection) → archive
- Tune heuristic classifiers against a harvested real-input corpus, not synthetic guards. (iterate-2026-06-10-complexity-classifier-prior) → archive
- A module loaded by absolute file path (`importlib.util`) that defines a `@dataclass` under `from __future__ import annotations` must be registered in `sys.modules` BEFORE `exec_module`. (iterate-2026-06-07-scaffold-churn-merge-machinery) → archive
- A silently-uncovered test directory hides real, pre-existing failures. (iterate-2026-05-31-ci-gate-guard) → archive
- Always quote `uv run <placeholder>` path arguments in shell snippets. (ADR-020) → archive
- A new `.github/workflows/*.yml` must be Semgrep-clean to merge ITSELF. (2026-06-11, iterate automerge-pr-review / B4.5 Phase 2) → archive
- Hook-installer-style code that detects "already-present" must also upgrade legacy forms in place, not just refuse to add a duplicate. (ADR-019) → archive
- Drift-protection tests across two SSoTs use AST + source-position sort, not substring grep. (iterate-2026-05-03-adopt-env-local-scaffold) → archive
- `.env.local` is the single secrets surface; `.gitignore` enforcement is a hard-stop. (ADR-021) → archive
- Producer/consumer round-trip is the only test that catches format drift. (ADR-024) → archive
- Default permissive on missing-marker guardrails. (ADR-026) → archive
- Idempotent writes preserve audit-trail fields. (ADR-026) → archive
- "Are you confident?" is unfalsifiable; the asymptote heuristic replaces it. (ADR-025) → archive
- `${CLAUDE_PLUGIN_ROOT}` is plugin-context-only. (ADR-030) → archive
- Subprocess tests on Windows must forward `SystemDrive`/`LOCALAPPDATA`/`APPDATA` alongside `SystemRoot`/`USERPROFILE`/`HOME`. → archive
- Pre-push test gates that depend on the marketplace cache must skip pre-push, not fail. (ADR-030) → archive
- Markdown is also a producer/consumer boundary. (ADR-024) → archive
- End-to-End Verification: spec-only authorship counts as no test; the Backend-affects-Frontend rule forces `surface = web` when a diff touches API / store / SSE / WS / message contracts even with no `client/**` file changed. → archive
- Marker scanners need comment-context recognition, not "match anywhere on line". (ADR-041) → archive
- Claude Code hook schemas are per-event, not uniform. (ADR-042) → archive
- Orphan-file detection requires consumer-grep, not git history. (ADR-043) → archive
- PyYAML loses comments — drift tests on workflow files must require ABSENT, not "commented or absent". (iterate-2026-05-10-adopt-ci-scaffolders) → archive
- F0.5 `surface_verification.py --surface cli --runner "..."` execs WITHOUT a shell — the runner must be a single executable, not a compound `cd … && …`. (iterate-2026-06-07-campaign-expands-triage) → archive
- `test_architecture_md_reflects_arch_impact.py::test_arch_impact_drops_found_at_all` is born-fragile against the post-release main-tree state. (iterate-2026-06-07-finalization-tooling-hardening) → archive
- Wiring a previously-CI-less test dir into CI: simulate a clean checkout, don't trust the dev tree. (iterate-2026-05-31-ci-shared-tests) → archive
- Pytest `conftest.py` name collision across plugin dirs. (iterate-2026-05-10-adopt-ci-scaffolders) → archive
- Silent `pytest.skip` on missing binary/import paths must hard-fail in CI. (ADR-044) → archive
- Vestigial `|| true` from a dormant-CI era silently disables gating; sweep for it when hardening. (iterate-2026-05-31-ci-gate-f821) → archive
- Producer auto-resolve reason tokens and `triage_gc.MACHINE_REASONS` are a decoupled SSoT pair — a new producer reason silently escapes the dismissed-pile GC. (iterate-2026-06-07-triage-gc-compliance-refreshed) → archive
- F0.5 `surface_verification.py` runs `--runner` via `shlex.split` + `subprocess.run(shell=False)` — no shell builtins. (iterate-2026-06-03-campaign-status-field) → archive
- A regression guard only counts if it lives in a directory CI actually runs. (iterate-2026-05-31-ci-gate-f821) → archive
- Registry → disk mappings need BOTH directions of drift protection. (iterate-2026-05-11-test-hygiene-and-skill-rules) → archive
- F0.5 cli-surface `--runner` commands must pass `--color=no`. (iterate-2026-05-15-rtm-adopt-worktree-fix) → archive
- Test-Update-Klausel: when an iterate changes test infrastructure (skip semantics, hygiene rules, conventions), the iterate skill's reference rules MUST be updated in the same diff. (ADR-021) → archive
- `pytest.fail`-raising helpers should be annotated `NoReturn`. (ADR-044) → archive
- OneDrive-synced repos + uv hardlink mode = hook subprocess hell. (iterate-2026-05-11-test-hygiene-and-skill-rules) → archive
- Cross-cutting Python helpers go under `shared/scripts/<name>.py`, NOT `shared/scripts/lib/<name>.py`. (iterate-2026-05-11-test-hygiene-helper-and-self-review-wiring) → archive
- AST + tokenize hybrid for source-pattern detection that respects comments. → archive
- Static-probe scope topology, not line geometry. (ADR-045) → archive
- Pre-backlog buffer keeps the WebUI task list curated. (ADR-046) → archive
- Concurrency-correct idempotent append: dedup-scan + write under the same lock. (ADR-046) → archive
- Cross-session dedup needs an explicit "no window" mode. (iterate-2026-05-11-triage-inbox-1a) → archive
- Gitignored test fixtures are absent in fresh worktrees/clones. (iterate-2026-05-16-fix-adopt-review-config) → archive
- A triage `source` is not always a unique producer identity — an auto-resolve pass must scope by the dedup-key shape it owns. (iterate-2026-05-16-fix-triage-dedup-resolve) → archive
- A load-bearing "always" step enforced only by prose gets skipped — convert it to a gate. (iterate-2026-05-16-spec-impact-gate) → archive
- Bash hooks must resolve a Python interpreter — never hardcode `python3`. (iterate-2026-05-18-fix-launch-blocker-tests) → archive
- Test fixtures must not use bare migrated-path words as content markers. (iterate-2026-05-18-phase-quality-check-fixes) → archive
- `check_secrets.sh` flags any `secret`/`password`/`token`-named variable assigned a string literal — test files are NOT exempt. (iterate-2026-05-19-github-triage-importer) → archive
- A Learnings entry that quotes a legacy-path pattern trips the path-canon lint it describes — and F0 runs before F3a, so the trip lands undetected. (iterate-2026-05-19-github-triage-importer) → archive
- An auto-resolving triage producer MUST distinguish a failed fetch from an empty one. (iterate-2026-05-19-github-triage-importer) → archive
- Triage producers importing from systems that already have a per-finding store MUST emit action-units, not finding-mirrors. (iterate-2026-05-20-triage-launch-surface) → archive
- Schema-migration sweeps in fail-soft producers MUST be per-original-source-gated. (iterate-2026-05-20-triage-launch-surface) → archive
- Launch-surface CLI parity contract: the GUI is a thin wrapper over the library, not a parallel implementation. (iterate-2026-05-20-triage-launch-surface) → archive **[RETRACTED 2026-07-27 — it IS a parallel implementation in `shipwright-webui`; see the Learnings entry of iterate-2026-07-27-triage-defer-review-followup]**
- Repo-identity resolution for triage producers MUST be local-first (git remote), never via `gh api`. (iterate-2026-05-20-triage-launch-surface) → archive
- Symmetric emit + resolve gates: a producer that emits an action-unit only when N upstream feeds succeeded must ALSO scope auto-resolve to require the same N feeds. (iterate-2026-05-20-triage-launch-surface) → archive
- Render-banner timestamps are a producer/consumer boundary — derive from input data, never from wall-clock. (iterate-2026-05-22-deterministic-render-timestamps) → archive
- Run the external LLM plan review against the *iterate spec + mini-plan*, not only against the diff — the plan is where structural bugs are cheapest to catch. (iterate-2026-05-21-security-artifact-producer) → archive
- Aggregate counters in producer JSON are untrusted — derive from the list. (iterate-2026-05-21-security-artifact-producer) → archive
- Consumer-side synthesis is a valid fallback when canonical producers are uninstrumented. (iterate-2026-05-21-empirical-followups) → archive
- Path-canon lint must allowlist regenerated artifacts that contain plugin-source-tree paths. (iterate-2026-05-21-empirical-followups) → archive
- Artifact contents are untrusted CI output — render aggregated counts only, never raw scanner strings. (iterate-2026-05-21-security-artifact-producer) → archive
- Tracked-but-derived artifacts need a single producer + snapshot-provenance audit, NOT live-render byte-compare. (iterate-2026-05-23-compliance-md-single-producer) → archive
- Avoid `sys.path.insert(0, ".../shared/scripts")` in plugin code — use `audit_adapters.load_shared_lib(name)` instead. (iterate-2026-05-23-compliance-md-single-producer) → archive
- Markdown table rows are also a producer/consumer boundary — but the consumer is the human eye / GFM viewer, not a shipwright parser. (iterate-2026-05-20-escape-md-cells) → archive
- `importlib.metadata` is disqualifying for compliance tooling — its `sys.path` lookup is ambient process state. (iterate-2026-05-23-sbom-resolver-pin-lockfile) → archive
- Filesystem glob enumeration of `*-VERSION.dist-info` directories must sort by parsed version, NOT lexicographic. (iterate-2026-05-23-sbom-resolver-pin-lockfile) → archive
- Skip-dir patterns for source walkers must distinguish "tool output at root" from "directory name nested under source". (iterate-2026-05-25-bloat-foundation) → archive
- Hook-budget vs message-content tension: long-form error messages are bytes-on-disk that count toward LOC. (iterate-2026-05-25-bloat-foundation) → archive
- Self-eating-dogfood signal: the new gate firing on its own author is a smoke test, not a failure. → archive
- Runtime/snapshot split generalises the single-producer pattern when the artifact carries iterate-specific context. (iterate-2026-05-27-tracked-artifacts-single-producer-and-finalize-sandbox) → archive
- A finalize sandbox-escape via stale session→worktree pointer is the second-most-common single-producer failure mode after "two producers." → archive
- Idle-main producers must resolve `main_repo_root` (never `Path.cwd()`) AND write transient state only to gitignored paths. (iterate-2026-06-09-idle-main-artifact-hygiene) → archive
- Branch integration: `git merge`, not `git rebase`, for Run-ID-bearing branches. → archive
- Audit-coverage extension to docs added AFTER the last snapshot needs a "not-in-snapshot" semantic, not a stale verdict. → archive
- Splitting an allowlisted monolith into a package MUST migrate its `artifact_migrations.py` ALLOWLIST entry to a package glob in the same diff. (iterate-2026-05-29-fix-path-canon-allowlist) → archive
- A "worktree-aware" abstraction built for one failure mode can become the bug for a later workflow — confirm the full blast radius before flipping it. (iterate-2026-05-29-events-jsonl-worktree-commit) → archive
- An SHA assigned by a commit cannot be patched back into a file that same commit just committed without re-dirtying it — drop the back-patch, don't add a second commit. (iterate-2026-05-29-events-jsonl-worktree-commit) → archive
- A patch that "rides inline" with a campaign silently vanishes unless the campaign's sub-iterate plans explicitly name it. (iterate-2026-05-29-sp3-os2-reintegration) → archive
- (2026-06-12) iterate — When making a routing oracle impact-aware, read ALL candidate target docs unconditionally; checking only an impact's primary target silently breaks a cross-target legacy fallback (the convention→architecture.md fallback false-failed until both docs were loaded). → iterate-2026-06-12-agent-doc-entry-rules.
- When compacting an always-loaded doc to one-line pointers, an entry pinned by a drift-protection test is load-bearing — keep its pinned phrases inline, don't archive them away. Blanket bolded-lead compression dropped the E2E learning's "Backend-affects-Frontend" sub-rule that `test_skill_e2e_gate_consistency.py` requires in conventions.md. Grep every test that reads the doc for asserted phrases BEFORE compressing. → iterate-2026-06-12-compress-agent-doc-backlog.
- GitHub auto-merge runs a server-side 3-way merge and CANNOT run the regenerate-at-merge resolver — safe ONLY for a single iterate whose branch is current. Parallel/concurrent iterate PRs that each commit regenerated snapshots cascade `DIRTY` (snapshot conflict → auto-merge stalls) or merge stale (Group-E); bring a branch current THROUGH `integrate_main` (the `ensure_current.py` refresh-if-behind guard) BEFORE it merges. (iterate-2026-06-12-automerge-serial-integrate) → decision_log (Run-ID).
- An iterate's base is the worktree (clean `origin/main`), not the dirty main working tree — re-baseline scouting there before editing. A task-named `drift_anchor.py` target was only uncommitted main-tree WIP, absent from `origin/main`; write the multi-site drift-guard test first — its collection error exposes phantom targets. (iterate-2026-06-12-canonical-project-predicate) → decision_log (Run-ID).
- (2026-06-13) iterate — doc-drift findings from an older audit must be re-verified against current HEAD before fixing: intervening merges (audit waves, the guide.md correctness PRs) shift the shipped reality the doc must match; reconcile to code, not the audit's snapshot. → iterate-2026-06-13-docs-ssot-reconcile.
- (2026-06-13) iterate — a behavior-snapshot/verify gate only catches drift the test suite COVERS: a probe mutating an un-covered path passes green, so the gate must hard-reject removed coverage and pair with non-mechanical reasoning (Chesterton-Fence + Five Principles), never stand alone. Also: a synthetic-project CLI test must set `PYTHONDONTWRITEBYTECODE` or a same-second source rewrite reuses a stale `.pyc` and masks the drift. → iterate-2026-06-13-code-simplify-skill.
- (2026-06-13) iterate — adding code to a shared lib already AT/near 300 LOC trips the new-crossing bloat Stop-gate mid-edit (its own writer fires); trim or split in the SAME iterate. Don't baseline a file that's only AT 300 (not over) — the baseline grandfathers OVER-limit files, so 300==limit needs a real reduction, not an allowlist entry. → iterate-2026-06-13-bloat-marker-writer-baseline.
- (2026-06-13) iterate — `integration-tests/` INHERITS the root `addopts = -m 'not slow'`, and CI's integration step (`pytest integration-tests/ -v`, no `-m`) inherits it too — so a subprocess-spawning integration test marked `@pytest.mark.slow` is SILENTLY DESELECTED in CI (never runs). Leave such tests UNMARKED (the `test_shipwright_run_e2e.py` convention); reserve `slow` only for the genuinely-too-slow few. The per-plugin CI step has no `-m` filter, so a plugin-dir slow test DOES run there — the rule is dir-specific. → iterate-2026-06-13-unify-simplify-reducibility.
- (2026-06-14) iterate — an autonomous-loop phase-quality Tier-1 FAIL can be a STALE degenerate-context artifact: a `run_id=unknown` snapshot (audit with no resolvable run/session) frozen before its audit-time guard landed, re-rendered by the rollup readers. Run a fresh audit before "fixing" the symptom — here it yielded ZERO FAILs. Write-time guards don't clear on-disk pre-fix snapshots; the durable fix is read-layer (`load_actionable_findings` excludes sentinels). → iterate-2026-06-14-phasequality-sentinel-rollup-filter.
- (2026-06-17) F11/pr-review — a required merge gate must FAIL CLOSED on partial/incomplete input: pr_review.py returned EXIT_OK on a truncated (>200k) diff, so an oversized untrusted PR could bypass the gate by size. Force request-changes + EXIT_BLOCK on truncation; the red check also lets the gh-pr-ci producer surface a triage item (a green check never would). → iterate-2026-06-17-pr-review-truncation-failclosed
- (2026-06-17) F11/anti-ratchet — a self-disabling gate is a false-green: anti_ratchet_check failed OPEN on a present-but-corrupt bloat baseline, silently disabling the CI/pre-commit gate. Distinguish absent (fail-open, fresh repo) from present-but-malformed (fail-closed) at the gate boundary via is_file() — a corrupt config signals tampering, not absence. → iterate-2026-06-17-anti-ratchet-corrupt-failclosed
- (2026-07-06) F-debug/security-ingest — a shared ingest that reads repo files (e.g. workflow-action owners) for a *downloaded* artifact (SARIF) must thread an explicit base dir (project_root as `workflow_base`), NOT rely on cwd: the SessionStart import hook never chdirs to project_root, so a cwd-relative read silently fails-KEEP and the accepted-risk drop would never fire. → iterate-2026-07-06-semgrep-accept-producer
- (2026-07-07) F-build/composite-action — authoring a shared GitHub composite action: pass inputs via `env:` `$INPUT_*`, NEVER `${{ inputs.* }}` in a `run:` body (expression injection); split a space-separated path input with `read -r -a` not unquoted `$VAR`; verify the action.yml blob is LF (`git ls-files --eol` → `i/lf`) so the Linux-runner bash has no `\r`; and unit-test the `inputs→INPUT_*` env seam separately — a runtime test that sets `INPUT_*` directly can't catch a typo'd mapping. → iterate-2026-07-07-diff-coverage-composite-action
- (2026-07-07) F-build/self-consume-action — when a repo consumes its OWN composite action, use a LOCAL `./.github/actions/<name>` path, NOT `@main`: a PR editing the action is then gated by its own checkout (no stale-`@main` bootstrapping) + no mutable-action-tag finding. AND a CI-gate guard that classifies gates by `run:`-body command tokens goes BLIND when a gate becomes a body-less `uses:` step — register the action in the guard's `uses:` list (`GATE_USES`) or the reverse-drift protection silently vanishes. → iterate-2026-07-07-diff-coverage-self-consume
- (2026-07-08) F0.5/cli-surface — a CLI/library iterate with no web/API surface satisfies the E2E gate honestly via `surface_verification.py --surface cli --runner "uv run pytest <integration-test>"`: the subprocess integration test IS the driven surface (the runner executes the command with no shell and parses `N passed` from stdout; pass `--tests-run N` to bypass). Avoids a bogus `surface=none` for a backend/orchestrator change that DOES have a scripted end-to-end exercise. → iterate-2026-07-08-ss5-resumability
- (2026-07-08) test/orchestrator-integration — the `env.pop("AIKIDO_CLIENT_ID")` + "security off" comment in orchestrator CLI integration tests (SS3/multi_session boilerplate) is DEAD: since the security-decouple `build_pipeline` is unconditionally 7 steps and `securityEnabled` is hardcoded False (`aikido_client_id` only sets the diagnostic `aikidoClientIdPresent`) — don't copy it into new tests. → iterate-2026-07-08-ss7-e2e-integration-suite
- (2026-07-08) config/schema-default — when a config field has a code-level FRESH-WRITE default AND a distinct ASSUME-WHEN-ABSENT read fallback, the JSON-Schema `default` MUST equal the read fallback (absent-value), NOT the fresh-write default — else a schema-default consumer (ajv `useDefaults`, the webui) silently reinterprets a field-absent legacy config. Keep the two as separate constants (DEFAULT_RUN_MODE vs LEGACY_FALLBACK_MODE). → iterate-2026-07-08-ss8-default-single-session
- (2026-07-14) config/schema-default — corollary to the rule above: when a field's ABSENCE is meaningful, publish NO `default`. Collapsing `run_config.mode` to one value deleted the read fallback, so absence now means "not a drivable run" — which no default can express, and one would let an ajv-`useDefaults` consumer fill it in and reinterpret a mode-less legacy config as drivable. Drivability is an explicit-literal predicate in code instead (`config_io.is_single_session`), mirrored by `gate_policy` so loop and gates can't disagree. → iterate-2026-07-14-remove-multi-session
- (2026-07-20) iterate — a render byte-equality test that flakes on a wall-clock token should ISOLATE that token in the assertion, not freeze the clock: freezing masks real data drift — finalize appends a fresh grade_snapshot each run, so latest_event_dt (the minute-resolution `> Updated:` banner source) legitimately advances between two runs. Split an oversize test file into a sub-300 sibling module rather than ratchet the ADR-096 exception larger. → iterate-2026-07-20-flaky-idempotency-banner
- (2026-07-21) test-design — a pytest.skip on a shallow-clone-unreachable commit is a fail-open that reports green while verifying nothing; raise a hard AssertionError naming the remedy (`fetch-depth: 0`) instead, and DRIVE the unreachable branch in a meta-test that catches BaseException (a reintroduced skip is `Skipped`, which would otherwise skip the guard too). → iterate-2026-07-21-fr-history-skip-hardfail
- (2026-07-21) iterate/security — the perf lockfile has no npm-based coverage: no CI job runs `npm ci` on `plugins/shipwright-test/scripts/perf/`, and the perf tests fake Lighthouse via `SHIPWRIGHT_PERF_LHCI_FAKE` so they never install `node_modules` — a pytest F0.5 surface over them passes vacuously. Drive the artifact itself (`npm ci` + start the runner). Also `performance_check.py` lazy-installs via `npm install`, which will NOT upgrade an existing `node_modules` that already satisfies the range. → iterate-2026-07-21-brace-expansion-cve
- (2026-07-23) iterate/review — generating the external-review diff with `git diff … 2>&1` leaks git's CRLF `warning:` lines INTO the diff file; the external reviewer reads them as corrupted file content and reports false-positive HIGH findings (word-splits, mangled tokens). Redirect stderr elsewhere (`2>err.txt`), never merge it into the diff. → iterate-2026-07-23-req3-elicitation-module

---
- (2026-07-27) iterate/plan — when a documented promise has no oracle, the fix is usually to change the WRITING, not to add a cleverer check: `SECTION_MANIFEST` numbering was documented as the build order but dependencies were not expressible, so nothing could ever establish it. Making the dependency declarable turned an unverifiable promise into a one-line rule (every dependency is earlier), which also subsumes cycle detection. → iterate-2026-07-27-plan-section-deps
- (2026-07-27) iterate — a gate that parses LLM output must be validated against real LLM output, not reasoned about: this run's verdict rule was disproven three times by the replies it received (counting the sentinel TOKEN threw away a genuine `reject` from a reviewer quoting the instruction; matching only the LAST line let a two-verdict reply through; counting only WELL-FORMED lines skipped a malformed attempt). Survivor: exactly one purported sentinel LINE, and it must be last; unreadable blocks. → iterate-2026-07-27-plan-verdict-record
- (2026-07-27) iterate/plan — reading an id out of a longer token is a recurring defect class here, not a one-off: an unanchored `FR-\d+\.\d+` search credited `FR-01.01x` and `not-FR-02.02-example` as the live `FR-01.01`, letting a typo in the one machine-readable linkage field satisfy both coverage directions — the same shape as the `@FR-01.03x` tag-prefix bug. Split on the separator and match each token in FULL. → iterate-2026-07-27-plan-step9-gates
- (2026-07-28) iterate/review — a filter that SUPPRESSES a gate's findings must be disproved, not reviewed: this one matched the real data, passed spec and code review, and the adversarial stage still built four real reverts it cleared — one of them the gate's own motivating test, reworded. Each disproof became a permanent test; "it reproduces the bug and nothing else" is not evidence a narrowing is safe. → iterate-2026-07-28-silent-revert-false-positives

## Contributing

Contribution rules, dev setup, the graduated trust model, and high-sensitivity areas live in the root [`CONTRIBUTING.md`](../../CONTRIBUTING.md) — the single source of truth. (Previously copied here verbatim by /shipwright-adopt; replaced with this link to keep always-loaded Layer-1 context lean — iterate-2026-06-12-agent-doc-entry-rules.)

## Convention Updates
> **One line per change** — always-loaded Layer-1 context. Format: `- **<run_id|ADR-NNN>** (YYYY-MM-DD): <Impact> — <one sentence>. → decision_log (Run-ID/ADR)`. **Budget ≤ 600 chars; detail goes in the ADR, not here.** Routed here for `convention`-impact entries (`component`/`data-flow` → `architecture.md`); enforced repo-agnostically by the F11 verifier + `shared/scripts/tools/check_agent_doc_budget.py`. Bullet **shape** (anchor + `<Impact> —` lead + `→` pointer; no `Campaign`/`sub_iterate`/free-text) is enforced from 2026-06-28 by `check_agent_doc_shape` (`lib.agent_doc_shape`); the aggregator writes no duplicate `ADR-NNN` bullet. `## Learnings` (date-first grammar) is exempt. See `references/F2.md`.
- **iterate-2026-07-27-derived-snapshots-off-branch** (2026-07-27): Convention — a derived view is produced where its inputs are complete: on main after merge, never on a branch. An iterate no longer commits the eleven regenerated compliance/agent-doc snapshots, and `restore_derived_to_head` resets them BEFORE the merge (after it, `git merge` refuses on a dirty tracked path). They were not merely the sole cause of parallel-PR conflicts but wrong — a worktree derivation reads pre-squash SHAs and an event log missing every concurrent branch. → decision_log (Run-ID).
- **iterate-2026-07-27-checks-that-gate-nothing** (2026-07-27): Convention — a CI step that blocks the merge carries the `(gate)` name suffix, which `check_ci_gate_coverage.py` matches on, so a blocking step running a bespoke script is policed rather than invisible. For any config-reading producer, track readability separately from content: "requires nothing" is a finding, not an error, and a 404 means "no such policy" only once the repo itself is proven readable. → decision_log (Run-ID)
- **iterate-2026-07-27-pytest-root-composition** (2026-07-27): Convention — one test root per pytest process is ENFORCED, not just documented in CI: a repo-root `conftest.py` refuses a session spanning two roots (args at session start + a collected-items backstop) and names the per-root commands. Each root needs `scripts`/`lib`/`tools` to mean a different directory, and a regular package is cached on first import, so `sys.path` order cannot rescue the second. → decision_log (Run-ID).
- **iterate-2026-07-27-run-unit-parallel-race** (2026-07-27): Convention — never force a platform branch with `monkeypatch.setattr(os, "name", ...)`: it is process-global and `pathlib.Path()` dispatches on it, so `Path(...)` raises `NotImplementedError` before the code under test runs — green on the host, RED on the other platform's CI. Branch on one mockable predicate and patch that. And a retry budget guarding a WRITE must not share a constant with one guarding a READ: a patient reader costs latency, a patient writer can publish stale data. → decision_log (Run-ID).
- **iterate-2026-07-23-req3-phase2-content-mono** (2026-07-26): Convention — a requirement states its phase's OUTPUT; cross-cutting agent discipline (TDD, the review cascade, conventional commits, tests-green) lives in the constitution ONCE and reaches clients through each skill's First Actions; measurable how-well is a quality requirement. Restating discipline per phase drifts — the review-cascade rule was found stated three ways. A rule moved into the constitution still owes an enforcement declaration. → decision_log (Run-ID/ADR-112)
- **iterate-2026-07-21-fr0115-coverage-bloat** (2026-07-21): Convention — a change that MINTS a requirement and recorded test totals COVERS and DELIVERS it (Group-D D1 counts `new_frs` beside `affected_frs`; D3 accepts a tested mint), because the write-side FR gate has always accepted `new_frs` alone — the reader, not the record, was wrong. D3's surviving reach is recording-integrity, not proof a requirement works; per-FR evidence stays D1's manifest link proof. → decision_log (Run-ID)
- **iterate-2026-07-19-compliance-prework** (2026-07-19): Convention — a file that drifts just past the 300-line limit on coherent code is REGISTERED (`state: grandfathered`), not trimmed: cutting a line to clear a threshold games the metric and hides that it crossed. Registering also arms the anti-ratchet. And never tighten a baseline entry for a file an in-flight iterate is editing — MEASURE that worktree first (here: `verifiers/common.py` grows to 768, so it was held; `group_d` shrinks, so it was tightened). → decision_log (Run-ID).
- **iterate-2026-07-18-fr-existence-gate** (2026-07-18): Convention — a declared requirement id is verified to EXIST at event-write time, not just to be a non-empty string. Both write paths go through one entry point `run_fr_gates()` (`lib/fr_gates.py`), which holds the classification gate and the new existence gate together so a path cannot wire one and forget the other; the pure predicate `unknown_fr_ids` stays in `fr_classification` (stdlib-only by contract). An unknown id now hard-fails at write time. → decision_log (Run-ID).
- **iterate-2026-07-19-adopter-pinning-posture-rule** (2026-07-19): Convention — the action-pinning asymmetry now SHIPS to adopters as a RULE, not just a state: `claude-md-template.md` carries why (GitHub-owned on mutable tags, third-party SHA-pinned, PORTABILITY not cost, no updater config), and `test_action_pinning_posture.py` gates shipped templates BOTH ways — unpinning a third-party action OR SHA-pinning a GitHub-owned one fails. Parser fails closed on quoted/dynamic `uses:`. → decision_log (Run-ID)
- **iterate-2026-07-18-accepted-risk-alert-convergence** (2026-07-19): Convention — a `github-dismissal` acceptance resolves onto live GitHub via `accepted_risks_cli converge`, keyed `(tool, rule, path)` — never the rule alone (8 unrelated judgments share one rule id here, and an open alert shared it) nor the line (drifts). Breadth is DECLARED (`scope.paths` or `match: rule-wide`); ambiguity is refused. Dismissals carry a `[shipwright-accepted-risk: <id>]` marker and only marked alerts reopen, so human dismissals stand. Read-only unless `--apply`; never CI-wired. → decision_log (Run-ID)
- **iterate-2026-07-18-requirements-golden-corpus** (2026-07-18): Convention — a behaviour-preserving refactor gets a golden corpus FIRST, freezing today's behaviour INCLUDING its bugs (`integration-tests/requirements_corpus/`, campaign S1). Frozen-wrong cells carry a `FROZEN-BUG` rationale naming the step that flips them; regeneration is a separate script requiring `--reason`, never a pytest flag, or the refactor self-certifies. Pin outcome-kind/type/order/exception-type, not just values. Validate the harness by MUTATION — the first one survived a green suite. → decision_log (Run-ID).
- **iterate-2026-07-18-accepted-risk-register** (2026-07-18): Convention — an accepted security risk is RECORDED in repo-root `shipwright_accepted_risks.yaml` (scanner-agnostic: `target` + native `rule` + `expires` + a `rationale_ref` naming a recorded decision + `statement`) while the wiring that APPLIES it stays put; `accepted_risks_cli check` gates BOTH directions (unrecorded suppression and stale record) and `expire` fails a past-due entry, via `shared/tests/test_accepted_risks_register.py`. Suppressed != accepted: an unrecorded suppression renders as drift. → decision_log (Run-ID)
- **iterate-2026-07-18-ci-supplychain-risk-flag** (2026-07-18): Convention — the CI trust boundary (`.github/workflows/**`, dependency-updater configs, `.github/actions/**`) is a risk-flagged surface: `touches_ci_supplychain` enforces a RECORDED ACKNOWLEDGEMENT (`iterate_latest.ci_supplychain_ack`, written by `record_ci_supplychain_ack.py`, bound to run_id + a content fingerprint), not merely a review — #285 already had review. Non-dodgeable via `check_ci_supplychain_ack` at every complexity; it must never be read as "pin everything". → decision_log (Run-ID)
- **iterate-2026-07-17-step3-fr-unmapped-review** (2026-07-17): Convention — in the framework monorepo, FR-untagged framework-internal tests are the accepted steady state, not a backlog: it self-adopted to 15 coarse per-plugin FRs, so ~6.8k internal tests map to none (STEP 1 tagged the ~192 that do map). Do NOT force-tag the remainder to coarse buckets (zero drift value) nor re-file a bulk "review N unmapped tests" card; `untagged_tests` is informational — only the delta `layer_coverage_removal` tag-drop gate is hard. Revisit only if granular product FRs land. → decision_log (Run-ID).
- **iterate-2026-07-15-tsjs-skip-ban** (2026-07-16): Convention — TS/JS specs follow the silent-skip ban (G6): a `test.skip`/`it.skip`/`describe.skip`/`test.fixme`/`xit`/`xfail` needs an `@quarantine` comment block (`reason`+`owner`+`ticket`+`expires: YYYY-MM-DD`; past-expires FAILs), and a focused test (`.only`/`fit`/`fdescribe`) is an unconditional FAIL — never quarantineable. Enforced by `shared/scripts/ts_test_hygiene.py` via `scan_test_hygiene.py --diff` (Self-Review §8 / F0.5), diff-scoped by line so untouched legacy skips don't block. → decision_log (Run-ID).
- **iterate-2026-07-15-required-layers-on-frs** (2026-07-16): Convention — each FR declares a `Layers` set (`{unit,integration,e2e}`); `_requirement_parse` (→ manifest) is the SINGLE producer of `required_layers`+provenance (drift/RTM/spec parsers stay layer-agnostic, R5 — they only stop dropping rows). Adopt-inferred cells are annotated `(inferred)` so they read as advisory `inferred_legacy`, never the `explicit` hard gate — keeping adopted brownfield FRs out of instant-MISSING (Spec §9). → decision_log (Run-ID).
- **iterate-2026-07-14-f0-parallel-suite** (2026-07-14): Convention — F0 runs the discovered test units (same rule as `ci.yml`) as parallel processes via `shared/scripts/tools/run_test_suite.py`, opt-in per project via a `suite` block in `shipwright_test_config.json`; `pytest-xdist` is a per-unit allowlist, never global `-n auto`. Anti-regression invariants (JUnit-proven "pytest ran", authoritative serial re-verify = no false STOP, same-shape infra retry, CI-stays-serial guard) live in the ADR. → decision_log (Run-ID).
- **iterate-2026-07-11-phase-completed-per-split** (2026-07-11): Convention — when changing an event type's cardinality (one-per-X → many-per-X) or its dedup key, audit EVERY consumer of that event, not just the obvious one: counts (`len(list)` → `len({distinct})`), start/end pairing, and latest-by-`ts` selection (`max`, reading the real `ts` field). Code review found 3 such consumers beyond the one anticipated. → decision_log (Run-ID).
- **iterate-2026-07-10-adopt-brief-plainbank** (2026-07-11): Convention — the run + iterate skill §A banners surface plain-language glosses quoted VERBATIM from the shared bank (docs/guide.md Appendix A Plain-Language Index, the same one the WebUI glossary mirrors); `shared/tests/test_banner_plain_language_parity.py` binds each gloss to the index so any un-mirrored edit FAILS (copy-with-parity, one source, no drift). → decision_log (Run-ID).
- **iterate-2026-07-10-emit-phase-started** (2026-07-11): Convention — `phase_started`/`phase_completed` emission is scoped to the 7 PIPELINE phases only; iterate F-phase (F0–F12) per-phase timing is DEFERRED (the iterate flow records one `work_completed` at F5b, so there is no `phase_completed` pairing model; the WebUI Iterate-Rail is a `narrator.ts` display-grouping over the ~20 real phases), tracked via triage `trg-8efeb3d7` rather than silently narrowed. → decision_log (Run-ID).
- **iterate-2026-07-10-claude-md-invariant-index** (2026-07-10): Convention — CLAUDE.md is orientation + a terse invariant index: a new invariant/DO-NOT rule is ONE line + an ADR pointer, rationale lives in the ADR. Seeded by both producers (greenfield template + adopt renderer, mirror-tested) and enforced repo-agnostically: the F11 gate `check_agent_doc_budget` blocks >30-line net CLAUDE.md growth per iterate (forward-only; creation/deletion exempt; deliberate escape `SHIPWRIGHT_CLAUDE_MD_GROWTH_OK=1`, visibly noted). → decision_log (Run-ID).
- **iterate-2026-07-08-marketplace-autoinstall** (2026-07-08): Convention — `scripts/update-marketplace.sh` now brings EVERY marketplace-registered plugin into the cache: a registered-but-not-installed plugin (e.g. the opt-in `shipwright-grade` lead magnet, previously left `not_in_cache` forever) is `claude plugin install`ed from the marketplace, then synced — instead of silently skipped. Install is guarded (only when uninstalled → never re-installs) and non-fatal under `set -e`; a shared `_install_path` helper de-dups the lookups. → decision_log (Run-ID).
- **iterate-2026-07-07-grade-public-url-network** (2026-07-07): Convention — the grader defaults network enrichment ON only for a **github.com URL / `owner/repo`** target (its identity is already sent to github.com to clone it) and OFF for a **local path** or a **GitHub Enterprise host** (privacy-first — a GHE slug must not leak to github.com). `open_target` tags a cloned remote `input_kind="url"`; `grade.py` gates the default on `is_github_com_remote`. Private remote auto-disables (`--allow-network-private` opt-in); no `gh` → local grade, never F. → decision_log (Run-ID).
- **iterate-2026-07-07-grade-local-honesty** (2026-07-07): Convention — Control-Grade change-traceability is graded ONLY off trustworthy provenance (real event-log records or `--allow-network` PR-association); the default LOCAL-only mode renders it **n/a**, since the git-log `#N` fallback anti-correlates with quality. Gated by `GradeInputs.change_traceability_measurable` (default True; the cold projector sets it False locally), so a well-run repo with reference-free commits no longer F-collapses to "out of control". Network fixtures + dogfood A unchanged. → decision_log (Run-ID).
- **iterate-2026-07-07-diff-coverage-adopt-templates** (2026-07-07): Convention — every vitest adopt CI template (ci-supabase-nextjs, ci-vite-hono) now ships a **warn-only diff-coverage gate**: a `diff-coverage` job runs npm coverage (cobertura) → pinned `uvx diff-cover@10.3.0 --fail-under=80`, `continue-on-error`, ubuntu-only, dormant. diff-cover resolves each package's paths natively via cobertura `<source>` (client/server needs no combine — proven on WebUI #205). Adopter adds `@vitest/coverage-v8`, flips to hard-block later. → decision_log (Run-ID).
- **iterate-2026-07-06-grade-cold-repo-b-cap** (2026-07-06): Convention — a COLD (heuristic) Control Grade now caps at **B**; **A is authoritative-only**. Change-reconciliation is a load-bearing control a cold external repo can't demonstrate, so the projector declares it the one `expected_dimensions` entry and the unchanged honesty gate caps the headline at B — it can't claim "full control" over an unseen control. Heuristic-only (authoritative sets its own; dogfood monorepo measures reconciliation 1.0 → stays A). → decision_log (Run-ID).
- **iterate-2026-07-06-diff-coverage-hard-flip** (2026-07-06): Convention — a PR whose CHANGED lines are < 80% covered by tests now BLOCKS merge: the ci.yml "Diff coverage (gate)" step runs the tested `measure_diff_coverage.py --fail-under 80` wrapper WITHOUT continue-on-error, and its `ci_gate_allowlist` entry is removed so the CI-gate guard's reverse-drift + stale-entry checks enforce it stays gating (a diff-cover crash/absence also fails closed). Ends the warn-only settling window; empirically justified by the real-PR replay corpus (4 pass / 1 legit fail). → decision_log (Run-ID).
- **iterate-2026-07-06-grade-g5-empirical-suite** (2026-07-06): Convention — shipwright-grade is verified against reality by an opt-in empirical launch gate: real OSS repos pinned to a commit SHA, projected once and cached as record/replay fixtures (projected `GradeInputs` + report-extras, so `compute_grade` re-runs offline), asserting BANDS + relative ordering, never exact scores; contradictions tune the projection heuristics, NEVER the shared scorer. Recorded live only by `grade-empirical.yml`; excluded from the PR gate. → decision_log (Run-ID).
- **iterate-2026-06-30-workflow-token-permissions** (2026-06-30): Convention — GitHub Actions workflows declare a read-only top-level `permissions:` (`contents: read`) and widen per-job ONLY to the write scope a job uses (least-privilege `GITHUB_TOKEN`, OpenSSF Scorecard Token-Permissions); a job block REPLACES the top-level, so it re-declares needed reads. `security.yml` is the documented exception (single-job SARIF; top-level convention-locked by compliance A5.3). Pinned by `test_workflow_token_permissions.py`. → decision_log (Run-ID).
- **iterate-2026-06-28-grade-anchor-clarity** (2026-06-28): Convention — public Control-Grade output (the dashboard Anchor column + guide.md) cites only OPEN, googleable standards in plain language (DO-178C / IEC 62304 / ISO 26262 / ISO/IEC 25010 / NIST SSDF / OWASP / SLSA / OpenSSF Scorecard); NEVER a commercial-vendor product (SonarQube / Sonar Way removed), enforced by `test_control_grade::test_no_commercial_vendor_trademark_in_anchors`. → decision_log (Run-ID).
- **iterate-2026-06-27-codeql-security-hardening** (2026-06-27): Convention — the monorepo CodeQL run keeps the full `security-and-quality` suite but tailors it via `.github/codeql/codeql-config.yml` `query-filters`, excluding ONLY quality queries ruff already gates (`unused-import`/`unused-local-variable`, import-style) or that conflict with the fail-open `empty-except` convention; NEVER a security or high-value-correctness query. The `/shipwright-adopt` CodeQL template stays generic (tailoring is monorepo-specific). → decision_log (Run-ID).
- **iterate-2026-06-20-aggregate-triage-stop-fanout-dedup** (2026-06-20): Convention — `aggregate_triage_on_stop` (regenerates the `triage_inbox.md` derived cache, registered in all 12 plugins) gains the `event_once.claim_once_for_event` once-per-(Stop,session) guard (`stop-triage-inbox` key) after the `is_shipwright_project` no-op guard, so one Stop regenerates once (was ~12× redundant non-atomic writes); a failed winner releases the claim so a sibling retries. Follow-up to the bloat-gate dedup. → decision_log (Run-ID).
- **iterate-2026-06-20-bloat-gate-stop-fanout-dedup** (2026-06-20): Convention — the bloat-gate Stop hook now carries the `event_once.claim_once_for_event` once-per-(Stop,session) guard (`stop-bloat` key) on its BLOCK path only, after every no-op/pass guard, so one Stop event emits ONE Iron-Law block instead of one-per-plugin (12× in webui session bfd244ca). Corrects iterate-2026-06-14-hook-fanout-dedup, which wrongly listed it as convergent — the empty pass path masked the N× block. → decision_log (Run-ID).
- **iterate-2026-06-16-compliance-rendering-fixes** (2026-06-16): Convention — Type-column compliance producers (RTM Verification Timeline, Build Dashboard) MUST route the event `intent` through `shared/scripts/event_classification.normalize_intent`; completed-work `passed<total` renders skip-aware PASS/COVERED (never a gap-driven FAIL — real regressions surface via the open-triage override); the dashboard links the audit report UNCONDITIONALLY (gitignored/transient → a conditional link flip-flops). → decision_log (Run-ID).
- **iterate-2026-06-17-launch-version-branding** (2026-06-17): Convention — all 13 plugins + `marketplace.json` now carry ONE shared version (0.29.0), bumped in lockstep at release; per-plugin independent SemVer retired. Maturity label is "Beta" (not "Early Access Beta"). → decision_log (Run-ID).
- **iterate-2026-06-14-hook-fanout-dedup** (2026-06-14): Convention — cross-cutting Claude-Code hook fan-out deduped symmetrically (no single controlling plugin): register-everywhere kept, genuinely-redundant work wrapped in a fail-open once-per-(event,session) guard (`event_once.claim_once_for_event`) on the audit/handoff/drift hooks; the phase-quality Stop audit resolves engaged phase(s) from session state (`resolve_engaged_phases`) not `CLAUDE_PLUGIN_ROOT`. → decision_log (Run-ID).
- **ADR-017** (2026-05-02): Repo cleanup post self-adoption: webui drift, legacy plans, FR populate
- **ADR-018** (2026-05-02): Adopt plugin: drift detection, test-fixture filter, compliance fallback fix
- **ADR-019** (2026-05-02): Hook installer writes canonical matcher-group shape
- **ADR-020** (2026-05-03): Quote uv-run path placeholders + upgrade legacy hook entries (Shape + command) in place
- **ADR-021** (2026-05-03): Adopt scaffolds .env.local with profile + framework keys (Layer-3 SSoT)
- **ADR-022** (2026-05-03): Quote ${CLAUDE_PLUGIN_ROOT} in plugins/*/hooks/hooks.json
- **ADR-023** (2026-05-03): Detect Git-Bash MSYS path-mangling in changelog drop bullets
- **ADR-026** (2026-05-03): Multi-Session Discipline — session-role marker + push guardrail (campaign iterate-skill-hardening Sub-Iterate C)
- **ADR-030** (2026-05-05): suggest_iterate hook is plugin-registered, not project-installed (retire hook_installer)
- **ADR-031** (2026-05-05): FR-table parser accepts 5-col adopt format + drift protection
- **ADR-037** (2026-05-06): F0.5 End-to-End Verification Gate (surface taxonomy + schema-enforced evidence)
- **ADR-038** (2026-05-06): F0.5 empirical-test backfill: drift-schutz + real subprocess probes + CLI audit chain
- **ADR-041** (2026-05-09): known-issues scanner requires comment context; remove dead save_session_config
- **ADR-042** (2026-05-10): Stop and SubagentStop hooks emit schema-compliant stdout
- **ADR-043** (2026-05-11): Adopt scaffolds profile-aware CI + Claude-Review workflows with cross-platform OS matrix default
- **ADR-049** (2026-05-16): Unconditional worktree isolation for /shipwright-iterate
- **ADR-050** (2026-05-16): Worktree-aware event-log resolution
- **iterate-2026-05-23-verifier-multi-commit-aware** (2026-05-23): three iterate-skill discipline lessons — (1) TDD RED-first applies to drift-protection tests (the RED phase is the proof the test discriminates); (2) run `check_iterate_isolation.py --stage f0` at finalization start, symmetric with `--stage f11`; (3) `--architecture-impact` and the doc bullet are structurally coupled (silent drift otherwise), now gated by `test_architecture_md_reflects_arch_impact.py`. → decision_log (Run-ID).
- **Drift-protection meta-pattern** (2026-05-23): a prose-only convention's historical drift is only findable by a mechanical test enumerating every governed artifact, written RED-first against current state (the architecture-md test surfaced 9 historical drift entries); keep it as a forward gate. → `shared/tests/test_architecture_md_reflects_arch_impact.py`.
- **ADR-056** (2026-05-21): Markdown table cell escaping helper
- **ADR-057** (2026-05-20): Triage Inbox redesigned as a launch-surface: action-units + launchPayload + CLI
- **ADR-058** (2026-05-21): Mode-aware dashboard + Why-warn column + Triage open indicator
- **ADR-059** (2026-05-21): SBOM undeclared-license triage (per workspace)
- **ADR-060** (2026-05-21): Per-layer FAIL triage + Layer column + record_event layers schema
- **ADR-061** (2026-05-21): RTM consumes frId cross-link + actionable Coverage subsections
- **ADR-062** (2026-05-21): Hard-enforce FR-or-change-type at iterate finalize (forward-only)
- **ADR-063** (2026-05-21): F4-F7 detective-only documentation hygiene checks
- **ADR-064** (2026-05-21): Detective-only plugin-cache vs repo drift check
- **ADR-069** (2026-05-21): Triage producer contract: schema + RTM-link fields + inbox polish
- **ADR-070** (2026-05-21): Deterministic render timestamps via events.jsonl max-ts
- **ADR-072** (2026-05-23): Compliance MDs: single-producer + snapshot-provenance audit
- **ADR-073** (2026-05-23): F7b seals F7 appends in self-tracking repos
- **ADR-074** (2026-05-23): Extend snapshot producers: adopt Step H + security Step 7.5
- **ADR-075** (2026-05-23): Test enforces architecture.md ↔ decision-drop coupling; 11 historical drift entries backfilled
- **ADR-076** (2026-05-23): F11 verifier resolves F7 event by run_id, not HEAD commit
- **ADR-082** (2026-05-24): SBOM producer collapses N workspaces with same undeclared-signature into one cluster
- **ADR-083** (2026-05-25): Pre-commit + CI anti-ratchet gate + bloat-exception ADR template + glossary
- **ADR-084** (2026-05-25): Two-hook structural prevention against file-size regrowth
- **ADR-085** (2026-05-25): Bloat-policy detective audit lands as Group H (G collision-avoidance)
- **ADR-089** (2026-05-27): Guide + README refresh for Campaign A+B (post-v0.22.0)
- **ADR-091** (2026-05-27): Runtime/snapshot split for agent-doc trio + hard-gated finalize repair pass
- **ADR-094** (2026-05-29): events.jsonl is a per-tree, PR-committed artifact
- **ADR-097** (2026-05-29): SessionStart bootstrap + plugin-cache Stop reminder; Python hooks keyed off payload session_id
- **ADR-100** (2026-05-30): Canonical .shipwright gitignore block propagates to consuming projects
- **ADR-102** (2026-05-30): Three-stage build reviewer cascade: spec-reviewer (HARD-GATE) -> code-reviewer -> conditional doubt-reviewer
- **ADR-106** (2026-06-01): Churn-artifact merge reconciliation: events=union + regenerate-on-conflict resolver
- **ADR-108** (2026-05-31): CI gate-coverage guard + workflow hardening
- **ADR-109** (2026-05-31): CI lint gate: curated bug-focused ruff ruleset + de-neuter
- **ADR-111** (2026-06-01): Per-project disabled_checks applicability gate + D5 change_type exemption
- **ADR-112** (2026-05-31): Compliance triage emits one rolling backlog action-unit, not one item per check
- **ADR-114** (2026-05-31): Phase-quality triage emits one rolling backlog action-unit, not one item per FAIL
- **ADR-118** (2026-06-01): Pin third-party GitHub Actions to SHA + verify Gitleaks download
- **ADR-119** (2026-06-01): Assert upload-sarif on the real uses: line, not a comment substring
- **ADR-120** (2026-06-02): Dedup SessionStart Phase-Quality injection to once-per-event
- **(2026-06-05, iterate gitleaks-report-path)**: gitleaks `--report-path -` writes a literal file named `-`, NOT stdout — read the JSON from a real temp file. A "detects on Windows, 0 on Linux CI" story can be a measurement artifact: verify the exact wrapped command's output channel against the real binary before concluding platform divergence. → decision_log (Run-ID)
- **(2026-06-06, iterate arch-drift-detector)**: a drift detector that diffs the git HISTORY of gitignored inputs (decision-drops) can never fire — reconcile the staging CONTENT against the tracked artifact instead; share ONE oracle between the detective and its finalize gate; a prose-only "always update X" step with no live gate WILL drift, so convert it to a canon gate. → decision_log (ADR-136 via Run-ID)
- **(2026-06-07, iterate triage-docs-monorepo-migration)**: migrate a MAIN-tree data pile via a worktree iterate by GC-ing main's pile in place (leak-guard-exempt) then copying the canonical snapshot into the worktree so F6 ships it; don't run `update_compliance` when no OPEN items of that source remain (it dirties main's clean compliance MDs and STOPs F0); verify a destructive GC by diff invariants against the `.bak`. → decision_log (Run-ID)
- **ADR-125** (2026-06-05): B7 excludes non-functional Conventional-Commit types (supersedes narrow Rule D)
- **ADR-126** (2026-06-05): Bloat marker + Stop-gate resolve the worktree path to the baseline key
- **ADR-128** (2026-06-05): Enforce the FR-gate on the finalize write-path; D3 accepts same-event delivery
- **ADR-130** (2026-06-05): SBOM cluster dedup-key = signature + manifest_type only
- **ADR-134** (2026-06-05): git-track triage.jsonl via gitignore negation + scaffolder self-heal
- **ADR-136** (2026-06-06): Architecture-drift detection is content reconciliation, enforced by a canon F11 gate
- **iterate-2026-06-10-event-self-id** (2026-06-10): pytest `-m` on the CLI REPLACES the `addopts` marker filter — `-m "not cross_plugin"` silently re-enabled the `slow` suite (D2V concurrency-stress spawners). Always compose: `-m "not slow and not cross_plugin"`.
- **(2026-06-10, iterate finalize-resolver / campaign S3)**: don't reuse the global "regenerate ALL derived snapshots" pattern for a per-entity artifact — scope a merge-time regeneration to the entities the merge actually TOUCHED (conflicted campaign `status.json`), leaving untouched ones byte-identical (never-downgrade self-heals); verify an auto-running producer against LIVE data, not a clean fixture. → decision_log (ADR-160 via Run-ID)
- **(2026-06-11, iterate bloat-gate-worktree-baseline)**: a check comparing a MEASUREMENT to a THRESHOLD must read both from the same tree — the bloat Stop-gate re-measured the worktree file but read its ceiling from MAIN's baseline, false-blocking a worktree baseline bump. Resolve per-tree state (baseline/config) from the worktree, fall back to main, never to empty. → decision_log (ADR-126 via Run-ID)
- **(2026-06-11, iterate automerge-gh-pr-ci-producer)**: a PR-checks producer must treat `len(check_runs) < total_count` as a truncated view (return None, don't classify on a partial set) and pass `?filter=latest` (else a superseded failed run re-reddens a fixed PR); a session-autouse network stub also shadows the unit tests OF those fns — expose `real_*` opt-back-in fixtures. → decision_log (Run-ID)
- **(2026-06-11, iterate automerge-f11-arm)**: a pipeline step depending on an out-of-band repo/infra setting must be fail-soft (`|| echo WARN`, never `|| exit`) or it breaks the pipeline for everyone the moment the setting is off; self-modifying plugin changes don't take effect this run (ship in a PR; future iterates get it); gate behavior on the actual branch name, not an assumption. → decision_log (ADR-165 via Run-ID)
- **(2026-06-12, iterate triage-status-idle-main-outbox)**: a routing rule keyed off a context predicate must cover EVERY write verb (append/status/delete/promote) — `mark_status` was left residence-derived after appends were routed to the outbox, re-introducing idle-main drift; to test a predicate that probes git state, the fixture MUST build a real repo WITH an origin remote. → decision_log (ADR-189 via Run-ID)
- **iterate-2026-06-12-utf8-churn-merge** (2026-06-12): strict UTF-8 round-trip in `resolve_churn_conflicts._git` + structured commit-failure handling in `integrate_main` (no mojibake / MERGE_HEAD wedge on cp1252). → decision_log (Run-ID; ADR-099 amendment).
- **iterate-2026-06-12-agent-doc-entry-rules** (2026-06-12): agent-doc update entries are one-line ADR pointers, routed by `lib.architecture_doc.IMPACT_TARGETS` (convention → here; component/data-flow → architecture.md `## Architecture Updates`) and capped by a forward-only 600-char budget gate; the oracle (F11 gate + Group-F detective) checks each impact against its target doc. → decision_log (Run-ID).
- **iterate-2026-06-12-triage-tooling-hardening** (2026-06-12): Convention — deep-audit WP9 triage-tooling hardening: `triage_gc.MACHINE_REASONS` gains `phaseQualityRefreshed` (+ a forward/reverse drift meta-test); `apply_gc` recomputes the droppable set under-lock ∩ the caller's planned ids (honors a concurrent re-open); `_strip_control_chars` is wired into title/detail/evidence incl. the C1 range; `triage_promote` relaxes its existence pre-check to tracked-OR-outbox. Campaign `2026-06-10-audit-1-auto`. → archive
- **iterate-2026-06-12-triage-gc-union-residence** (2026-06-12): Convention — a1-6/F19 follow-up: `apply_gc`'s under-lock recompute is now union-residence aware (`read_all_items`, tracked ∪ outbox) so an outbox-routed re-open survives GC (D1: report tracked-only, only the tracked file rewritten). The source-derived drift meta-test adds the missing `prChecksResolved` + allowlists the `auditResolved` orphan; `_strip_control_chars` → `shared/scripts/lib/tty_sanitize.py`. Campaign `2026-06-10-audit-1-auto`. → decision_log (Run-ID).
- **iterate-2026-06-11-automerge-f11-arm** (2026-06-11): Convention — `/shipwright-iterate` F11 arms GitHub-native auto-merge after `gh pr create` (`gh pr merge --auto --squash --delete-branch`) for `iterate/*` PRs only (a `case` branch-scope guard) and fail-soft (`||`-guarded WARN, never `|| exit`). B4.5 Phase 3; `trg-bdc160e2`. Drift: `test_f11_automerge_arm.py`. → archive
- **iterate-2026-06-11-automerge-pr-review** (2026-06-11): Convention — new CI workflow `.github/workflows/pr-review.yml` gates external-contributor / sensitive-path / `needs-review` PRs through an OpenRouter LLM review before auto-merge (Tier 1/2 PRs are NOT reviewed at the PR stage); logic in shipwright-security `pr_review.py` + `pr_review_lib.py` (stdlib urllib, ≤300 LOC each); required check `PR Review`. B4.5 Phase 2. → archive
- **iterate-2026-06-11-backfill-docs** (2026-06-11): Convention + docs — closes campaign `2026-06-07-tracked-campaign-status` (S4, anchor `trg-fda5f7a3`): `parse_campaign_skeleton` strips wrapping markdown emphasis from id/slug cells (else a legacy bold-id `campaign.md` drops completed subs on re-projection); new read-only drift-guard `test_campaign_status_backfill.py`; glossary + hooks-and-pipeline docs landed. → archive
- **iterate-2026-06-11-spec-path-relative** (2026-06-11): Convention — campaign sub-iterate `spec_path` is now repo-relative POSIX (not a machine-absolute Windows path) so the WebUI + fresh clones resolve it portably; new pure module `shared/scripts/lib/campaign_paths.py` (`relativize_spec_path` / `campaign_spec_path`); producers write relative + the projection self-heals; a one-off migration rewrote all 7 tracked campaigns. `trg-196f4aa6` (N1). → archive
- **iterate-2026-06-10-complexity-classifier-prior** (2026-06-10): Convention + data-flow — the iterate Stage-1 complexity classifier's fall-through default is history-calibrated: with no scope-keyword match, `classify()` reads the last ≤20 F5c entries and uses their median final complexity (clamped to medium); `signals.prior_source` surfaces `keyword|history|default`. Scope keywords moved to `complexity_vocabulary.py` (alnum-boundary + optional-plural). → archive
- **iterate-2026-06-10-d2v-evidence-write-optin** (2026-06-10): Convention — the D2V empirical-gate evidence artifact (`D2V-empirical-results.md`) is regenerated only on opt-in (`SHIPWRIGHT_D2V_WRITE_EVIDENCE`): `_d2v_helpers.Evidence.flush()` early-returns otherwise (the gate assertions still run). Ends the dirty-tree / manual-revert churn that hit 3 consecutive iterates. → archive
- **iterate-2026-06-10-triage-list-json** (2026-06-10): Convention — new machine-readable contract `triage_cli.py list --json` for the Command Center WebUI live-view: emits the unioned open items (`triage.read_all_items`, tracked ∪ outbox) as a JSON array, each enriched with `pendingDelivery: true` iff the item lives only in the gitignored outbox (TRACKED-PREFERRED). Read/output surface only. `trg-e2a0ebb3`. → archive
- **iterate-2026-06-09-audit-report-hygiene** (2026-06-09): Convention — the compliance detective-audit writer relocates `shipwright_audit_report.json` from the repo ROOT to `.shipwright/compliance/audit-report.json` (re-excluded in the gitignore canon, propagating to adopted repos); the stdout JSON stays the stable contract. Same ADR-089 leak class as idle-main-artifact-hygiene. PR #174. → archive
- **iterate-2026-06-09-external-review-marker-gitignore** (2026-06-09): Convention — the gitignore canon re-excludes the transient external-review gate markers under `.shipwright/planning/iterate/**/external_*review_state.json` (run-scoped, NOT RTM evidence — `rtm.collect_external_review_states` skips `planning/iterate/`); it deliberately does NOT touch the durable `planning/<split>/` evidence. Near-miss guard via a real `git check-ignore` round-trip. → archive
- **iterate-2026-06-09-idle-main-artifact-hygiene** (2026-06-09): Convention — completes ADR-089 for two idle-`main` producers: new `shared/scripts/lib/repo_root.py::main_repo_root_or` (the bloat marker writer + reader resolve the canonical main root, not `Path.cwd()`); the 3 phase-quality `skill-compliance` roll-ups relocate under the already-gitignored `skill-compliance/`; new canon rule `**/.shipwright/locks/`. PR #173. → archive
- **iterate-2026-06-07-scaffold-churn-merge-machinery** (2026-06-07): Convention — the append-log `merge=union` `.gitattributes` driver is scaffolded into every managed repo: new SSoT `gitattributes-union.template` + `shared/scripts/lib/gitattributes_union.py` (`UNION_PATHS`, `merge_into`, `self_heal_gitattributes`); adopt Step E.13c + `setup_iterate_worktree` self-heal. New write-surface: the target repo root `.gitattributes`. → archive
- **iterate-2026-06-07-sbom-not-installed-vs-undeclared** (2026-06-07): Convention — the SBOM license resolver distinguishes `NOT_INSTALLED` (a scan-environment property — silent, rendered `-`, excluded from counts/verdict) from `UNKNOWN_LICENSE` (a real triage finding + `sbom.md` concern). New sentinels in `collectors/_license_const.py`; producer-side so it generalizes to every repo. Reverses ADR-056 for the not-installed case. → archive
- **iterate-2026-06-07-adopt-gitleaks-allowlist** (2026-06-07): Convention — /shipwright-adopt scaffolds a companion `.gitleaks.toml` allowlist alongside `security.yml` (Step E.13b, new `gitleaks_config_scaffolder`); the deployed path + template are declared as SSoT constants in `security_workflow.py`, pinned by `test_gitleaks_config_convention.py`. Stops every adopted repo's first scan false-redding on the `cafebabe:deadbeef` placeholder. PR #163. → archive
- **iterate-2026-06-08-outbox-delivery-d3** (2026-06-08): Convention — the canonical `.shipwright/` artifact-ignore block (incl. `/.shipwright/triage.outbox.jsonl`) is self-healed into every adopted / stale-cache repo on its next iterate via new `lib/gitignore_selfheal.self_heal_gitignore` (sibling of `self_heal_gitattributes`), wired into `setup_iterate_worktree` step 4.6 as a guarded `chore` commit. Campaign `2026-06-08-triage-outbox-delivery` D3. → archive
- **iterate-2026-06-10-canon-exempt-agent-doc-caches** (2026-06-10): Convention — the ADR-089 single-producer agent-doc trio (`triage_inbox.md` + `session_handoff.md` + `build_dashboard.md`) is exempt from the `artifact-path-canon` Layer-1 lint in all 4 `ALLOWLIST` migrations (they render free-form text that may quote any legacy path + are regenerated each iterate). Drift-locked by `test_generated_cache_canon_exempt`. Closes `trg-6ed063ae`. → archive
- **iterate-2026-06-12-compress-agent-doc-backlog** (2026-06-12): Convention — compacted the always-loaded agent-doc backlog to one-line pointers: architecture.md `## Architecture Updates` (component/data-flow only; `convention` entries migrated here), conventions.md `## Convention Updates` (+16 migrated) and `## Learnings` (verbatim detail archived under `.shipwright/planning/adr/_archive-agent-doc-updates.md`). Retired the `_LEGACY_CONVENTION_DOC` fallback in `architecture_doc.py` and lowered the entry-budget gate cutoff to 2026-05-01. → decision_log (Run-ID).
- **iterate-2026-06-12-canonical-project-predicate** (2026-06-12): Convention — the greenfield/foreign boundary is now a single canonical predicate `lib.project_root.is_shipwright_project` (any config marker OR `.shipwright/agent_docs/`; fail-closed) that every hook delegates to, collapsing 4 divergent marker sets so a tree is classified identically everywhere; `resolve_project_root` gains a config-subdir tie-break. Drift-guard `test_canonical_project_predicate.py`. PR #209. → decision_log (Run-ID).
- **iterate-2026-06-12-w2-unresolvable-runid-skip** (2026-06-12): Convention — every run_id-keyed phase-quality verifier must share the `unresolvable_run_id_skip` guard (`tools/verifiers/_iterate_run_id.py`): a sentinel run_id (`unknown`/empty) with no run-specific marker SKIPs as not-applicable, never FAIL/PASS on the run-agnostic state file. W2 was the asymmetric outlier vs S2/S3 → recurring audit-context Tier-1 false-positive. Drift-guard `test_workflow_w2_run_id_guard.py`. → decision_log (Run-ID).
- **iterate-2026-06-29-compliance-exempt-not-deficit** (2026-06-29): Convention — compliance producers must credit a legitimately-exempt item OUT of its deficit signal (BP-1 traced-credit, generalized): grandfathered bloat over-limit renders INFO not WARN (ratchet delta is the live signal); "Iterate tests passing" excludes satisfied-no-FR changes (`iterate_test_coverage`); audit `_Suggested:` emits only on `status==fail`; routine regens stamp a churn-free `audit-report.md` staleness banner (`audit_freshness`). → decision_log (Run-ID).
- **iterate-2026-06-30-compliance-artifact-usability** (2026-06-30): Convention — compliance Event labels prefer an authored plain-language `summary` (new optional `work_completed` field via F5b `--event-extras-json`; falls back to a cleaned `description`); the audit artifacts are navigable — Test-Evidence `Source` + RTM `(iter)` link to the event's Verification-Timeline row (newest-first), whose FRs link to their `rtm-fr-…` coverage anchor and commits to the host diff. Synthesized "Full Suite Runs" gains an honest unit-only note. → decision_log (Run-ID).
- **iterate-2026-06-30-timeline-utc-date** (2026-06-30): Convention — render a date column in the SAME frame it is sorted by. The RTM Verification Timeline sorts by absolute UTC instant, so the Date column now uses `_rtm_links.utc_date` (ts→UTC `%Y-%m-%d`) instead of the literal `ts[:10]`; otherwise a non-UTC event recorded near midnight prints its local date one day off its sort position (cosmetic inversion — caught empirically on a real `+02:00` webui event). → decision_log (Run-ID).
- **iterate-2026-07-19-requirements-merge-catalog** (2026-07-19): Convention — a requirement states WHAT the product does, nothing about how it got there: requirement text carries no run id, no ADR number and no file path. Deep-link anchors are emitted EXPLICITLY, never left to slug generation, and are verified by resolving the link END TO END rather than inspecting the file. Pinned by `test_requirements_catalog_contract.py`; detail in ADR-109. → decision_log (Run-ID).
- **ADR-143** (2026-06-07): Adopt scaffolds .gitleaks.toml + hardens security.yml.template
- **ADR-147** (2026-06-07): SBOM splits 'not installed' from 'no declared license'
- **ADR-148** (2026-06-08): Scaffold append-log union merge driver into managed repos
- **ADR-152** (2026-06-08): Explicit outbox gitignore propagation + iterate self-heal
- **ADR-153** (2026-06-09): Relocate detective-audit JSON under .shipwright/compliance/ so the gitignore canon covers it
- **ADR-154** (2026-06-09): Iterate-scoped external-review markers are gitignored (not blanket)
- **ADR-155** (2026-06-09): Idle-main producers resolve main_repo_root + write transient state only to gitignored paths
- **ADR-156** (2026-06-10): Generated agent-doc cache trio fully exempt from artifact-path-canon (finalize trg-6ed063ae)
- **ADR-157** (2026-06-10): History-calibrated complexity prior + cross-domain scope vocabulary
- **ADR-158** (2026-06-10): D2V evidence artifact write is opt-in (SHIPWRIGHT_D2V_WRITE_EVIDENCE)
- **ADR-159** (2026-06-10): Campaign sub-iterates self-identify via event extras stamp
- **ADR-164** (2026-06-10): triage_cli.py list --json: machine-readable contract for the WebUI live-view
- **ADR-165** (2026-06-11): F11 arms GitHub-native auto-merge for iterate/* PRs
- **ADR-167** (2026-06-11): Tier-3 PR review via OpenRouter custom-script
- **ADR-168** (2026-06-11): Campaign skeleton tolerates markdown emphasis; backfill drift-guard
- **ADR-171** (2026-06-11): Campaign sub-iterate spec_path is repo-relative POSIX
- **ADR-172** (2026-06-12): Compact agent-doc entries + impact-aware routing SSoT
- **ADR-176** (2026-06-12): Single canonical Shipwright-project predicate
- **ADR-179** (2026-06-12): Compact agent-doc backlog + retire convention-routing fallback
- **ADR-188** (2026-06-12): triage_gc under-lock recompute is union-residence aware
- **ADR-190** (2026-06-12): Triage tooling hardening: GC token SSoT, GC TOCTOU, control-char sanitizer, outbox CLI
- **ADR-192** (2026-06-12): UTF-8-strict churn _git + structured commit-failure handling
- **ADR-222** (2026-06-14): Hook fan-out deduped via once-per-event guard + session-state phase resolver
- **ADR-227** (2026-06-16): Skip-aware compliance rendering + centralized intent normalization
- **ADR-230** (2026-06-17): Unify all plugin/marketplace versions to 0.29.0; relabel Early Access Beta to Beta
- **ADR-234** (2026-06-20): Dedup the aggregate_triage Stop regen across the plugin fan-out
- **ADR-235** (2026-06-20): Dedup the bloat-gate Stop block across the plugin fan-out
- **iterate-2026-07-07-gh-tag-helper-wire-in** (2026-07-07): SIMPLIFY — route both GH-owned-action-tag call-sites (SARIF ingest + plugin scan) through the single shared `gh_action_tag_owner.is_github_owned_action_tag` predicate; dropped the inline duplication + now-unused primitive imports. Behavior-preserving (snapshot==verify). → decision_log (Run-ID).
- **iterate-2026-07-07-ss4-phase-runner-persistence** (2026-07-07): Convention — subagents own artifact persistence (write to disk via a Write tool: phase-runner + section-writer); `single-session-apply` fail-closes on a claimed-but-unwritten artifact (`verify_artifacts_exist`); the section-writer SubagentStop hook is a non-blocking fallback (supersedes ADR-042); resume reloads from run_config summaries via `single-session-reload`. Campaign SS4. → decision_log (Run-ID).
- **ADR-239** (2026-06-27): Tailor CodeQL to this repo; root-fix genuine findings, don't blanket-suppress
- **iterate-2026-07-14-phase-invocation-mode** (2026-07-14): Convention — a driven phase skill decides pipeline-vs-standalone ONLY from the dispatch token (`get_phase_context.py`, authority `phase_invocation_mode.py`), never from run-config state; 3 outcomes (pipeline / standalone=no-token / error=STOP). v1 `current_step` is write-once. Corollary: `update-step` is inert in a driven run. Drift-guard `test_phase_skill_invocation_mode_canon.py`. → decision_log (Run-ID).
- **iterate-2026-07-15-finalize-utf8-guard** (2026-07-15): Convention — every CLI that prints json.dumps(..., ensure_ascii=False) (or interpolates arbitrary exception/path text) MUST reconfigure sys.stdout/stderr to UTF-8 at the top of main(); finalize_iterate.py was the last finalization tool without it, crashing under cp1252 when finalize_bundle pipes F5b. Mirrors finalize_bundle/triage_cli/resolve_gate_policy/verifiers.stdio; drift-guard test_finalize_iterate_utf8.py. → decision_log (Run-ID).
- **iterate-2026-07-15-adopt-traceability-baseline** (2026-07-16): Convention (TT7) — `/shipwright-adopt` Step E.17 establishes the requirement→test traceability baseline at onboarding (scaffold the `@FR` convention into `.claude/rules/`, TT6 backfill of existing tests, repo-wide TT4 skip inventory, predeclared-decision layer resolution, `test_links` wired into the adopt compliance phase). → decision_log (Run-ID) + ADR-105.
- **iterate-2026-07-16-collector-test-roots** (2026-07-16): Convention — the `test_links` collector's scan roots are project-configurable via `shipwright_compliance_config.json` `traceability.test_roots` (dir names / fixed-depth globs; absent⇒historical default, present⇒exactly those) + `traceability.exclude_dirs` (extra descent-prune dir names). `os.walk`+in-place-prune replaces `rglob` (no descent hang); describe/it suite propagation gated to TS/JS. The monorepo opts `plugins/*/tests` + `shared/tests` in with `exclude_dirs:[fixtures]`. → decision_log (Run-ID).
- **iterate-2026-07-17-backfill-plugin-fr-tags** (2026-07-17): Convention — the enforcing TT5 gate (`_layer_coverage_regen`) scans a SUPERSET of `generate_file`'s configured roots (union `default_test_roots` + a monotonic re-scan of the base manifest's test dirs + git-rename targets) so a narrowed `traceability` config can't false-green the removal gate; RTM keeps pure REPLACE. Gotcha — `backfill_scan` must prune on IN-TREE parts or the `.worktrees` ancestor false-prunes every file. → decision_log (Run-ID).
- **iterate-2026-07-18-fr-authoring-rules** (2026-07-18): Convention — FR authoring has one cross-plugin SSoT `shared/fr-authoring.md` (plain business language, capability altitude, MINT-vs-FOLD gate, next-free `FR-{split}.NN` over live AND removed rows, name fence), cited by adopt step-b8 / project spec-generation / iterate path-a+b Step 2; adopt's contradicting "technical / describe what the code does" leitplanke was REPLACED. Advisory compliance Group I reports drift (I1-I3 never emit `fail` so they cannot flip the audit verdict; I4 duplicate/reused FR ID fails). → decision_log (Run-ID).
- **iterate-2026-07-18-fr-fold-map-resolution** (2026-07-18): Convention — a spec's `## FR-Fold-Map` is a first-class traceability input; one SSoT `fr_fold_map` resolves a tagged FR to its survivor for BOTH the `test_links` collector and the backfill engine, so granular `@covers` tags survive a taxonomy fold instead of orphaning. FALLBACK-only (a live FR never consults the map) and terminal-based. Broken edges fail CLOSED into `fold_defects`. New keys omitted when no fold-map ⇒ byte-identical manifest. Gotcha — both FR-table parsers MUST skip fold-map lines. → decision_log (Run-ID) + ADR.
- **iterate-2026-07-19-events-record-boundary-readers** (2026-07-19): Convention — every append-only JSONL reader delegates record boundaries to the shared SSoT `lib/jsonl_records`, never a per-line `json.loads` + skip-whole-line (which discards ALL records on a concatenated line); compliance crosses the ADR-045 barrier via `load_shared_lib`, not a duplicate parser. A module that is ALSO sentinel-loaded must import siblings BY PATH. 11 of 16 sites converted (rest: trg-360e494f). → decision_log (Run-ID).
- **ADR-328** (2026-07-20): Convention — Change history as a query over the event log, measured against what it replaced. → decision_log (ADR-328)
- **iterate-2026-07-21-review-record** (2026-07-21): Convention — every iterate review pass (self/plan/code/doubt/external_code) records what it FOUND to a per-run `.shipwright/planning/iterate/<run_id>/reviews.json`, immutable once terminal; F11 `check_review_record` STOPs while any type is `pending` AND when the tracked record is absent from the commit (F6 must stage the run dir — the `*.md` glob misses it, and an unstaged record dies with the worktree). Legacy `external_*review_state.json` markers are DUAL-written, never moved. → decision_log (Run-ID).
- **iterate-2026-07-23-design-verifier-skip-no-design-phase** (2026-07-23): Gotcha — a phase verifier reached by the between-phase validator (`_validate_design` → `run_design_checks`) runs BEFORE the orchestrator appends the phase to `completed_steps`, so a lifecycle-skip keyed on `completed_steps` must gate ONLY the artifact-missing branch, never a top-level short-circuit, or it neuters enforcement mid-phase; the sister check left strict is the artifact-presence sentinel. → decision_log (Run-ID).
- **iterate-2026-07-23-req3-elicitation-module** (2026-07-23): Convention — requirement elicitation has one cross-plugin SSoT `shared/requirement-elicitation.md` (Pocock grilling + domain-modeling; universal coverage stop-condition: every context dimension answered or `Basis: assumed`) + `shared/context-format.md` (the CONTEXT.md domain glossary, distinct from framework `glossary.md`), cited by project/adopt interview + iterate path-a/b; drift-guard `test_requirement_elicitation_refs.py`. Minted cross-cutting FR-01.16. → decision_log (Run-ID).
- **iterate-2026-07-27-test-phase-record-honesty** (2026-07-27): Gotcha — a LAZY `from lib.X import` does not dodge the ADR-045 collision once a plugin's own `scripts/lib` already owns `sys.modules['lib']`; the lazy import resolves against IT. Latent until the first in-process triage producer (all prior ones emit from subprocesses). Fix: path-based fallback (`triage._load_shared_lib`), safe only for lib modules with no intra-package imports. → decision_log (Run-ID).
- **iterate-2026-07-27-phase-gate-override-evidence** (2026-07-27): Convention — an override overrides the VERDICT, never the CHECK: a skipped gate destroys the evidence the waiver needs, and the reason is refused at BOTH the CLI and the library entry point. Detail in ADR-113. → decision_log (Run-ID).
- **iterate-2026-07-27-review-gate-failclosed-fork** (2026-07-27): Gotcha — GitHub scores a SKIPPED job as a SUCCESSFUL required check, so a required context must never come from a job that can be skipped; and `pull_request` runs its workflow FROM THE PR HEAD, so a `workflow_run` stage must re-derive tier AND content from the API. → decision_log (Run-ID).
- **iterate-2026-07-27-checks-that-gate-nothing** (2026-07-27): Gotcha — a drift producer fails in BOTH directions and each one mutes it: under-derivation (deriving check names from `automerge_readiness.KNOWN_WORKFLOWS`, deliberately adopt's five) reported this repo's own contexts as phantoms; over-derivation (counting `workflow_dispatch`-only workflows) reported the manual-only `grade-empirical.yml` as drift. → decision_log (Run-ID).
- **iterate-2026-07-27-plan-section-deps** (2026-07-27): Convention — a `SECTION_MANIFEST` line may name what a section presupposes (`03-api: 01-auth, 02-db`), read by ONE parser `lib/plan_manifest.py` that replaces the two drifting private copies, so the plan plugin's Step-7 gate and the phase verifier cannot disagree. A bare line declares nothing and passes — the promise it makes checkable is the numbering, and 'every dependency is earlier' subsumes cycle detection. → decision_log (Run-ID).
- **iterate-2026-07-27-plan-step9-gates** (2026-07-27): Convention — a plan section names the requirements it serves on a `Requirements:` line (tokens must match the FR-ID grammar in FULL, never mined from a longer string), and says what it is for with >=2 steps and a test strategy. The four Step-9 gates are code in `verifiers/plan_gate_checks.py` + the in-session `check-plan-gates.py`; a split showing no sign of the format warns strict-exempt instead of being stranded. → decision_log (Run-ID).
- **iterate-2026-07-27-handoff-tally-and-gate-honesty** (2026-07-27): Convention — a document that reports state must denominate against what will EXIST, not against what has been created so far, and may only assert a fact it has positive evidence for (a task's own status, never a counter that merely records a past dispatch). A drift guard must force the classification, not just a set membership. Detail in ADR-114. → decision_log (Run-ID).
- **iterate-2026-07-27-pr-review-diff-cap** (2026-07-27): Gotcha — a gate's verdict must be set by CONSTRUCTION, never derived from a parsed list: the input where no filename parses is exactly the one that must still fail closed, and a list-driven predicate reads it as "nothing was dropped". Corollary — a policy constant tests only reference symbolically is unpinned; pin the INTENT against measured evidence. → decision_log (Run-ID).
- **iterate-2026-07-28-cascade-delegated-to-nobody** (2026-07-28): Gotcha — an instruction is real only if it names an ACTOR and has an executable path; the reviewer cascade was specified purely as a delegation that pointed back at itself, so nobody spawned it. Five documented commands in this one run turned out to be unrunnable, three inside the fix itself. Drive the test from the PRODUCER's own documented example. → decision_log (Run-ID).
- **iterate-2026-07-28-cascade-ask-before-lapse** (2026-07-28): Gotcha — write a rule where the failure is DECIDED, not where the behaviour is described: every observed lapse of the review cascade closed its row at F11 (`F11.md` + the verifier's remediation string), so a rule added only to SKILL.md Step 8 reached agents that had already complied. Corollary — a named escape class (`--autonomous`) is worse than a vague one if operators can self-apply it, and a permission that nothing persists across compaction/handoff/resume must fail OPEN to asking. → decision_log (Run-ID).
