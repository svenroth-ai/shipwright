# Iterate ADR: iterate-2026-06-12-installer-shell-fixes

Campaign `2026-06-10-audit-1-auto` / sub-iterate `a1-7` / deep-audit WP10
(F33-F38). Complexity: **small**. Risk flag: **touches_io_boundary** (the
`.env.local` dotenv reader in verify-setup.sh).

See the mini-plan (`…-installer-shell-fixes-miniplan.md`) for the per-finding
problem statement and chosen approach.

## Changes

- `scripts/install.sh` — F33 (`missing=$((missing + 1))`), F34 (PATH
  `~/.local/bin:~/.cargo/bin`), F35 (add shipwright-adopt + awk strip-and-
  rewrite refresh with backup + non-empty guard), F36 (quote all
  `--plugin-dir` paths).
- `scripts/update-marketplace.sh` — F37 (resolve `$PYTHON_BIN` =
  python3→python→py; replace 3× bare `python -c`).
- `scripts/verify-setup.sh` — F38 (parse `.env.local` via canonical
  `shared/scripts/lib/env.py:parse_env_file`; `env_has` tests live-env OR
  identifier-validated parsed key list via fixed-string `grep -Fxq`; no
  `source`, no eval).
- `shared/tests/test_installer_shell_scripts.py` — 13 bash-subprocess tests
  (new).

## External-Plan-Review-Findings (Step 3.5, OpenRouter: openai + gemini)

| # | Provider | Sev | Finding | Disposition |
|---|----------|-----|---------|-------------|
| 1 | gemini | HIGH | F35 awk-overwrite can corrupt rc (no backup / empty result) | accepted-and-fixed — `.shipwright.bak` backup + `[ -s "$tmp_rc" ]` non-empty guard |
| 2 | openai | HIGH | F38 key names from `.env.local` not validated → could influence shell logic | accepted-and-fixed — parser emits only `^[A-Za-z_][A-Za-z0-9_]*$` keys; shell uses fixed-string `grep -Fxq` |
| 3 | gemini/openai | MED | F38 needs the python resolver in verify-setup.sh too (same bare-python bug) | accepted-and-fixed — `ENV_PYTHON` resolver (python3→python→py) added; degrades to a `[??]` warning if none |
| 4 | gemini/openai | MED | F34 `~/.local/bin` replacement may shadow/regress `.cargo/bin` installs | accepted-and-fixed — PREPEND both `~/.local/bin:~/.cargo/bin:$PATH`, never replace |
| 5 | gemini/openai | MED | F36 path may still break on `$`/backtick/quote chars beyond spaces | rejected-with-reason — the documented real-world case is spaces (OneDrive); a literal `$`/backtick in a clone path is vanishingly rare and `printf %q` would make the rc-embedded alias unreadable. Quoting fixes the reported defect; broader escaping is out of scope. |
| 6 | openai | MED | F37 `py` is a Windows launcher; deterministic python3→python preferred | accepted (already implemented) — resolver probes python3 first, then python, then py, with an explicit error+exit when none found (no reliance on empty substitutions) |
| 7 | openai | MED/LOW | F34 hardcoding astral target is brittle | partially-accepted — both XDG + cargo dirs prepended; a full `command -v uv` post-probe is a larger install-flow rework deferred (low value for the bugfix scope) |
| 8 | openai | LOW | docs/templates may still reference old alias/PATH | checked — no checked-in template embeds the 12-plugin alias or the cargo PATH guidance (grep below); the alias lives only in install.sh |
| 9 | openai | MED | review full prereq control flow, not just the increment | accepted — verified the `[ $missing -gt 0 ]` branch reports all missing prereqs before exit; behavioral test asserts all 3 are counted |
| 10 | openai | LOW | add an integration test that sources the emitted block | accepted — `test_f36_emitted_alias_survives_spaced_repo_root` evals the emitted block under a spaced REPO_ROOT with a stub `claude` |
| 11 | openai | MED | `curl … | sh` trust boundary | rejected-with-reason — pre-existing behavior, out of WP10 scope; the installer already aborts on failure under `set -e` |
| 12 | gemini/openai | LOW | verify-setup now needs Python; degrade gracefully | accepted-and-fixed — `[??]` warning + the rest of verification still proceeds when no interpreter is found |

## External-Code-Review-Findings (Step 3.7, OpenRouter: openai + gemini)

| # | Provider | Sev | Finding | Disposition |
|---|----------|-----|---------|-------------|
| C1 | openai | HIGH | `verify-setup.sh` `env_has` uses `eval "live=\${$key:-}"` — reintroduces shell evaluation at the dotenv boundary F38 removes | accepted-and-fixed — replaced with bash indirect expansion `${!key:-}` (no eval) |
| C2 | openai | MED | install.sh alias strip matches header + bare `}`; fragile to indentation / a stray matching header | accepted-with-reason — the emitted block is generated by install.sh itself (fixed shape: header + unindented `}`); a backup + non-empty guard bound the blast radius. Explicit begin/end markers are a nicer design but would not match alias blocks written by older installs (the migration case F35 must handle); deferred as a polish item. |
| C3 | openai | MED | F34 test only greps `.local/bin`, doesn't run the installer branch + `uv sync` | accepted-with-reason — exercising the uv branch requires `curl … | sh` (network + side-effecting) and a populated cache; out of unit-test scope. The load-bearing logic (PATH prepend order) is asserted structurally. |
| C4 | openai | MED | F37 test runs only the resolver snippet, not the full update-marketplace.sh | accepted-with-reason — a full run needs a populated `~/.claude/plugins/` cache (integration fixture). The resolver + the no-bare-`python -c` static assertion together prove the finding is fixed; the 3 substitutions were mechanically rewritten to `"$PYTHON_BIN"`. |
| — | gemini | — | output truncated (echoed the diff back, no parseable finding) | n/a |

Internal reviewer cascade (spec-reviewer → code-reviewer → doubt-reviewer):
**delegated_to_orchestrator** (the runner has no Agent tool).

## Self-Review (Step 3.6 — 7-item)

1. **Spec Compliance** — PASS. All 6 findings (F33-F38) fixed; ACs met (shell
   tests for missing-prereq/spaced-path/no-python pass; alias lists 13 incl.
   adopt).
2. **Error Handling** — PASS. `$PYTHON_BIN`/`ENV_PYTHON` resolvers exit/ warn
   explicitly when no interpreter; F35 refresh backs up + guards empty output.
3. **Security Basics** — PASS. F38 removes `source` (arbitrary code exec on a
   data file); parser validates key names; shell uses fixed-string match. No
   injection (empirically probed).
4. **Test Quality** — PASS. 13 tests; premise-guards prove the bug, behavioral
   probes prove the fix; ADR-044 `_require_bash` gate (no silent skip).
5. **Performance Basics** — PASS. One extra python subprocess in verify-setup
   (a diagnostic script); negligible.
6. **Naming & Structure** — PASS. `$PYTHON_BIN`/`ENV_PYTHON`/`env_has`/
   `DOTENV_KEYS` are descriptive; comments cite the finding IDs.
7. **Affected Boundaries (ADR-024)** — PASS. Boundary = `.env.local` dotenv
   format. Producer = `validate_env.py`/adopt scaffold + the user's editor;
   consumer = `parse_env_file` (now also verify-setup.sh). Round-trip probe
   run (spaced/`$(…)`/quoted/malformed-key values) — see Confidence Calibration.

Items failed: 0.

## Confidence Calibration (Step 3.8 — touches_io_boundary)

Boundary: `.env.local` (human-edited dotenv) → `parse_env_file` →
verify-setup.sh key-presence logic.

Probes run (REAL):

1. **Injection / spaced-value** — `.env.local` with
   `OPENROUTER_API_KEY=sk-abc with spaces $(touch …/PWNED)`; ran the full
   verify-setup.sh (python3 shim). Finding: no PWNED file, key correctly
   detected as set. (The OLD `source` would have executed `$(touch …)`.)
2. **Malformed-key rejection** — added `BAD-KEY=…`, `123BADKEY=…`; both
   silently dropped (identifier filter); valid `GOOGLE_API_KEY` kept.
3. **Quoted value** — `JELASTIC_TOKEN="quoted value"`; parsed + detected set.
4. **Live-env precedence** — `SUPABASE_ACCESS_TOKEN` (live env, not in file)
   correctly reported set via `env_has`.
5. **F33 prereq-loop under set -e** — drove the install.sh idiom with all 3
   prereqs forced missing; loop ran to completion (`missing=3`, exit 0)
   instead of aborting at the first.
6. **F35 strip-and-rewrite** — stale 12-plugin block stripped, user content
   (`export FOO`, `alias ll`) preserved, backup created.

Asymptote: probes 1-4 (the dotenv boundary) — probe 1 & 2 each found/confirmed
a real defect class (injection, malformed key); probes 3 & 4 found nothing →
two consecutive clean probes → boundary calibrated. Edge case NOT probed:
clone path containing `$`/backtick (F36) — accepted (review #5, rejected with
reason: spaces are the real-world case; broader escaping out of scope).
