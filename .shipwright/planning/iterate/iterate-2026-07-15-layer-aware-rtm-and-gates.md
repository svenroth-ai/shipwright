# Iterate ADR — TT2: layer-aware RTM + `D-orphan` + `D-layer` + D1 hardening

- **Run-ID:** iterate-2026-07-15-layer-aware-rtm-and-gates
- **Campaign:** 2026-07-15-test-traceability-layers (sub-iterate TT2, serial #4 of 9 — after TT1, TT-EV, TT3)
- **Complexity:** medium (classifier said `touches_auth` — prose-keyword FP, overridden; see Risk-flag note) · **change_type:** feature · **spec_impact:** none (framework tooling)
- **Covers gap:** G2 (layer-blind RTM), G3 (orphaned-test detector) + the D1 `COVERED`-≠-tested false-green. Depends on TT1 (manifest), TT-EV (execution evidence in coverage), TT3 (`required_layers` provenance).

## Summary

Turn the TT1 manifest into visible traceability + enforceable detectives. Both capped
files (`rtm_generator.py` 763, `group_d.py` 465 ADR-096) are at their anti-ratchet ceiling,
so all new logic lives in two ≤300 sibling modules and the capped files get only net-neutral
edits (rtm 763→763; group_d 465→464).

1. **RTM layer columns** (`_rtm_layer_columns.py` + rtm_generator net-0). Per-FR
   `Unit | Integration | E2E` glyphs (`ok`/`MISSING`/`n/a`/`—`) appended AFTER `Status`
   (so `test_rtm_reconciled_column`'s positional `_cell(row, 7/8)` indices stay valid).
   Rows match by the namespaced key `split::FR-id` (= manifest namespace), so the
   frozen-grammar fan-out can't mislabel a specific row; a same-id fallback merges
   conservatively (`MISSING` wins). Legend + glossary RTM line updated to match reality.
2. **`D-orphan`** (`_group_d_traceability.check_orphan`). Surfaces `manifest.orphans`
   (test tagged → removed/absent FR); `confirmed`⇒MEDIUM, `possible`⇒LOW, `unmapped`⇒
   informational (never a stale-feature accusation, R4).
3. **`D-layer`** (`check_layer`). Active FR with a required layer lacking an
   executed-passing test (`coverage[layer] != "ok"`, R1). Provenance valve (carry-forward
   #2 / Spec §9): `explicit`⇒FAIL by priority (Must=HIGH/Should=MED/May=LOW) + `any_fail`;
   `legacy`⇒advisory (surfaced, `status="pass"`). `invalid_layers`⇒HARD. Fan-out: an `ok`
   on a display id shared across active namespaces is NOT credited (fail-closed).
4. **D1 hardening** (`_check_d1` + `refine_d1_covered`). `covered` requires a *tested*
   event (`tests.total>0`) AND — for an `explicit`/post-rollout FR — a real non-ambiguous
   manifest test link. Two SEPARATE proofs keyed per-FR; legacy FRs keep the event-only
   proof so the pre-TT8 monorepo does not avalanche.

Bloat baseline NOT ratcheted. New/edited source ≤300; capped files net-neutral. Marketplace
cache re-synced (framework change).

### Notable design decisions (fail-closed reasoning — why no detector false-greens / false-reds)

- **D1 "split not joined" (binding §11).** Event-coverage and test-link-coverage are two
  SEPARATE sets, both required for an explicit FR, intersected per-FR — so an old tested
  event for FR-A + an unrelated tagged test for FR-B can never combine into a false
  `COVERED` (links are keyed to their OWN FR node; a fanned collision link is excluded).
  Can't false-green: a 0/0 event or a missing link drops the FR from `covered`. Can't
  false-red the monorepo: the link proof is provenance-gated to `explicit` FRs only (Spec
  §9 landmine — "hard for FRs created after the field ships"); all 15 monorepo FRs are
  legacy ⇒ event-proof only ⇒ D1 unchanged (empirically: still flags only FR-01.15).
- **D-layer requires executed-passing, not merely present (R1).** `coverage=="ok"` is a
  schema-structural proof of `enabled AND executed=pass`; a green-but-skipped test yields
  `MISSING`. Can't false-green a rotted (skipped) layer.
- **Namespace fan-out fail-closed (carry-forward #1).** A display id owned by ≥2 active
  requirements is ambiguous — one fanned tag credits both. D-layer treats such an `ok` as
  NOT confirmed (`ambiguous_fanout`); D1's link proof excludes collision ids; D-orphan
  cannot false-flag because the collector only orphans a tag resolving to NO live FR in
  ANY namespace. Pinned in both directions.
- **Manifest source (R3).** The detectives read the committed manifest (regenerated by
  `update_compliance` every phase); absent/malformed/non-v2 ⇒ SKIP (no proof ≠ pass), not
  a silent pass. The base+head-regenerating ENFORCING gates are TT5's F11 (this is a
  post-merge, non-blocking detective).

## External-Plan-Review-Findings (Step 3.5)

Folded into the Step-3.7 code-mode adversarial review: the sub-iterate spec is the frozen
mini-plan, and the code-mode panel (GPT-5.4 + Gemini via OpenRouter) adjudicated the design
AND the implementation together (the binding "adversarial panel = hard gate" is code-mode,
brief §"ADVERSARIAL PANEL"). No separate plan-mode round. Marker: `skipped_folded_into_code_review`.

## External-Code-Review-Findings (Step 3.7 — GPT-5.4 + Gemini 3.1 Pro via OpenRouter; both succeeded, not degraded; verdict block → addressed)

| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| O1 | High | D1 required only a tested event, not the mandated manifest test link — a bare event mention could still false-green. | **accepted-and-fixed** — added `refine_d1_covered`: an explicit/post-rollout FR now needs a tested event AND a real non-ambiguous manifest link (two separate per-FR proofs). Provenance-gated so legacy FRs keep the event-only proof (no monorepo avalanche). Pinned by 3 new tests. |
| O2 | High | The D1 "complement" test asserted coverage with no manifest/link — codified the false-green. | **accepted-and-fixed** — reframed as `..._no_manifest_covers_on_event_proof_alone` (legacy path, documented), and ADDED `explicit_fr_tested_event_but_no_link_is_uncovered`, `explicit_fr_with_tested_event_and_link_is_covered`, and `link_for_a_different_fr_does_not_cover_this_fr`. |
| O3 | High | D-layer downgrades `legacy` to advisory (`pass`); binding says D-layer is HARD immediately (no advisory-until-TT3). | **rejected-with-reason** — two different axes. The ORDERING advisory ("advisory until TT3 lands") IS gone: TT3 landed before TT2, so D-layer is hard immediately for `explicit` FRs. The PROVENANCE advisory for `legacy` FRs is a SEPARATE, explicitly-mandated release valve (my brief carry-forward #2 verbatim: "inferred_legacy/defaulted_legacy FRs get ADVISORY (WARN, NOT any_fail)"; Spec §9 landmine: "start advisory (WARN) for pre-existing FRs, hard (FAIL) for FRs created after the field ships"). Reviewer lacked the carry-forward context. |
| O4 | High | "Bare tag removal on a changed test = HARD" not implemented (no base/head diff). | **rejected-with-reason** — that gate is diff-scoped and per R3 belongs to the ENFORCING iterate gate the campaign sequences as TT5 ("iterate gates: removal→orphan + change→cross-layer (F11)"), which regenerates base+head. TT2's Group-D is a post-merge, point-in-time DETECTIVE with no diff; implementing base/head here would duplicate + pre-empt TT5. Delivered the achievable half (confirmed_orphan surfacing). Recorded as an explicit TT5 handoff. |
| O5 | Med | `_suggest_orphan` interpolated an untrusted test path into a copy-paste command (quote-break/injection). | **accepted-and-fixed** — the command now carries only the schema-pinned FR id (`FR-\d{2}\.\d{2}`); the untrusted test path stays in evidence text only. Pinned by an assertion that the path is NOT in the command. |
| O6 | Med | Wiring tests only exercised absent/non-v2 manifest, not the end-to-end audit path with a valid manifest. | **accepted-and-fixed** — added `test_group_d_run_emits_traceability_findings_end_to_end` (writes a valid manifest with an orphan + an explicit layer gap, runs `group_d.run`, asserts D-orphan + D-layer FAIL and detective-only source). |
| G-* | — | Gemini's response was an un-finished chain-of-thought probing `None in _WORST` in `_rtm_layer_columns`. | **verified non-issue** — `coverage.get(l)` returning `None` is correctly excluded (`None in _WORST` is `False`), so a missing layer never renders a stray glyph. No change. |

## Internal review cascade (delegated to orchestrator)

The runner has no `Agent` tool, so the internal `spec-reviewer` (HARD-GATE) → `code-reviewer`
→ `doubt-reviewer` cascade + the independent **Codex** adversarial pass run in the campaign
orchestrator on the pushed diff. `reviews.code.status = "delegated_to_orchestrator"`.

## Self-Review (Step 3.6 — canonical 7-item checklist)

1. **Spec Compliance** — PASS. AC1 (RTM layer columns + `E2E: MISSING` glyph, real-manifest
   probe), AC2 (D-orphan fires on removed/absent, green when every tag resolves live — both
   fan-out directions pinned), AC3 (D-layer fires on missing required layer; legacy advisory
   per provenance), AC4 (D1 false-green regression pinned), AC5 (RED-before→green, ≤300, no ratchet).
2. **Error Handling** — PASS. Manifest load catches OSError/JSONDecodeError → SKIP; each
   detective wrapped in try/except → degraded HIGH finding, never crashes Group D; `.get(...)`
   with defaults throughout.
3. **Security Basics** — PASS. No eval/exec; suggested command carries only a regex-pinned FR
   (untrusted test paths kept out — O5 fix); manifest parsed as data, schema-guarded (non-v2 rejected).
4. **Test Quality** — PASS. 27 new tests across 3 files (D-orphan/D-layer unit + fan-out
   integration via `build_manifest`; D1 hardening + link-proof; RTM columns). RED-before verified
   (new modules didn't exist / old D1 passed the 0/0 case).
5. **Performance Basics** — PASS. Manifest read once per check; collision set is one O(n) pass; no
   N+1; RTM index built once per render.
6. **Naming & Structure** — PASS. Two cohesive siblings (`_group_d_traceability`, `_rtm_layer_columns`)
   named for their capped parents; pure check functions (manifest dict → tuple) separated from the
   IO/wiring, so tests hit the pure core.
7. **Affected Boundaries (ADR-024)** — PASS. Producer = TT1 `test_links` manifest
   (`test-traceability.json`); consumers = `group_d` (D-orphan/D-layer/D1) + `rtm_generator`
   (layer columns). Real producer→file→consumer round-trip probed (see Confidence Calibration).

## Confidence Calibration (Step 3.8 — empirical probes, asymptote heuristic)

Boundary: the machine-generated/consumed `test-traceability.json` manifest (a serialized-format
producer + two consumers) — `touches_io_boundary`.

- **Probes run (5):** (1) external code review found the D1 missing-link false-green →
  **fixed** (`refine_d1_covered`); (2) round-trip — `build_manifest` over a synthetic two-namespace
  fixture (FR-03.01 active in 01-a, removed in 02-b; FR-09.09 removed) → `check_orphan`: FR-03.01
  NOT orphaned (live in a namespace), FR-09.09 IS → clean; (3) real monorepo committed manifest →
  `group_d.run`: D-orphan pass (0 orphans), D-layer pass (15 legacy advisory, no any_fail), D1
  flags only FR-01.15 (has NO covering event — pre-existing, my hardening did not change it) → clean;
  (4) real monorepo → `rtm_generator.generate`: header carries `Unit | Integration | E2E`, FR-01.01
  renders `MISSING | — | —`, FR-01.15 `— | — | MISSING` (glyphs, not counts, AC1) → clean; (5)
  non-v2 / absent manifest → `load_manifest` returns None → both detectives SKIP (fail-closed) → clean.
- **Findings:** one bug (probe 1) → fixed → probes 2–5 clean. Two+ consecutive clean probes ⇒
  **asymptote reached; boundary calibrated.**
- **Edge cases not probed (acceptable):** the diff-scoped bare-tag-removal HARD gate (O4 — TT5
  scope, needs base/head); a maliciously hand-edited committed manifest (churn-regenerated every
  phase + audit_staleness/Group-H catch divergence; R3 enforcement is TT5); multi-split coverage
  precision beyond the fail-closed collision rule (inherited TT1 fan-out limitation).

## Risk-flag note

The classifier returned `touches_auth` (`prior_source: keyword`, `history_n: 0`) — a
prose-keyword false positive: the spec/manifest text mentions no auth code, and the diff
touches ZERO auth (two compliance sibling modules + net-neutral edits to RTM/group_d + tests +
a glossary line). Overridden to `medium` per the documented `classify_complexity` message-keyword
FP pattern; F11 recomputes risk from diff-predicates, which will not re-raise it.
