{
  "section": "iterate-2026-09-20-shared-rollout-commit-resolver",
  "review": [
    {"severity": "low", "category": "architecture", "file": "shared/scripts/tools/verifiers/_rollout_resolution.py", "line": 50, "finding": "Local _GIT_TIMEOUT_SECONDS re-declaration duplicated git_helpers's own constant.", "suggestion": "Import _GIT_TIMEOUT_SECONDS from git_helpers instead of re-declaring it.", "disposition": "fixed"},
    {"severity": "low", "category": "architecture", "file": "shared/tests/test_project_gate_rollout.py", "line": 190, "finding": "The shared primitive's only committer-epoch-postdates-cutoff test lived in a family test file, white-box-patching another module's private import.", "suggestion": "Move the test into shared/tests/test_rollout_resolution.py.", "disposition": "fixed"},
    {"severity": "low", "category": "security", "file": "shared/scripts/tools/verifiers/_rollout_resolution.py", "line": 79, "finding": "epoch widened from a module-private int constant to an unvalidated parameter; a malformed value could reopen the approxidate misparse hazard or raise TypeError out of a never-raising function.", "suggestion": "Add an isinstance(epoch, int) guard at function entry, returning None on a malformed epoch.", "disposition": "fixed"},
    {"severity": "low", "category": "readability", "file": "shared/tests/test_rollout_resolution.py", "line": 35, "finding": "New test file's real-git harness duplicates the identical harness already present in 2+ other shared/tests files; a pre-existing repo-wide convention, not a regression introduced by this diff.", "suggestion": "Lift the shared harness into shared/tests/conftest.py in a separate follow-up commit.", "disposition": "disclosed — out of scope for this behavior-preserving refactor (touches 6-8 unrelated files); recorded in the iterate ADR as a known limitation"},
    {"severity": "low", "category": "correctness", "file": "shared/tests/test_rollout_resolution.py", "line": 158, "finding": "test_resolve_rollout_commit_none_for_a_shallow_clone's discriminating assertion is is_shallow(...) is True, not the trailing resolve_rollout_commit(...) is None.", "suggestion": "Add a docstring making the discriminating assertion explicit.", "disposition": "fixed"}
  ]
}
