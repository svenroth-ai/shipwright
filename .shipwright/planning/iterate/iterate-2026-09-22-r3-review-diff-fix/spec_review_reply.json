{
  "stage": "spec-compliance",
  "verdict": "REJECT",
  "spec_citations": [
    {
      "spec_ref": "R3-review-diff-fix.md:L19-25 (name every unit-scoped call site, incl. run_dir)",
      "divergence": "Round 5's run_dir re-derivation fix covers every READ site (pin block L401, promote-rows L470, ship L493, REJECT branch L558, 3g L580/589/597/599 -- all correctly re-derive run_dir immediately before reading) but NOT the fires WRITE site at L356-357. The doc's own prose (L243-249) states the fires judgement forces a fresh Bash call before pin runs -- meaning the fires digit is necessarily assigned in a NEW call, past the nearest run_dir= assignment (L259, 97 lines earlier). echo \"$fires\" > \"$run_dir/fires\" therefore targets \"/fires\" on the happy path. Consequence: the new fail-closed guard at L405 ([ \"$fires\" = \"1\" ] || [ \"$fires\" = \"0\" ] || STRICT-STOP) fires on the HAPPY PATH for every unit -- 3f-bis never reaches pin, the cascade, or 3g. Fails closed (no unpinned merge) but makes the review lane unexecutable. Same class, lower confidence (no doc-asserted boundary there), applies to the pr_json write at L291 and the diff_head write at L330-333 -- same run_dir=L259 dependency across 30-70 lines of intervening prose.",
      "diff_location": "campaign-mode.md:356-357 (primary); 289/291/331 (extend for consistency); test coverage needed at test_campaign_step_3f_bis.py",
      "kind": "unfaithful"
    }
  ],
  "summary": "REJECT, single narrow finding: round 5's run_dir re-derivation rule was scoped to READS only; the fires WRITE site (L357) still dereferences the stale run_dir from L259, defeating the fix's own purpose on the happy path. ALL other round-5 claims independently VERIFIED genuine: pr_json dual-write/re-read (L291/406/411-413) with corrected prose; fires fix mechanism sound (literal fires=1/fires=0 assignment at L356, not a bare $fires read -- functionally equivalent to what was asked, just via an assignment line rather than substitution); fail-closed re-read guard present and test-guarded (L405); ADR Decision body, architecture.md:536, and CLI help all correctly describe the 3-mode $unit_wt-scoped design; new bloat exception for test_campaign_step_3f_bis.py (540 lines) mechanically correct with a complete separate ADR; zero regressions in previously-settled rounds (spot-checked ship-before-legacy-write, bounded-wait re-checks, gh pr merge guard, pin-diff path equality, jq hardening, mkdir -p, misattribution fixes, and the Python-side hardening in review_attribution.py). Minimal fix: extend the round-5 rule from reads to reads-AND-writes; add a run_dir= rebuild immediately before the fires write (and, for consistency, the pr_json and diff_head writes); add a test asserting run_dir= precedes the fires write specifically.",
  "recorded_from": "a30cfbfeda6d7ae1a",
  "run_id": "iterate-2026-09-22-r3-review-diff-fix"
}
