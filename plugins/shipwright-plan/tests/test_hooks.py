"""Tests for shipwright-plan hooks.

The SessionStart capture_session_id.py hook is shared across all
plugins and tested in shared/tests/test_capture_session_id.py.
This file exists as a placeholder for future plan-specific hook tests
(e.g. write-section-on-stop.py).
"""
