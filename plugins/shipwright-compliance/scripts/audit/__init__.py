"""shipwright-compliance detective audit (plan v7 Option Z).

On-demand cross-artifact consistency audit surfaced via
``/shipwright-compliance``. Complements the preventive canon checks (run at
phase boundaries) and the reactive Phase-Quality Stop hook (run at every
session end).
"""
