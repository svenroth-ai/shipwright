# F5 — Write Test Results JSON

Write latest-run state to `shipwright_test_results.json`:

> **`run_id` is load-bearing, not decoration.** An iterate does not commit this
> file, so the copy a worktree is checked out with is `HEAD`'s — the PREVIOUS
> run's, sitting here before your F5 writes a byte, and a block that does not name
> its own run cannot be told apart from it. Since `trg-81fbf8ed` the ledger and
> F0.5 gates REFUSE a block naming another run, or naming none. Carry the same two
> blocks in the **F5c entry** (`references/F5c.md`) — a per-run path nothing in the
> integration touches. (`restore_derived_to_head` used to rewind this file mid-run
> as well; trg-ad29a709 carved it out, which closes that second route and not the
> checkout above.)

```json
{
  "iterate_latest": {
    "run_id": "{run_id}",
    "date": "{YYYY-MM-DD}",
    "unit": { "status": "passed|failed|not_run", "passed": N, "total": N },
    "integration": { "status": "passed|failed|skipped|not_run", "passed": N, "total": N },
    "pgtap": { "status": "passed|failed|skipped|not_run", "passed": N, "total": N },
    "e2e": { "status": "passed|partial|skipped|not_run", "passed": N, "total": N },
    "design_fidelity": { "status": "passed|partial|skipped|not_run", "passed": N, "total": N },
    "smoke": { "status": "passed|skipped|not_run" },
    "degraded": []
  }
}
```

## Test Completeness Ledger block (small+)

At small / medium / large complexity, also write the
`iterate_latest.test_completeness` block — the machine-readable form of the
Step 7.5 ledger. The F11 verifier `check_test_completeness_ledger`
(`shared/scripts/tools/verifiers/iterate_checks.py`) reads it and STOPs the
run on any gap. At trivial complexity the gate is skipped; record
`{"status": "n/a", "justification": "..."}` so the F11 summary still shows a
line.

```json
"test_completeness": {
  "status": "complete",
  "behaviors": [
    { "behavior": "untested_testable>0 → FAIL", "disposition": "tested",
      "evidence": "test_verify_iterate_finalization.py::test_...positive PASSED" },
    { "behavior": "send live email", "disposition": "untestable",
      "reason_code": "requires-prod-credential",
      "reason": "needs the real SMTP secret, absent from CI" }
  ],
  "counts": { "testable": N, "tested": N, "untestable": M, "untested_testable": 0 },
  "enumeration_basis": { "acs": N, "covered_acs": N }
}
```

Rules the verifier enforces (fail-closed, ERROR):

- `status` ∈ {`complete`, `n/a`}. `n/a` is valid only at **small** (with a
  non-empty `justification`) or trivial (auto). At **medium/large** `n/a` is
  rejected — a medium+ iterate has testable behavior by definition, so it
  MUST enumerate (`status=complete`).
- `status=complete` needs ≥ 1 behavior; each `disposition` ∈ {`tested`,
  `untestable`}. **No "deferred" / "untested" / "acceptable"** — that escape
  hatch is exactly what the gate kills.
- `tested` rows must cite `evidence`; `untestable` rows must cite a
  `reason_code` from the closed vocabulary (see
  `references/confidence-anti-patterns.md`).
- `counts.untested_testable` must be `0`.
- `enumeration_basis` (optional but recommended at medium+): if `acs >
  covered_acs`, the gate fails — guards a vacuous pass via under-enumeration.
- **`category` (optional per behavior)** — a behavior may carry
  `"category": "integration"` to mark it as INTEGRATION coverage (a real-scenario
  test proving cross-component pieces compose). At **every complexity**, when the
  iterate touches FRAMEWORK cross-component machinery (the `cross_component` risk
  flag), the F11 verifier `check_integration_coverage` RECOMPUTES the flag from the
  diff and STOPs unless ≥ 1 behavior is `category:"integration"`
  (iterate-2026-08-01-coverage-gate-recompute-order — it was medium+ until then,
  which meant the recompute stood down in exactly the missed-detection case it
  exists to catch). **This is the one ledger obligation `small` does not buy you
  out of**, so record the behavior here even on a small run. Reference pattern:
  `shared/tests/test_parallel_merge_cascade_integration.py`. See SKILL.md Risk
  Taxonomy + `references/confidence-anti-patterns.md` (Integration Stopping Rule).

## Per-test execution evidence (traceability R1 / G5)

`iterate_latest` above is the **aggregate** per-layer count. It is layer-blind
about *which* test proved *which* requirement, and a silent `test.skip` can leave
it green. For requirement→test traceability, finalization also emits **per-test**
execution evidence so `coverage[layer]=ok` can require a tagged test that is
enabled AND executed=pass (not merely present).

Drop this run's raw runner reports under
`.shipwright/compliance/evidence/` (`junit.xml`, `playwright.json`,
`vitest.json`) — the compliance `test_links` collector calls the
`execution_evidence` reader (`_execution_evidence_io.refresh_index`) on regen to
normalize them into `.shipwright/compliance/test-evidence-index.json` and join
`status`/`executed` onto each manifest test link. The reader is **fail-closed**:
a missing report resolves to `not_run` (never a self-reported pass), a
green-but-skipped required-layer test is `MISSING`, and an out-of-vocab evidence
value is coerced, not trusted. A layer that genuinely cannot run in an
environment needs an explicit **expiring waiver** (honored while valid, fails
when expired) — never self-reported success.

**Emit-side (TT5) — clear before the run, stage after.** Use the shared
`evidence_drop` CLI so the enforcing **cross-layer F11 gate** reads fresh,
HEAD-attributable evidence (it trusts the index ONLY when a provenance sidecar
proves this run staged those reports). Before F0/F0.5 runs the tests, clear stale
evidence; after they produce reports, stage them:

```bash
# once, before this run's test execution (F0/F0.5) — never inherit a prior run's reports:
uv run shared/scripts/lib/evidence_drop.py clear --project-root .
# after the runners produce reports (F0.5/F5) — records run_id + head_commit provenance.
# A single pytest process's report (the common case for an iterate touching one root):
uv run shared/scripts/lib/evidence_drop.py stage --project-root . \
  --run-id {run_id} --head-commit "$(git rev-parse HEAD)" \
  --junit <path> --playwright <path> --vitest <path>
```

> **Repo has 18 pytest test-roots (ADR-044) — `--junit` is REPEATABLE (R1a, E-B).**
> One pytest process can never emit one `junit.xml` spanning more than one root, so a
> change that touched tests in several roots needs one report PER root, each with its
> OWN base (the dir that pytest process ran in, for cross-root id-rebase — see
> `_evidence_readers.norm_path`):
>
> ```bash
> uv run shared/scripts/lib/evidence_drop.py stage --project-root . \
>   --run-id {run_id} --head-commit "$(git rev-parse HEAD)" \
>   --junit =<path-to-shared-tests-report.xml> \
>   --junit plugins/shipwright-compliance=<path-to-that-plugins-report.xml>
> ```
>
> The single-report form above (`--junit <path>`, no `=`) stays valid **only when it
> is the sole `--junit`** — base defaults to the project root. As soon as a SECOND
> `--junit` is given, every one of them must carry an explicit `base=` (an empty base
> is spelled `=<path>`); a bare path among several is REJECTED, not silently defaulted.
> Every root's report is staged **byte-identical** as `junit-01.xml` .. `junit-NN.xml`
> — the `_provenance.json` sidecar records each report's `base` alongside its name.
> A repo-wide evidence pass across every discovered root (not just the ones this
> iterate touched) is `scripts/run_full_suite_evidence.py` (`--run-id`, `--head-commit`
> defaults to `git rev-parse HEAD` in `--project-root` when omitted — Stage-2 review:
> an unresolvable-and-unset commit refuses to run rather than stage evidence
> `fresh_evidence` would hard-reject wholesale) — it derives BOTH the root list and
> each root's base from `conftest.py::discover_test_roots` (no second hand-maintained
> list), drives every root one pytest process at a time, and stages them all together
> via the same `evidence_drop.stage_reports` Python API this CLI wraps (it calls the
> function directly, not the CLI — its own repeated same-base `''` reports, one per
> root that rebases at the project root, are exactly the case Stage-2 review found the
> CLI wrongly rejecting; the CLI's duplicate check is now scoped to an exact
> `(base, path)` pair, matching what the API always allowed).

Order matters: `clear` (before tests) → run tests → `stage` (copies reports + provenance).
You do NOT rebuild the index by hand — the cross-layer gate REGENERATES it from the staged
raw reports itself (the R3 "regenerate, don't trust" rule applied to evidence), and the F5
compliance regen also rebuilds it. The gate trusts the result ONLY when the provenance
run_id matches this run AND the recorded `head_commit` is an ancestor of / equal to the
commit being verified — so a stale, planted, or foreign-branch index can never credit a
pass. Without `--head-commit` the evidence is not HEAD-attributable and is rejected.

> **Staging is REQUIRED (not optional prose) for a behaviour-changing medium+ iterate.**
> The cross-layer gate is fail-closed: a required layer with no staged executed-passing
> evidence reads `MISSING` and BLOCKS. So if this iterate changes an FR's behaviour, you
> MUST `stage` the run's passing reports (with `--head-commit`), and either the report
> paths must already be **project-root-relative**, OR you name the runner's actual
> subdir via `--junit <base>=<path>` so the ids rebase and still join the manifest (R1a
> closed the earlier A6 "per-runner `bases` not threaded here" deferral — a per-root
> base is now threaded all the way through `refresh_index` and the cross-layer gate). A
> pure-tooling / no-FR-delta iterate needs no staging (the gate does not fire).

## Stamp the record with the state it describes (always — LAST step of F5)

**Run this after every other block above has been written.** It must be the final F5
step: it edits `shipwright_test_results.json` in place, so any later whole-file
`Write` in this phase would silently drop `source_state`. That is not hypothetical —
this record has already lost `coverage.total` exactly that way once.

```bash
uv run "{shared_root}/scripts/tools/stamp_test_results.py"   --project-root "{project_root}" --run-id "{run_id}"
```

Writes the top-level `source_state` block: the run id, the HEAD commit, and whether
tracked files were modified. `--project-root` **must be the worktree root** — the
same tree the record was written to. The tool ignores the shell's cwd and both trees
contain this file, so a main-root path here silently stamps the main tree's record
with the main tree's HEAD.

Expect `dirty: true` here: at F5 the whole iterate is still uncommitted (F6 has not
run), so a modified tree is the norm, not an anomaly. The record itself is excluded
from that calculation, so it is never the *cause* of the flag.

**Never hand-write the commit or the modified flag into the JSON.** The tool reads
them from git, which is what makes them evidence rather than a claim — the same
reason a self-declared `"mode": "standalone"` was found not to be a control (card
`trg-4d5b6a56`, FR-01.10). The run id itself is declared, not verified. Re-running
the whole of F5 is safe: the stamp is idempotent.

> **Why the next steps run BEFORE the commit (F6):** the finalization
> script and the `iterate_history` append write artifact files tracked
> in the repo. Running them after F6 would dirty the working tree
> immediately after committing and force a `git commit --amend` —
> which conflicts with the "never amend" rule in the global
> `CLAUDE.md`.

> **If the diff touches the CI trust boundary, record the acknowledgement HERE**
> — after this final results-file write and before the F6 commit stages it:
>
> ```bash
> uv run shared/scripts/tools/record_ci_supplychain_ack.py \
>   --project-root . --run-id {run_id} \
>   --consistent-with "<ADR-NNN | iterate-YYYY-MM-DD-slug | #NNN>" \
>   --statement "<what this change does to the CI trust boundary>"
> ```
>
> It writes `.shipwright/planning/iterate/<run_id>/ci_supplychain_ack.json`, which
> F6's directory-level add stages. It does **not** write into
> `shipwright_test_results.json`: that file is a derived snapshot the iterate commit
> may not carry, and parking the ack there made two ERROR-severity F11 checks
> unsatisfiable at once (iterate-2026-07-28-ci-ack-per-run-home). The tool
> fingerprints the WORKING TREE, so any edit to a CI file after recording
> invalidates the ack — re-run it, that is the intended cost.
