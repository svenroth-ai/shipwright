# F1 — Drift Check

```bash
uv run "{shared_root}/scripts/artifact_sync.py" \
  --project-root "{project_root}" --ref "HEAD~1..HEAD"
```

If drift detected, update specs. If iterate spec exists (medium+),
check off completed ACs and update status to `implemented`.
