# B4.5 Smoke-Test — Tier 2 (delete me)

Throwaway file to verify that an internal PR (author `svroch`, no sensitive paths,
no `needs-review` label) is classified **Tier 2** by the `pr-review.yml` `decide`
job, so the `review` job is **skipped** and the required `PR Review` status check
still passes green. This PR is closed without merging.
