PR-review gate: `is_safe_to_skip_review` no longer grants blanket skip-safety by directory prefix. `.shipwright/compliance/`, `.shipwright/agent_docs/iterates/`, and `.shipwright/agent_docs/runtime/` are removed from skip-safety entirely; `CHANGELOG-unreleased.d/` is anchored to its real closed write shape. Closes the gap a deploy-time security gate's unauthenticated trust in `.shipwright/compliance/ci-security.json` exposed.
