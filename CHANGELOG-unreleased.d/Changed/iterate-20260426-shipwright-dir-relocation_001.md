**shipwright-security: report directory moved from `securityreports/` to `.shipwright/securityreports/`** for convention parity with `.git/`, `.claude/`, `.vscode/` — Shipwright artifacts now live under a single hidden directory at the project root. `run_scan_and_report.py` writes to the new location and gains an `_emit_legacy_dir_notice` helper that prints a one-time stderr message when a stale legacy `securityreports/` directory is detected (no auto-move, never blocks). `.gitignore` updates target `/.shipwright/` (covers all current and future Shipwright artifacts in one entry); legacy `/securityreports/` entries are recognised as already-present so migrating projects don't get double-written. Scanner self-exclusion via `_DEFAULT_EXCLUDES` adds `.shipwright` and keeps `securityreports` for one release cycle. SKILL.md, `references/ci-integration.md`, and the `generate_security_report.py` argparse help all updated accordingly. Aikido path untouched; WebUI / orchestrator / v2 schema unaffected.
