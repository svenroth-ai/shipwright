Compliance evidence now ships with the release. The seven documents under `.shipwright/compliance/` are recomputed and checked in through the release PR instead of standing frozen on the default branch — they were already recomputed at release, just one step too late to be staged. Each markdown document's `Source-State:` line now names the fixed point it describes (`base=<commit>`, and `release=<tag>` when it shipped with one) rather than implying it is live.
