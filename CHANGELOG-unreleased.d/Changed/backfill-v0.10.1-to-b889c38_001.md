**Security target operating model documented as CI-only (Phase 0 / I1 / T1).** Removes lingering claims that `/shipwright-run` auto-invokes security from `plugins/shipwright-security/skills/security/SKILL.md`, `plugins/shipwright-test/skills/test/SKILL.md`, `docs/guide.md` Chapter 4, and `docs/hooks-and-pipeline.md` — that auto-trigger was already decoupled in iterate `sec-report-and-orchestrator-decouple` (2026-04). Pipeline phases now explicitly state security runs in CI (`.github/workflows/security.yml`) or standalone via `/shipwright-security`, never auto-invoked. `grep -r "Invoked by /shipwright-run" plugins/shipwright-security/` is empty (commit `b9f2dbf`).
