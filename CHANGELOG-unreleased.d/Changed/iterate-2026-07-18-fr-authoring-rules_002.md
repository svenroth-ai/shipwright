`/shipwright-adopt` semantic-enrichment guidance now requires capability-level labels and plain business-language descriptions; the previous instruction to keep descriptions technical and to 'describe what the code does' has been replaced rather than appended to, so the two rules no longer contradict each other. `/shipwright-project` gains the same plain-language rule and no-implementation-how fence on its IREB template.
