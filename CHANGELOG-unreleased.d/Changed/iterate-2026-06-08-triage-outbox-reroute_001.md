Idle-main background triage producers (plugin-sync Stop hook, compliance audit, `triage_add`) now append to a per-tree GITIGNORED `.shipwright/triage.outbox.jsonl` buffer instead of the tracked `triage.jsonl`, so an idle main checkout no longer accrues uncommitted triage drift; `triage.read_all_items` returns the tracked ∪ outbox union so consumers still see background findings immediately.
