The shipwright-grade HTML report is now a guided, plain-language walkthrough for non-experts: each control leads with a plain question and a one-line explanation, with an expandable "why it matters" that uses a concrete everyday scenario and an honest "what improves with Shipwright" (the open-standard anchor + provenance move into that expandable). n/a controls are reframed as "what your repo is missing", and the call-to-action offers two next steps — understand it (the Masterclass) and fix it (`/shipwright-adopt`).
