**`/shipwright-run` reframed as "Pipeline Initializer & Phase Coordinator" (Phase 0 / I1 / T2).** The word "Orchestrator" was overdimensioned at the command level — a single command initialises configs, prints the launch card, and starts the first phase, that is not orchestration. Per Phase-0 Cluster 2a, "Orchestrator" is dropped from the command-level description in `plugins/shipwright-run/skills/run/SKILL.md`, `docs/guide.md`, `docs/hooks-and-pipeline.md`, and the `README.md` Run section, while "orchestrate" stays as a verb for the pipeline-as-a-whole concept and `orchestrator.py` remains as a script name (commit `5b38edd`).
