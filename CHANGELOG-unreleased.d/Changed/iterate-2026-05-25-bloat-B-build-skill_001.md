- (refactor) Split `plugins/shipwright-build/skills/build/SKILL.md` from 1162 LOC into a 291-LOC Kern + 8 NEW topical reference files (`references/tdd-tests.md`, `migrations-apply.md`, `browser-verify.md`, `code-review.md`, `section-state.md`, `autonomous-loop.md`, `error-handling.md`, `first-actions.md`) alongside the pre-existing reference set. Kern preserves the intro banner verbatim + Step 0..12 headers with trigger conditions + the new `## Phase Index — Where the prose lives` table. `{plugin_root}`/`{shared_root}`/`{project_root}` placeholders preserved verbatim across all references (37 occurrences). `shipwright_bloat_baseline.json` entry for the build SKILL.md removed per cleanup invariant rule (a). Campaign B sub-iterate B1.build (ADR-029-aware).
