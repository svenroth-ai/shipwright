iterate complexity classifier: the history-calibrated fall-through default is capped at `small` (was `medium`). A fall-through may lower a tier but never raise it into one that carries an iterate spec, mini-plan, approval gate and external plan review — those need positive evidence (scope keyword, risk flag, cross-split, or the Stage-2 Repo Scout). This breaks a self-reinforcing loop: the prior was the median of FINAL complexities while a no-keyword run's final complexity WAS the prior, measured at 84% medium over 50 recorded runs.
