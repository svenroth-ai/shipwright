**Iterate iterate-history-file-per-iterate (2026-04-23)** — eliminates both parallel-iterate merge hotspots structurally. Iterate entries now live per-file at `agent_docs/iterates/<run_id>.json` (new `shared/scripts/lib/iterate_entry.py` + `shared/scripts/tools/append_iterate_entry.py`). Changelog bullets now drop per-iterate into `CHANGELOG-unreleased.d/<category>/<run_id>_NNN.md` (new `write_changelog_drop.py`) and aggregate at release time (new `aggregate_changelog.py`) with structural insertion above the first existing version section (no more blind-prepend corruption). The append tool holds the run-config lock for the full migration+append+retention transaction so same-worktree concurrent finalizes serialize cleanly; parallel branches never touch the run-config (migration output is deterministic — no timestamps — so two branches produce bitwise-identical diffs and git auto-merges them). Legacy `iterate_history` arrays migrate on first contact with normalized legacy tolerance (Feature→feature, feat→feature, fix→bug); invalid + duplicate rows quarantine to `agent_docs/iterates/_quarantine/` with a loud handoff + verifier WARN so losses don't go silent. Dual-mode reads across iterate_checks + spec_checks + generate_session_handoff keep legacy-array-only AND dir-only projects working without drift; 16 new dir-only tests + integration test with first-migration-on-both-branches prove cross-scenario merge cleanliness. `/shipwright-adopt` artifact_writer initializes fresh projects directly with the new structure (`agent_docs/iterates/{,_quarantine/,_meta/}/.gitkeep` + `CHANGELOG-unreleased.d/<category>/.gitkeep`) and stamps `_iterate_migration_state: complete` so no first-touch migration ever runs. **Test totals:** shared 164/164 + write/aggregate 30/30 + dual-mode 16/16 + retention-race 4/4 + adopt 3/3 + integration 3/3 — 220 new or updated tests, all green. Planned via 2 external LLM review passes (GPT-5 + Gemini-3.1 via OpenRouter) with 37 findings applied. (ADR-056)
