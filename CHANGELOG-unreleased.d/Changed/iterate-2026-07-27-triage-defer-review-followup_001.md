Every entry in `triage_cli.py list` now starts with an explicit `[open]` or `[deferred]` marker. Previously only deferred entries were marked and only by a heading above them, so a line could not be classified on its own — and the marker could be imitated by text arriving from the code host. One-line fields in the listing are also truncated at 120 characters, matching the rendered inbox document.
