shipwright-security Step 7.5 finalizer now stages the full compliance write-set (compliance MDs, the appended grade snapshot in `shipwright_events.jsonl`, `shipwright_compliance_config.json`, and a direct `.shipwright/triage.jsonl` append) instead of only the MDs, so it no longer leaves the working tree dirty — a stray dirty events log otherwise aborted the next `ensure_current` pre-merge refresh and discarded the grade snapshot.
