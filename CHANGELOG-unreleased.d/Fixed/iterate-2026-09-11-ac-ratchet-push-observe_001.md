The AC coverage ratchet CI gate (P3.7 feeder a) now also runs on push to main, not just pull_request, and blocks if the baseline file itself grew since the push's pre-image — so a same-PR self-grandfathered entry surfaces as red on main the very next push instead of never being observed again after merge, though that run is detective rather than a required check and self-clears afterward (a later regression of an already-baselined AC remains a tracked, open follow-up)
