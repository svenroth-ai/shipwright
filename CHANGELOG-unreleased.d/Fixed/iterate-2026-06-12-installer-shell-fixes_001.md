Installer/shell scripts: `install.sh` no longer aborts at the first missing prerequisite under `set -e`, exports the real uv install dir (`~/.local/bin`), lists all 13 plugins (incl. shipwright-adopt) in the `shipwright()` alias with space-safe quoted paths and refreshes a stale alias in place; `update-marketplace.sh` resolves `python3`/`python`/`py` instead of a bare `python`; `verify-setup.sh` parses `.env.local` via the canonical dotenv reader instead of `source` (no shell-injection of spaced values). (deep-audit WP10, F33-F38)
