The grade report's honest-ceiling disclaimer is now mode-aware: an authoritative grade (read from a repo's own Shipwright records) no longer falsely calls itself a "heuristic estimate from the outside" — it states it measures control discipline, not behaviour-correctness. The internal "BP-2" codename is dropped from the change-reconciliation provenance line, so the report is codename-free.
