Hardened iterate finalization tooling (3 robustness fixes): the F11 spec-impact verifier now accepts the FR-gate's `none_reason` as a justification for `spec_impact=none`; F0.5 `surface_verification` rejects a compound/shell runner fast with an actionable message instead of a cryptic exit-127; and the architecture-drift sanity test no longer false-FAILs on a post-release tree whose `decision-drops/` holds only non-arch drops.
