Parallel iterates no longer conflict on the curated agent-docs `architecture.md` + `conventions.md`: their bullet-prepended `## …Updates` / `## Learnings` sections now use the `merge=union` driver (a distinct `CURATED_DOC_UNION_PATHS` category beside the JSONL append-logs), so both prepended bullets survive automatically — honored by GitHub server-side too. Closes the last piece of the auto-merge churn cascade the regenerate-at-merge resolver couldn't touch (curated prose is not regenerated).
