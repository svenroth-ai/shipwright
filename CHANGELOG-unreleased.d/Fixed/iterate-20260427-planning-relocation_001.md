**.gitignore**: `shared/templates/shipwright_*_config.json` was unintentionally matched by the project-level `shipwright_*_config.json` ignore rule, leaving the canonical sync-config template untracked. Added a `!shared/templates/shipwright_*_config.json` exception so the template ships with the framework. Also fixed a Sub-Iterate-C path-construction regression in `plugins/shipwright-build/tests/test_integration.py` (`.shipwright/.shipwright/planning` -> `.shipwright/planning`).
