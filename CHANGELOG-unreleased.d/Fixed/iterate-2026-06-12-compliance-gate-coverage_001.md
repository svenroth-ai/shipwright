Two structurally-inert compliance gates now fire (deep-audit 2026-06-10 WP3). F20: `audit_detector.run_all`'s default group set added `"H"`, so the Group H bloat-policy detective audit — the net all three preventive bloat gates hand off to — runs by default instead of zero checks; the `audit_compliance_on_stop` full-coverage-before-dismiss gate's expected set widened from A-G to A-H to match. F21: `spec_checks.check_s4_fr_preservation` no longer raises `TypeError` (the window builder joined 6-line slices — lists — instead of strings), which had aborted the whole spec category whenever a removed FR id was still referenced on an unchanged line.
