iterate F11 traceability gate `cross_layer_coverage`: a behaviour-changed FR (spec/AC/FR delta, not source inference) must have an executed-passing tagged test at each required layer; a pure refactor triggers nothing. Evidence is regenerated from this run's staged runner reports with run_id + HEAD-ancestor provenance, so a skipped/stale/foreign test can never credit a pass (traceability TT5, gaps G4/G7/G8).
