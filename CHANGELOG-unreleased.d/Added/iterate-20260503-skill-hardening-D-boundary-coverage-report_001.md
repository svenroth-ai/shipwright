test: Boundary Coverage Report -- new tool plugins/shipwright-test/scripts/tools/boundary_coverage_report.py + /shipwright-test --report-boundary-coverage flag (Step 3.95). Scans .shipwright/planning/iterate/**/*.md for `## Affected Boundaries` sections (introduced by ADR-024), correlates with shipwright_events.jsonl commits, emits markdown + JSON report with Coverage Summary, Per-iterate breakdown, and Drift Signals (iterates touching IO files but lacking the section). Markdown table parser hardened against all 8 probe categories from references/boundary-probes.md (BOM, CRLF, non-ASCII, alignment markers, whitespace, empty section, exact heading, missing). Smoke test on live tree: 17 specs scanned, 4 with boundaries (campaign A/B/C/D), 7 drift signals on pre-A specs (ADR-027)
