FR `Layers` declaration: functional requirements now declare the test layers they must be covered at (`unit`/`integration`/`e2e`). `/shipwright-project` and `/shipwright-plan` emit the heuristic defaults (UI⇒e2e, CRUD⇒integration, every FR⇒unit) with author override; `/shipwright-adopt` infers them from a reverse-engineered FR's detected surface, marked `(inferred)` so adopted FRs stay advisory rather than hard-gated.
