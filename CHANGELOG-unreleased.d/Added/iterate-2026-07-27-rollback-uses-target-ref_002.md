**A rollback that fails part-way now names the state and stops.** It says plainly that neither the new nor the previous version is confirmed running, what it last attempted and what that returned, and what it had already changed — then halts with a distinct exit code so nothing continues unattended. A rollback refused *before* the target was touched is reported separately, and says nothing was changed.
