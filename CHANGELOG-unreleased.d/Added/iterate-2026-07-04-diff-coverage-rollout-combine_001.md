Diff-coverage now measures the **whole monorepo** — every plugin's `scripts/` plus `shared/` and `integration-tests` are combined (via `combine_coverage.py`, per-plugin `[paths]` remap) into one repo-relative `coverage.xml`. The tracked repo-wide `coverage.total` (80.2%) is populated by `record_coverage_total.py`, lighting the previously-dormant **W4** coverage verifier against a calibrated `shipwright_test_config.json.coverage.min` = 70 anti-ratchet baseline. Still informational — no grade effect, no CI fail-gate (roadmap Phases 3–4).
