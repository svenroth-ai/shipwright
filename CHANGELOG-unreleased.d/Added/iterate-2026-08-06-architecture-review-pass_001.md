External review: a fourth mode, `--mode architecture`, asking whether a change should be built at all rather than whether its plan is sound. It runs as a second call inside the existing review step (`/shipwright-iterate` Step 3.5, `/shipwright-plan` Step 5a), reads a short architecture brief instead of the plan — options listed without the reasons any were rejected — and stops to ask you when either reviewer says the approach is wrong.
