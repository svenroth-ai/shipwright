/shipwright-plan: the gates Step 9 listed as "verification gates (all must pass)" now run as code — every requirement lands in at least one section, every section names a requirement it serves, and every section states what it is for with at least two implementation steps and how it will be tested. One new command, `check-plan-gates.py`, runs them in session; the same gates run again when the phase is marked complete.
