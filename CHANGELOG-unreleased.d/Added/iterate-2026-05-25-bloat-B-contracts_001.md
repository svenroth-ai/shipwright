Sub-Iterate B8 (Campaign B): introduce `shared/contracts/` cross-plugin contract surface. `shared.contracts.compliance` re-exports `collect_all`, `ComplianceData`, the supporting dataclasses, plus the canonical `PHASE_REPORTS` + `GENERATORS` dispatch from compliance's `update_compliance`. `shared.contracts.iterate` re-exports `is_io_boundary_change`, `touches_build_files`, and the risk taxonomy from iterate's `classify_complexity`. Adopt's `compliance_bridge.py` and the test plugin's `boundary_coverage_report.py` now consume the contracts directly — no subprocess, no ancestor-walk, no `_ITERATE_LIB` path constant.
