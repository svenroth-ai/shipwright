Adopted repositories now inherit the GitHub Actions pinning **rule**, not just the resulting state: `claude-md-template.md` explains why GitHub-owned actions stay on mutable major tags while third-party actions are SHA-pinned, that the reason is portability rather than cost, and that no dependency-updater config may be added. Previously an adopter got correct workflows with no explanation, so the first well-meant "let's pin everything" reversed the posture in good faith.
