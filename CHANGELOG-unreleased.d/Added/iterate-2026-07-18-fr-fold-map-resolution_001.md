Tests keep working after a spec clean-up regroups requirements. When a spec folds a detailed requirement into a broader one, it records the old ID in an `## FR-Fold-Map` table; a test still labelled with the old ID now counts as covering the surviving requirement instead of being reported as pointing at something that no longer exists. Previously this could turn a routine spec tidy-up into hundreds of false "orphaned test" findings.
