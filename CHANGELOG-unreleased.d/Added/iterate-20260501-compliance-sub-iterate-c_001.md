shipwright-compliance: Groups E + G land per Plan v7 Steps 7+8 — close-out of detective-audit Option Z. Group E (compliance-doc staleness) regenerates each tracked doc and byte-compares against on-disk; --fix rewrites stale docs and records paths in AuditReport.fixes_applied (threaded through run_all via per-run cfg injection). Group G (agent-docs freshness): G2 conventional-commit scope match against alias-map / stoplist / project_config splits (case-insensitive type parsing — Feat(...) / FIX(...) accepted), G3 ADR-N refs in commit BODIES vs decision_log.md (subjects intentionally excluded per Shipwright convention; unbounded-digit regex so format-mismatch surfaces as dangling-ref signal). Step 13 tuning: changelog added to default g2_stoplist after smoke-run. A4 default-config schema fix: plan_config.splits.{}.plan_file (multi-split, aiportal shape) + plan_config.spec_file (single-split, setup-planning-session shape); _walk_dotted_path extended with key{} dict-iteration syntax. Adopted-status decision documented: B1/B4 stay scoped to status=complete intentionally; adopted projects covered by B7 + Group G regardless of split status. /shipwright-compliance feature-complete; no slot reports not-implemented any more.
