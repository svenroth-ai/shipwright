decision_log.md and the decision-drops staging directory each get a generated index (lib/decision_log_index.py, lib/decision_drops_index.py), extending the ADR spec folder's INDEX.md producer pattern: pure render + locked/atomic rebuild, refreshed on every write. The decision-log index is committed and churn-allowlisted like the ADR index; the decision-drops index stays local-only since that directory is gitignored.
