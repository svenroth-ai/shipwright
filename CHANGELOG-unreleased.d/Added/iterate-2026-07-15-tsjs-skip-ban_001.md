TS/JS test-hygiene gate (traceability G6): the `scan_test_hygiene.py` probe now scans changed Playwright/Vitest/Jest specs. A committed `test.skip`/`it.skip`/`describe.skip`/`test.fixme`/`xit`/`xfail` must carry an `@quarantine` annotation (reason+owner+ticket+expires; a past expiry fails), and a focused test (`.only`/`fit`/`fdescribe`) is an unconditional failure. Diff-scoped by line so pre-existing untouched skips do not block (TT4).
