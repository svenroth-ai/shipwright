Requirement elicitation now follows one shared, rigorous method across /shipwright-project, /shipwright-adopt and /shipwright-iterate: one question at a time each with a recommended answer, facts looked up in the code instead of asked, wording challenged against the project's own glossary and stress-tested with edge cases, and no requirement treated as settled until every context dimension is answered or explicitly marked as an unconfirmed assumption.
