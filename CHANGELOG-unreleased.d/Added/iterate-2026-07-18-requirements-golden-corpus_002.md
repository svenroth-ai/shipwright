Five known-wrong behaviours in the requirements control plane are now pinned as current behaviour with `FROZEN-BUG` rationale naming the step that fixes each: a populated spec that fails to parse reports "nothing to check" instead of failing, an empty requirement set reports full coverage, the traceability matrix can display the wrong requirement text, and the requirements hygiene audit silently skips whole files in two situations.
