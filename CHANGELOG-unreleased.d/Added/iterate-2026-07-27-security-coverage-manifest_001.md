Security scans now say what they did NOT check. Every scan records one row per class of weakness — code flaws, vulnerable dependencies, leaked secrets, prompt injection — marking it checked, failed, not requested, or not available on this machine. A scanner that was never installed used to be invisible, so a machine with one tool produced a report that read clean for everything; those classes are now named as unexamined rather than clean.
